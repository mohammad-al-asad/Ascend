# Update Log

## 2026-08-25, later still (giving a story back its facts, and retiring a borrowed word)

Two things had been noticed earlier and set aside for later — a screen that told a story about recovery with no real events behind it, and a scale of concern that borrowed a letter already spoken for twice elsewhere in this same system, meaning the same letter now claimed three different things depending on which screen you were reading. Both were worth fixing properly rather than patching around.

For the first: rather than invent the missing events, went looking for what was already real and simply not yet gathered up. It turned out the pieces were all there — every flight already had a real, current count of who was actively working through recovery, and a real record of who was overdue for a check-in. Assembled those into an honest picture: how many are recovering right now, how many of those are current on their reviews, and — because it was already true and just needed saying — which flights are genuinely keeping pace and which aren't. Nothing invented. Just the truth that already existed, finally collected into one place, and handed to the part of the system that writes real sentences about it — which now writes a genuinely different sentence depending on what's actually happening in each flight, instead of the same hollow phrase every time.

For the second: gave concern its own honest vocabulary instead of reusing someone else's. The old label meant something else already, twice — a medical routing signal in one place, an injury-severity tier in another — so wearing that same letter a third time here would have meant three different things quietly sharing one costume. Swapped it for a plainly different one, tied to the exact same real underlying judgment as before, just no longer pretending to be a word it never was.

Tested both against real, freshly made-up-but-genuine flights and people — planted the exact conditions by hand, watched the numbers come back matching precisely what was planted, and read the actual sentence the system wrote about it afterward. It named the right flight, by name, and said the right thing about it.

## 2026-08-25, later (a photo doesn't need to be opened to be seen)

A small correction, right on the heels of the last one. A photo had just been wrapped up carefully, the way a sensitive document gets wrapped — sealed inside an envelope, needing to be opened and unpacked before anyone could actually look at it. Reasonable, for a document. Wrong, for a photo. A photo isn't something you unwrap — it's something you simply look at, the moment you arrive.

The two other things that had been wrapped the same way stayed wrapped, on purpose — a medical file and a shared attachment both genuinely need that envelope, because not everyone is allowed to see what's inside, and an envelope is exactly how you control that. But a profile photo was never actually guarded that carefully to begin with — anyone already could look at anyone else's, no envelope ever changed that. So unwrapping just this one thing didn't loosen anything real. It just stopped pretending a photo needed the same handling as a file that genuinely has to be handled with care. Now it's simply there — a real address, always the same one for a given person, that shows the picture the instant you point at it, with nothing to unpack first.

## 2026-08-25 (a warning sign mistaken for a locked door, and a house inspected room by room)

A message arrived that looked, at first glance, exactly like a security failure — a browser refusing to talk to the server, the familiar CORS wording, the kind of error that usually means a real door was left the wrong way. Traced it all the way through instead of guessing, one request at a time — first the polite handshake browsers do before a real request, which came back perfectly correct. Then the real request itself, and that one came back wearing someone else's name. Not this system's response at all — a warning page from the tunnel service standing between the phone and the server, put there for its own free-tier reasons, with none of the real headers on it because the real server had never actually seen the request. A locked door mistaken for the wrong keys. Confirmed it by asking the tunnel to step aside, and the real system answered immediately, correctly, exactly as it should have all along.

Then a much bigger question, the kind that deserves an actual walk-through rather than a guess: does the whole staff-facing side of this system — every role's screen, all eight of them — genuinely have something real standing behind it? The honest way to answer that isn't to skim; it's to open every single page and check every number, every button, every claim against what the system actually does. Sent that work out in six pieces running at once, one per role, each with the same instruction: don't report something as missing just because nobody's wired it up yet — that's a known, separate job. Only report it if it was never real to begin with, or if it claims something the system doesn't actually do.

Most of what came back was reassuring in a boring, correct way — screens still showing numbers from before a fix landed, which will simply resolve themselves once someone points the wiring at the current system. But a few findings were worth real attention. One page invents an entire idea — a "recovery program" — that a past version of this same work explicitly named as exactly the kind of thing not to build, because nothing real backs it. Another shows a number under a name that exists nowhere else in this whole system, standing in for a real one that already has a name everywhere else. A pastoral role's page imagines a whole journal of private reflections that was never built — only a much smaller, plainer note exists for real. And one page — the one with the most serious implications — offers a setting that would let a records-handoff role see a person's raw medical file directly, when the actual rule, written down on purpose, says that role should only ever receive a finished summary and never the file itself. Not a missing feature. A page describing power that was deliberately never granted, as if it already had been.

Brought all of it back and asked plainly what to do with it. The answer was: keep the record, build nothing. Which was the right call — nearly everything found lives on the side of the house that isn't this one to touch.

Later the same day, two small, honest fixes. A file — someone's photo, an uploaded document, a message's attachment — used to come back as itself: a picture, a document, raw and direct. Correct, by the usual rule of how these things are built. But the person actually using it said plainly that their own tools couldn't make sense of that shape, only of files wrapped inside a plain data envelope like everything else this system returns. So every one of those three doors was changed to match — the same picture, the same document, now handed over inside the same kind of package as any other answer, so nothing has to be treated as a special case anymore. And a door that had been open and working the whole time — asking to leave, gracefully, always subject to review — turned out to simply be missing from the map of doors, not missing itself. Added it to the map.

## 2026-08-24, later (a face for the profile, and two doors that were never cut)

Someone was building the piece that connects the mobile app to this system for real, and sent back a screenshot of what the profile screen actually returns today — a real response, not a mockup — with two complaints attached. No picture field. No way to update anything.

Checked both instead of taking either on faith. The second one was mostly wrong: there's a real endpoint that already lets a person edit their own settings — it just doesn't cover very much. The first one was simply true. There is no picture anywhere in this system, for anyone, and there never has been. Before touching anything, went back to the original written requirements to see whether a picture was ever actually asked for. It wasn't — not once, in the whole document. What was there instead was a line about not uploading photos at all unless specifically cleared for official use. So the honest answer wasn't "someone forgot this." It was "this was never asked for, and the one time photos come up at all, it's a caution sign, not a request."

That's the kind of moment where the right move is to stop and ask rather than decide alone — build past what was actually requested, or leave it out and say so plainly. The answer that came back was build it anyway. Fair enough — that's a real answer to a real question, and it's now built the same careful way everything else here handles a file: encrypted before it touches disk, a small size limit, only real image formats accepted, and nothing invented about what it means.

While in there, looked harder at the "no way to update anything" complaint, because it wasn't entirely wrong either — a person's own display name and their own login email genuinely had no path to being corrected by the person who owns them. A typo at sign-up would have just... stayed a typo, forever, with no way back. Fixed the name easily. The email took more care, because changing it also means changing how someone proves who they are the next time they sign in — so that one now asks for the current password first, and afterward asks the new address to prove itself too, the same way a brand-new account does. What didn't move: a person's role and their unit assignment. Those stay something only an administrator sets, on purpose — not because nobody asked, but because letting someone quietly hand themselves a different role is a very different kind of mistake than a misspelled name, and nothing about this request actually asked for that door to open.

Tested the whole thing for real before calling it done — uploaded an actual small image and got the exact same bytes back out, deleted it and watched the system correctly say there was nothing there anymore, tried a disguised executable and watched it get turned away, changed a name and an email address end to end and confirmed the old email genuinely stopped working while the new one immediately did, and went and looked directly at the system's own permanent record of what happened to make sure it remembered all of this correctly — and, just as important, that nobody's password ended up sitting in that record by accident.

## 2026-08-24 (a filter that already worked, plus one silent hole found while widening it)

Pulled the mobile app fresh and read through every real screen in it, comparing what each one describes against what this backend actually does. Almost none of it is wired up yet — same story as the admin dashboard, a fully-built interface with nothing connecting it to the real system underneath. But the interesting part wasn't the wiring gap. It was how *accurately* the interface described real backend behavior it was never actually calling — down to exact internal field names and an exact weekly reset rule, matching this backend's real logic almost word for word, on a screen with zero connection to it. Whoever built that interface clearly understood the real system deeply, even without touching it.

Out of everything checked, two things had genuinely nothing behind them. One is a full alternate sign-in system built around military ID cards — out of scope for today, correctly set aside as its own separate piece of work. The other was smaller and worth finishing: a message-content filter that already exists and already works — it catches a short list of sensitive terms and refuses to send anything containing one — but the interface describes it as having five levels of seriousness, and the real filter had exactly one level: caught or not caught.

Added the graduated version without touching how the filter actually behaves. Every flagged term still blocks the message exactly as before — nothing got looser, because loosening a safety check on my own judgment, with no one asking for that specifically, isn't a call to make alone. What changed is that a blocked message now carries a real severity number instead of just a yes/no, based on how specific and unambiguous the flagged term actually is — a literal coordinate reference rates higher than a common word like "mission" that shows up plenty of times with no operational meaning at all.

While in there, found something that had nothing to do with severity at all: when a message got blocked, it left no trace anywhere. Not a warning, not a log line, nothing — as if the attempt had never happened. Every other real action in this system writes a record of what happened; a blocked message quietly didn't. Fixed that too, since it was sitting right there and was clearly supposed to work like everything else. Tested by actually trying to send messages with real flagged content and confirming both the severity number came back correct and the attempt now shows up in the real record of what happened — and separately confirmed an ordinary, harmless message still goes through exactly as before.

## 2026-08-23, later still once more (two real numbers, and three honest refusals)

A whole status page this time, asked all at once — an uptime figure, a services table, session counts, a compliance badge, a backup timestamp, a rate table. Eight small numbers and two tables.

Different from most of today, though: two pieces of real machinery already existed on this exact page, built specifically to replace fake versions of it, sitting there unused by the screen itself. Checked every single value against them one at a time. Some lined up exactly — a cohort-size limit and a grace period both matched the real configured values precisely. One turned out to be simply the wrong number where a real one already existed, corrected earlier today. Another claimed a specific hour window that's real but stated backwards — the actual number is six times larger. One had no real counterpart at all — a percentage threshold where the real system only ever produces a plain word, never a number.

Asked to build what was still missing, and went through each one honestly rather than filling every blank. Two turned out to have a real, defensible answer sitting in already-tracked data: how many people have used the system in the last little while, and how many completed clinical handoffs are sitting fully approved but not yet actually sent. Built both — labeled the first one plainly for what it actually is, since this system doesn't keep a real record of active connections, only of when someone last signed in, which is a reasonable stand-in but not the same thing.

Three others got refused outright, on purpose. A percentage uptime figure, a backup timestamp, a days-since-last-incident badge — none of these have anything real behind them anywhere in this system. No process measures uptime. No backup job exists to report on. No incident-day counter runs anywhere. Inventing plausible numbers for any of them would have been exactly the mistake caught and fixed a dozen times already today, just wearing a new outfit. Left them alone, and said so directly rather than quietly filling the gaps with something that merely looked complete.

## 2026-08-23, later still yet once more again, more still (the one that turned out to be exactly right, and pointed at the biggest gap of the day)

One more heading on the same page: a small table titled with a number — nine of something required. Asked whether it was real before touching anything, the way today had already gone wrong once by assuming too fast.

This time the number was exactly, precisely correct. The actual requirements document has a table naming exactly nine specific reports this whole system is contractually required to produce, word for word matching what the screen's title claimed. That part wasn't invented at all — it was the most literal, verbatim-accurate thing found on this page all day.

But checking what the backend already builds against that same list of nine turned up a real mismatch, not a display problem this time. Five of the nine lined up cleanly. The other four didn't: three of the required nine had never been implemented at all — the underlying pieces of data for them already existed elsewhere in the system, built for other reasons over the past several days, just never assembled into the specific report the requirements document actually asks for. And the fourth slot was filled by three different homemade variants of a similar idea, none of them actually matching what was required — more effort than the requirement needed, spent in a direction that missed it.

Asked whether to just note that and move on, or actually go build the three missing ones and fix the fourth. Chose to build it properly. Wrote the real requirements list into the codebase directly from the source document, word for word — what each report must contain, who it goes to — so it can't quietly drift out of sync with a paraphrase later. Built the three genuinely missing reports from data that already existed, being explicit anywhere the requirement asks for something this system doesn't actually track, rather than filling that gap with something that only looks complete. Replaced the three mismatched variants with one real report that actually assembles what the requirement calls for. Added a way to see, honestly, whether each of the nine has ever actually been produced and when — nothing invented for the ones that haven't been.

Verified by actually running all four newly built reports for real, not just reading the code and assuming — watched the ordinary ones come back immediately and the sensitive ones correctly land in the same approval queue everything sensitive already goes through, then confirmed the tracking list updated to reflect exactly what had just happened. Opened one of the new reports' actual output and read real numbers in it, not sample text.

## 2026-08-23, later still yet once more again, more (a button that only ever did half of what it says)

Same page, one more piece — a table of recurring exports, each with an "Edit" link. Clicking it opens the exact same form used to create one in the first place, filled in with the existing values.

Checked what actually happens on save, and found the real version of that button could only do one thing: turn a schedule on or off. Rename it, change how often it runs, switch its file format, change who it goes to — none of that had anywhere to go, even though the form asking for all of it was sitting right there, fully built, just never connected to anything that could act on more than the on/off switch.

Widened it properly rather than bolting on a second endpoint: one real edit action now, where every field is optional and only the ones actually sent get touched — sending just the on/off switch, the way it always worked, still works exactly the same as before, so nothing that already depended on the old shape breaks. Changing how often something runs also recalculates when it's due to run next, the same math used when a schedule is first created, so it doesn't end up scheduled against a stale clock. What kind of report it produces stays fixed once created — the same form never lets someone change that either, so there was no reason to allow it underneath.

Tested it the way that actually proves something changed: made a real schedule, edited four things about it in one request, and confirmed all four landed and the next-run date moved correctly. Sent nonsense for the two fields with real allowed values and watched both get correctly refused. Sent an edit with nothing different in it and confirmed it correctly did nothing — not even writing a pointless note to the record of changes. Checked the real log entry for the four-field edit and found it spelling out exactly what changed, old value next to new, for every one of them.

## 2026-08-23, later still yet once more, again (a number that can't honestly exist until the thing it describes does)

A short side panel this time, listing a handful of past exports, each with a file size next to it.

Nothing tracks a size like that anywhere in this system. Simple enough to add — except this particular feature has an existing rule sitting right in its own documentation, written before today, that made the obvious approach wrong: these files are deliberately never saved anywhere after being built. Every time someone downloads one, it gets rebuilt fresh from live data, on purpose, specifically so nothing sensitive sits around in storage waiting to go stale or leak. So the file's actual size, at any given moment, is a fact that plainly doesn't exist yet for something that hasn't been rebuilt.

That's fine for most of these exports — the ordinary ones get built and handed over in the same instant, so their size is known the moment they're created, with no separate step required. But some exports need a second person's sign-off before anyone can have them at all, and for those, nothing gets built until that approval happens and someone actually downloads it. Saving a size before that point wouldn't be estimating — it would be lying about something that hasn't happened yet.

So the rule for this field became: record it exactly once, the moment real bytes are actually produced. For an ordinary export, that's immediately. For one still waiting on approval, the field simply reads "not yet known" — correctly — right up until someone approved it and someone downloaded it, at which point the exact real number finally exists and gets written down for good. Confirmed both cases the only way that actually proves anything: created one of each kind for real, watched the ordinary one report its size instantly, watched the restricted one sit empty through approval and only fill in the instant it was actually opened — and checked that number against the same file's real, independently-reported size on the way out the door. Matched exactly, both times.

## 2026-08-23, later still yet once more again (a label I'd already refused to copy, for a reason worth re-reading)

One more popup on the same page: a confirmation dialog, showing five pieces of information about an action waiting for a second person's approval before it goes through — what it is, what it targets, what slice it covers, what it affects, and how risky it's rated.

Same story on the button underneath it — clicking "approve" only erases a row from a list living in the browser tab, never actually approves anything for real. Two of the five fields already lined up closely with real information already tracked elsewhere. One field didn't line up at all, and going back to check why turned into the more interesting find of the two.

The risk label on screen uses a short code — a letter and a number. Digging into the code that computes something similar for real turned up a comment already sitting there, written before today, explaining a decision already made: that exact style of code is deliberately avoided here, because the same letter-and-number pattern already means something completely different elsewhere in this system — a clinical escalation level tied to an individual's care. Reusing it for something as unrelated as export risk would be a genuinely dangerous kind of confusion in a system handling health data. So the real version uses plain words instead. That wasn't a gap. It was already correct, and re-reading the old reasoning confirmed it rather than changing anything.

The other missing field — which slice of the organization or dataset something touches — had nothing behind it anywhere, and this one really was just missing. Filled it in from data that was already being tracked at the moment each of these approval requests gets created, just never summarized into one line before: whose role and unit for a role or account change, what kind of clinical content and whose for a records handoff, and how sensitive plus who it's going to for an export. Tested by actually creating one real request of every kind that requests, not by reading the code and assuming it would work — a real restricted export, a real account deactivation, a real clinical handoff, and a real admin-level role change — and checking that each one's summary line came back correct.

## 2026-08-23, later still yet again once more (chased a placeholder name back to where it actually lived)

A modal this time — a form for scheduling a recurring export, with a field asking who it should go to and another asking what slice of data it covers.

Two of the four fields in that form had nothing behind them anywhere. Went looking at the code for the button that submits it, and the trail went cold fast: nothing in that whole page ever calls out to anywhere else. It just edits a list sitting in the browser's own memory. Some of the entries in that list, sitting right there in the screenshot, were obvious throwaway placeholder words — and searching the entire codebase for those exact words turned up nothing. Someone had typed test text into that form once, clicked submit, and it went precisely nowhere, because there was nowhere for it to go.

One of the two empty fields — who receives the export — wasn't actually a new idea. The requirements document already describes this exact piece of information as part of every export record, and the system already tracks it for one-off exports, just quietly filling it in with "whoever clicked export" instead of asking. Made it a real, required question here too, and reused the honest fallback for the case where nobody was asking before.

The other field — what slice of data — could have been added the same easy way: a text box that just gets saved as typed. Didn't do that, on purpose. This system already has a real, working way of deciding whether a given kind of report is broad-and-safe or narrow-and-sensitive, and that decision already controls something serious — whether a second person has to sign off before it goes out. A second, hand-typed field claiming to say the same thing would just be a second story that can quietly stop matching the first one. Used the real one instead, computed automatically from what kind of report it is, the moment the schedule is created.

Didn't stop at proving the form saves correctly, either — actually advanced the clock on two real schedules and ran the same function the hourly job runs, to watch what happens when one of these finally executes for real. Both came out right: the everyday one went straight through with the right person's name attached, and the sensitive one landed in the same second-reviewer queue every other sensitive export already goes through, also correctly labeled.

## 2026-08-23, later still one more time again (a promise made in a comment, never actually kept)

One more page, one more set of four small number cards at the top. Checked the page's own code first this time, out of habit by now — all four are typed-in numbers, same as everything else today.

But two of the four already had a real, working source behind them — a daily total and a comparison against the week's average, both computed correctly, just never connected to the screen. The third and fourth had nothing. And digging into the fourth one turned up something odd: the function that computes these numbers already had a sentence in it, written earlier, saying it returns a count of serious/reviewable actions. It didn't. It never had. Someone — past me — wrote down that the work was done before actually doing it, and nothing since had caught the gap until this exact card forced a re-read.

The count itself wasn't a hard call to make correctly, once found — there's already a real queue in this system for actions serious enough to need a second person's sign-off, and its own internal description already calls itself exactly that: a queue for serious admin actions. Counting what's in it, and how much of it is still waiting on that second signature, is that promised number. Wired it in and checked it by hand against the same queue counted a second, independent way — matched exactly.

## 2026-08-23, later still one more time (walked the whole page, found the same story four times over)

Kept following the same trail onto a different page — the one that shows who can see what, at what level, and under what size limit. Circled a piece of it, asked whether it was real.

Went through it panel by panel this time instead of stopping at the first one. A unit picker with six names that don't exist anywhere in the real system. A cohort-size selector that only pretends to save anything — clicking it changes nothing outside the browser tab. A box of "resolved paths" that's three paragraphs of text typed directly into the page, completely disconnected from whatever unit or role is actually selected above it. A table further down whose columns have the right names but wrong values — it claims two particular roles have a restricted view at the top organizational level, when the real system gives them no view at all up there, restricted or otherwise. And a second table where two whole columns are the exact same sentence, word for word, copy-pasted onto every row regardless of what row it's on.

Every one of those four panels turned out to be disconnected from the real system, which already has working, tested endpoints sitting right behind all of it, unused. Confirmed that by calling each one directly and getting back real, sensible numbers.

One piece of what was circled had nothing behind it anywhere — not in the requirements, not as a byproduct of any other real feature. A record of who approved a given specialist track being offered, and when it was actually switched on. Asked directly whether to build that as new, standalone work even without a requirement pointing at it, and the answer was yes.

Built it as a small two-step state machine: approved, then separately enabled — matching how the screen described it, and refusing to let something be turned on before it's been approved. Neither step requires a second person to sign off, unlike the handful of genuinely high-stakes actions elsewhere in this system that do require that. Tested the refusal paths deliberately: tried to enable before approving (correctly blocked), tried to approve something that doesn't need approval at all (correctly blocked), then ran the real sequence through cleanly and watched the surrounding table update to reflect it.

## 2026-08-23, later still once more (the one column that really was empty)

Followed the same table one step further. Circled two columns and asked directly whether they were missing. One of them, it turned out, already had a real answer as of the last fix — it just hadn't been wired up on the screen yet. The other one genuinely had nothing behind it anywhere, on either side.

Went looking at the actual screen's source code to be sure, and found the whole table is just a list of numbers typed directly into the page — not fetched from anywhere. It even invents a few categories that don't correspond to anything real in the system at all, and marks one question as scored in a special reverse way that, checked directly against the code that actually does the scoring, it isn't. That's a frontend problem, not something for this backend to fix, and matches a boundary already agreed on earlier this project: wiring the two together is someone else's part of the work.

But one of the two circled columns pointed at something real underneath the fake table — a whole feature, described in the requirements document, that had simply never been built: a record of which version of the question set is currently approved, who approved it, when it took effect, and when (if ever) it was retired. Asked directly whether to build that piece for real, and the answer was yes.

Built it as its own small, versioned record — the same pattern already used elsewhere for scoring configuration, plus a genuine "retired" state that one doesn't have. Creating a new version doesn't erase the old one; retiring is its own separate, explicit step, which matches the requirement's own reasoning: never break the history of what a past score meant. Tested the whole shape of it end to end — nothing active, then something active, then that same thing showing up automatically where the registry reports it, then retired, then correctly refusing to retire it twice, then back to nothing active. Every step checked against the real database, not assumed.

## 2026-08-23, later still yet again (asked "shouldn't this be backend?" and it turned out I owed an apology)

Got shown a live screenshot of a table on the same dashboard — a question registry, 46 rows, split into four groups labeled O, D, W, and M. Asked directly whether it was real, or made up on the frontend like a few other things had turned out to be earlier today.

Checked the live endpoint. It said 40, not 46. And a note I'd written when building it, months of work ago in the same conversation, explicitly claimed the "D" group in the design didn't correspond to anything real — that it looked like an invented sixth category with no backing.

That note was wrong, and re-checking it properly is what caught the mistake. The "D" group isn't a fabricated category at all — it's the daily check-in questions, six of them, already live in production code, driving actual daily check-ins right now. They were just never counted because the piece of code that builds this registry only ever looked at three of the four question sets that exist. Once the fourth is included, the real total is 46. Exactly what the design showed. The screen wasn't inventing a number — the backend was undercounting.

Fixed the count, and while in there, added one more thing honestly: each question's scoring direction, "higher is better" or "lower is better," computed the same way the real scoring code computes it rather than guessed at. Every single question in the system currently scores the same direction, so that part checks out too.

One thing still doesn't have a real answer behind it: a "validation" status shown per question. Nothing in the requirements or the codebase tracks that at the individual-question level — the closest real concept is a whole different feature (versioning and approval for the question bank as a set) that was never built. Left that column alone rather than inventing a status for it, and flagged it as a real gap if it's wanted.

## 2026-08-23, later still again (a card with six numbers, and one of them was lying)

Fourth gap on the same dashboard today: a summary card up top with six metrics — account status, onboarding, access expiration, assigned providers, effective permissions, purpose consent. None of it existed on the backend. Nothing in the requirements document mentions it either, so unlike some of today's earlier fixes this one has no paper trail to point at — it's real data built to serve a screen that Figma invented on its own.

Most of the six were straightforward: count active accounts, count expired ones, list who's automatically assigned (Fitness and Injury-Recovery, same as everywhere else in the app), point at the RBAC matrix already built earlier today for "effective permissions," reuse a consent summary that already existed. Two of them weren't straightforward at all.

The access-expiration panel's mock copy says access renews automatically every year. It doesn't — the code that manages this field says so directly, in a comment written back when that feature was built: nothing renews it, an admin has to do it by hand. Asked which way to go rather than guessing, and the answer was to keep the real behavior and say so plainly, instead of quietly matching a screen that's wrong.

The onboarding count needed no such conversation, because the bug it was hiding wasn't a judgment call — it was just wrong. First pass counted every account whose onboarding wasn't marked complete, and got back 116 of 117. Almost nobody, it turned out, had actually finished onboarding — except that "onboarding" isn't a thing staff and admin accounts ever go through in the first place. Only field operators have that permission at all; every other role's status field just sits at its starting value forever because nothing ever moves it. The real number, once scoped to the role onboarding actually applies to, is 65 — checked by hand against a direct breakdown of every account by role and status before trusting it.

## 2026-08-23, later still (a one-line fix that would have been wrong)

Third gap on the same dashboard today: the people table has a "Last edit" column, and nothing was filling it. Every other column in that table — name, email, role, unit, status — already had real data behind it. That one didn't.

There was an obvious quick fix sitting right there. Every account record already carries a "last updated" timestamp, so returning it would have been a single line. But checking what actually writes to that field showed it moves whenever the record is saved *for any reason at all* — including someone simply logging in, or filling out their daily check-in. A column labelled "last edit", on a screen an admin uses to manage roles and permissions, that quietly changes every time the person opens the app, is worse than an empty column. It looks like information and isn't.

So it's built from the audit trail instead — the log that already records exactly the things that count as editing an account: someone's role changed, their unit reassigned, access renewed, password reset, account created or deactivation requested. Logging in is recorded there too, but it's deliberately excluded, because signing in isn't an edit. This also happens to be what the screen's own policy banner already promises: every change writes to that log.

For accounts nobody has ever touched, the answer is honestly nothing rather than a made-up date — so the column comes back empty, with the account's real creation date supplied alongside if the interface would rather show that.

Worth recording the testing misstep, since it nearly passed: the first check picked an account that had never been edited, logged in as them, and confirmed the timestamp "didn't change" — which proved absolutely nothing, since it was empty to begin with and stayed empty. Redone against an account carrying a real timestamp: three logins in a row left it untouched, then one genuine edit moved it. That's the test that actually demonstrates the distinction the whole decision rests on.

## 2026-08-23, later (finishing the sentence the last feature started)

Same day, same dashboard, one screen over: the profile page has a "Change password" box. That didn't work either — and this one is a slightly more interesting story than the last, because the requirements document never mentions passwords at all. Not once. So there was no requirement to point at.

Built it anyway, on the strength of two things this system was *already* doing that quietly assumed it existed. First: when an admin resets someone's password, the email they get says "sign in and change it as soon as possible" — advice that was, until today, impossible to follow. Second: the "add a person" feature finished earlier this morning hands a brand-new user a password chosen by the admin. With no way to change it, that admin would keep a working key to that person's account indefinitely. Neither of those is acceptable, and neither needed a requirements doc to justify fixing.

Worth being clear about what makes this different from the password-recovery flow that already existed: recovery is for someone locked out, so they prove who they are with a code sent to their email. This is for someone already signed in, so they prove intent by typing the password they currently have. Different problem, different proof, both needed.

Tested it as an actual sequence rather than as an isolated endpoint, because the point of the feature is the handoff: an admin created a real account, the new person signed in with the admin's starter password, changed it to their own, and then the old admin-issued password stopped working while the new one worked. That last step is the whole reason this matters. Also confirmed the obvious refusals — wrong current password, mistyped confirmation, trying to "change" it to the same password, a too-weak new one, and an unauthenticated attempt — and confirmed the audit trail records that a password changed without recording either password.

## 2026-08-23 (the one thing nobody could actually do: add a person)

Someone pointed at a screen in the admin dashboard — the little "Add person" window where you type a name, an email, pick a role, and get handed a starter password to pass along — and asked whether it actually worked behind the scenes. It didn't. Anyone could already be *edited* once they existed: change their role, move them to a unit, deactivate them, reset their password. But there was no way to create somebody in the first place. The only path into the system at all was people signing themselves up.

Checked the requirements document before building anything, since a screen existing in a mockup doesn't make it a real requirement — and this one is spelled out plainly: the admin, or an approved roster import, is supposed to be what creates accounts, and signing yourself up is meant to be the minimal exception, not the main road. So this was a genuine hole, not a nice-to-have.

One decision worth recording: earlier this month we deliberately made the *password reset* feature email-only, never showing the password on screen, for good security reasons. This new screen does the opposite — it hands the password straight back to the admin to relay in person. That looked like a contradiction, so it got raised before building rather than quietly resolved either way. The distinction holds up: resetting an existing person's password is a different situation from setting up a brand-new account for someone standing in front of you, and the requirements doc doesn't dictate how the first password should travel. Either way, it never gets stored in the audit trail — that part is unchanged.

Verified the whole thing against the real system rather than trusting it: created accounts both ways (letting the system generate a password and supplying one), signed in as those brand-new people immediately afterward to prove the passwords actually work, confirmed a duplicate email is refused, confirmed a regular admin can create a coach but cannot quietly create another top-level admin, and confirmed the audit trail records who did it without ever recording the password itself.

## 2026-08-19 (testing everything, on purpose, and it paid off)

Built a Postman collection covering the whole backend - the mobile app's side and the staff dashboard's side kept as two separate sections in one file, so the two people building against them can work independently without stepping on each other's login sessions. Wrote a proper handoff document for whoever's building the mobile app: how auth works, what every screen's data looks like, and the one part that's genuinely easy to get wrong (a few onboarding questions quietly require a follow-up answer or the save gets rejected).

Then, rather than call it done, actually ran through nearly everything in both collections against the real, live server - not just the mobile side, which had already been checked earlier, but the entire staff dashboard too: every admin screen, every provider role's dashboard, the whole approval-queue workflow, report exports, the works. Roughly two hundred real calls in total across both passes.

The mobile side came back clean. Every single "failure" traced back to the test itself doing something wrong - forgetting a required field, using the wrong shape for a request, trying to message someone before the two accounts were actually linked as coach and operator.

The dashboard side turned up one real, honest bug: nine different "create something" actions across the admin screens were quietly returning the wrong success code - technically fine, nothing was broken for a user clicking a button, but a status code that doesn't match what every other create action in the same file returns is exactly the kind of inconsistency that trips up whoever's writing the frontend against it, since they'd reasonably assume the same convention holds everywhere. Fixed all nine, restarted the server, and checked each one again to be sure. Also chased down two things that looked like bugs at first glance and turned out not to be: a report-generation screen that takes a genuinely long time because it's writing real AI-generated prose section by section (confirmed it actually finished successfully in the background even after the test gave up waiting), and an endpoint that correctly refuses to let a coach look up a recommendation that isn't theirs to look up - by design, not by accident.

## 2026-08-13 (one small note concept, shared across three specialist roles)

Three separate gaps - a clinical SOAP-note editor for Mental Performance, a pastoral-note editor for the Chaplain, and no note-taking at all for the Nutritionist - had each already been checked against the requirements doc earlier this session and correctly turned down, because the doc is explicit that the app should never become a medical record, a chaplain record, or any kind of official clinical documentation. That verdict hadn't changed. But reading the doc again, more carefully this time, turned up a different, smaller idea sitting a few pages over: a genuinely lightweight note - just a date, which kind of specialist wrote it, what the person's concern was, what action got assigned, whether follow-up is needed, and a status - explicitly described as something to support ongoing coaching, not to become clinical or privileged documentation. That's a real, different thing from what was rejected, and it happens to be worded generically enough to cover all three specialist roles at once rather than needing three separate builds. Confirmed with the user, then built it once, shared.

The one real design decision made beyond what the doc specifies: a note is only visible to the same kind of specialist who wrote it, plus Admin - a Chaplain's notes don't leak into the Nutritionist's view of the same person. Nothing in the doc says that explicitly, but it matches how every other kind of sensitive information in this system is already handled - visible only to the people who genuinely need it, not shared by default just because it's technically in the same account. Also caught and fixed, before it could cause a silent bug, the same kind of type-mismatch mistake that had already bitten this project twice before - the fix is now the third and hopefully last time it needs discovering the hard way instead of just applying it up front.

Verified it end to end with two real specialists writing genuinely independent notes about the same real person: confirmed each specialist could only see their own note, confirmed Admin could see both, confirmed a specialist could close their own note but was correctly blocked from closing someone else's, and confirmed both actions left a real, findable trail in the audit log rather than just trusting that they had.

## 2026-08-13 (building only the honest third of a five-part table)

Asked directly whether the SCS "workload by flight" table was even possible to build for real. Went through it column by column instead of guessing. Two of the five columns - training lanes and staff capacity - don't correspond to anything tracked anywhere in the system; there's no source of truth to pull from, so building them would mean making numbers up. A third column, a weekly staff-availability grid, would need providers to actually submit their own availability somewhere first, and no such input exists anywhere in the app - that's a real feature on its own, not something to bolt on here. The remaining two columns, though, map cleanly onto real data that already exists: how many people in a given flight currently have an active reconditioning plan, and what share of that flight that represents. Built exactly that, reusing the same real, privacy-gated way flights get grouped and counted everywhere else readiness numbers are shown by unit.

Verified it against real data rather than trusting the math on paper: confirmed a flight with too few real people in it stayed correctly excluded, checked directly that a flight showing a zero count genuinely had zero people with an active plan rather than the number being wrong, then gave one real person in that flight an active plan and confirmed the count and percentage both moved to exactly the right numbers afterward.

## 2026-08-13 (a gap that turned out not to be one)

Picked "feedback sessions" as the next item off the gap list, expecting to build it. Checked the requirements doc first anyway, out of habit, and confirmed the underlying field really is a real requirement. Then went to actually look at the code before writing anything new - and found it already existed. A report method that aggregates exactly this had been built and verified days earlier, during an entirely different pass, and was already wired into the Leadership dashboard. The gap-analysis document itself was simply wrong about this one - whatever check produced it missed that method. Fixed the document instead of building anything. Worth writing down not because it's interesting on its own, but because it's a reminder that the gap list is a snapshot, not gospel - it's worth a quick look at the actual code before trusting an old "not built" verdict, even one written earlier the same day.

## 2026-08-13 (OFT for providers, and a driver flag that isn't made up)

Went back to the very first gap-analysis pass, from before the branch update, and picked up the first item still sitting open: SCS staff have never been able to look up one specific operator's OFT test history directly - only the operator's own account could see it. That's just missing wiring around something that already fully works, so it was quick to add.

The second half needed more care. The mockup shows little tagged chips per operator - a sleep flag, an OFT flag, and so on - color-coded by readiness driver. Checking the requirements doc for that specific idea found nothing, but it did turn up something real and adjacent: a six-level escalation scale (L0 through L5) that this project already uses elsewhere, and that's already being calculated - quietly, per recommendation - without ever being shown on this particular screen. Rather than invent the per-driver chips the mockup shows, the real number already sitting there got surfaced instead.

Verified the OFT lookup returns exactly what the operator sees themselves, confirmed a role with no business looking at someone else's data gets correctly refused, and confirmed an operator with nothing currently flagged shows up as genuinely flag-free rather than some default placeholder. The one case that needed a real elevated flag didn't have a live example currently assigned to a test provider to click through end to end, so that specific value was confirmed by looking directly at what's stored rather than pretending to click a button that wasn't actually available to click.

## 2026-08-13 (giving "send to PT/IM" its own real state)

Fourth item off the branch-update list. Earlier today, recommendations that need both SCS and PT/IM's attention got a real label - "joint coordination item." What that label didn't have yet was any sense of where the conversation stood: has anyone actually sent this for PT/IM to look at, and has PT/IM actually signed off on it. The frontend clearly expects that distinction, so it got built for real, tightly scoped to just that one thing rather than expanding into a bigger workflow.

The two new actions both check something real before they do anything. Sending something "for signoff" only works if it's actually the joint-coordination kind of item - trying it on an ordinary recommendation is correctly refused. Signing off only works if it was actually sent first - there's no way to sign off something nobody flagged for review. Signing off itself is restricted to PT/IM, matching how PT/IM has consistently been treated as the primary reviewer everywhere else this project touches injury or coordination decisions.

One small mechanical side effect: displaying who signed something off meant looking up that person's name, which meant a piece of code that used to run instantly now needs to wait on a database call - so every place in that file that used it needed a small adjustment to match, purely plumbing, no behavior changed anywhere it wasn't supposed to. Verified that directly by re-running an entirely unrelated action on an ordinary recommendation afterward and confirming it worked exactly as before. Also verified the two new guarded actions rejected exactly the situations they should and succeeded in exactly the one they should, with a real audit trail entry for each.

## 2026-08-13 (PT/IM restrictions that actually enforce their own release condition)

Third item off the branch-update list, and another one with no real requirement behind it - checked the requirements doc for "restriction" and "phase" as gated concepts and found neither exists in any structured way. The user chose to build it anyway as genuinely useful tooling, same as a couple of earlier additions today.

The interesting part was making sure the gate actually means something. The mockup's language - a restriction stays blocked "until Phase 3 signoff" - describes two separate real conditions, not one. So releasing a restriction now genuinely checks both: has the operator's real reconditioning phase actually reached the phase this restriction requires, and has a provider explicitly signed off to release it. Reaching the required phase by itself changes nothing - the restriction stays active until someone deliberately releases it, which is exactly what "signoff" is supposed to mean instead of being decoration.

Verified the whole sequence for real: added a restriction while the operator was still in an earlier phase and confirmed it correctly refused to be released, with an honest error explaining exactly which phase it was waiting for. Then advanced the operator's real phase and confirmed the restriction correctly recognized it was now eligible - but stayed active until the explicit release call came in, which then succeeded and left a real timestamp and a permanent entry in the same event history built earlier today. Also confirmed none of that touched the existing ROM data or leaked onto an unrelated operator's record.

## 2026-08-13 (admin password reset - built more securely than the mock)

Second item off the branch-update list, and this one came with a real disagreement with the mockup worth pausing on. The frontend shows an admin clicking "reset password" and immediately seeing the new password on screen. That's fine for a prototype with no real backend behind it, but doing that for real - returning or logging a plaintext password anywhere - is a genuine security mistake, not a style choice. Raised that directly before writing any code, and got confirmation to build it the safe way instead: generate a real temporary password, change the account's actual password to it, and only ever send it by email - never show it back through the API, never write it into any log. If email sending isn't actually configured in this environment, the password still gets changed for real, but the response says so honestly instead of pretending it was delivered.

Verified all of it for real: logged in with the account's old password after the reset and confirmed it was genuinely rejected, checked the API response and the audit trail entry and found no trace of the actual password value in either, and confirmed a different, already-existing admin action in the same file still worked exactly as before, untouched by this change.

## 2026-08-13 (reason category for the reveal-field flow)

First real item off the branch-update delta list. The frontend's redaction UI wants a category picked before anyone can request to see a hidden field - not just a free-text reason, a specific bucket like "escalation" or "audit." The real unredact endpoint already required a written reason; this adds the category on top, and makes sure it's not just decoration - it gets folded straight into the same permanent audit trail every other action on a medical record already writes to, so a category picked in the UI actually shows up in what an admin or PT/IM can review later. Tested a valid category going through cleanly and showing up correctly in the log, an invalid one being rejected, and confirmed the one-time-reveal behavior and PT/IM's normal full access were both completely unaffected by the change.

## 2026-08-13 (ROM tracking + real reconditioning timeline - last gap-analysis item)

The final item on the gap-analysis list, and checking it against the DOCX came back empty on both counts - there's no range-of-motion concept anywhere in the requirements, and the word "timeline" only ever refers to compliance due-dates, never a chronological view of someone's recovery. Asked whether to build it anyway, purely as useful tooling rather than a requirement, and the answer was yes.

The interesting design question was what a "timeline" should even mean if nothing forces it to be honest. Building a timeline as its own thing that someone types into risks it drifting from what actually happened in the real plan. So instead, it isn't authored at all - every time a real field on someone's reconditioning plan actually changes (their phase, their clearance status, their severity), that change writes itself into a permanent, real event log automatically. Changing an unrelated field, like a scheduling note, writes nothing, because nothing about their recovery actually changed. Adding a real measurement writes its own entry too. A plan that existed before this feature simply has a shorter real history - which is the honest answer, not a gap to paper over.

While verifying this live, found a genuine bug: the new routes that let a provider look up someone else's timeline were comparing a plain text ID against a database field that expects a different internal type, so the query silently matched nothing and always came back empty - even though the data was really there. This is the exact same category of bug caught and fixed once before in this project, in a completely different feature, so the same fix applied cleanly. Also ran into a second real outage with the database provider mid-testing, identical to one hit earlier in this project - waited for it to genuinely come back rather than assuming success, then finished the interrupted checks. Everything after that passed cleanly: real events appearing exactly when they should and not otherwise, real measurements matching across both the provider's view and the operator's own view, and the full sequence of events matching the real order things actually happened in.

## 2026-08-13 (Plan-link category + real IDMT documentation handoff)

The fifth gap-analysis item turned out to be two things bundled together in the frontend mock, and checking the DOCX split them apart cleanly. The "SCS coordination items" table - a whole list of open items with triggers, owners, and statuses - has almost nothing behind it in the requirements. The DOCX mentions the idea in exactly one sentence, as a label a plan-link can carry, not as a thing with its own fields and workflow. So instead of building the fabricated table, added the one real thing the DOCX actually asks for: a tag on each recommendation stating which of five real categories it falls into. Two of those categories are inferred automatically from who a recommendation gets routed to; a third, "joint coordination," is a real judgment call the DOCX names explicitly (pain, a failed test, declining recovery, an uploaded record affecting training decisions) - so that one is a deliberate flag a provider sets themselves, not something guessed at.

The IDMT handoff half went the opposite direction - checking the DOCX for it turned up a fully specified feature, not a vague mention. It names the exact fields a handoff record needs, requires tracking three separate real states (prepared, transmitted, acknowledged) rather than collapsing them into one, and is explicit that raw medical records must never be sent without an approved role and an audit trail. Built that for real: a new handoff record, a service that walks it through its real lifecycle, and reused the same two-person approval queue that already gates sensitive report exports - a second admin has to sign off before anything moves, exactly like the existing restricted-export flow. What gets handed to IDMT is never a copy of an uploaded file - it's a real generated summary built from the operator's actual reconditioning status, how many records of each type are on file, and their OFT status, using the same PDF and CSV rendering code already built for other reports.

Tested the whole path end to end with real accounts standing in for each role: a PT/IM provider prepared a handoff, a different admin approved it, IDMT correctly couldn't see it until it was explicitly transmitted, and once transmitted could download it, acknowledge it, and correctly got blocked from acknowledging it a second time. Pulled the actual generated summary content directly to confirm it matched the real underlying data exactly, and confirmed the existing approval-queue types (role changes, exports, deactivations) still worked normally alongside the new one.

## 2026-08-13 (`Plan` dashboard - a decision, not a build)

The fourth item on the gap-analysis list was different from the others - the user asked to resolve the decision first, before writing any code. The frontend has a full "Plan" dashboard: authoring a plan once, routing it with read/write/notify permissions across five different provider roles at the same time, tracking cohort-level progress. Checked the DOCX specifically for that workflow - not just the word "Plan," which had already been checked once before on an earlier screen and found to not exist as a role - but the actual cross-role routing idea itself. It isn't there either. Every plan-like concept the DOCX does define belongs to one owner, or at most to the SCS-and-PT/IM pair that's already built. Nothing spans five roles at once anywhere in the requirements. That lines up exactly with the earlier, separate finding from a different screen, which is a reasonable signal this isn't a blind spot - it's just not part of the real requirements. Decided to skip it and record that decision plainly in the gap-analysis doc rather than leave it as an open question. No code was touched this pass.

## 2026-08-13 (Nutritionist meal-consistency/hydration trend)

Third item off the gap-analysis list. The frontend's Nutrition dashboard mockup shows a full food-logging system - individual meals with calorie and macro breakdowns, a donut chart for carbs/protein/fat. Checked it against the DOCX first, and there's simply nothing there to support it - no calorie tracking, no food items, not even the word "consult" appears anywhere. What the DOCX does say about the Nutritionist role is different in kind: they should be able to review meal consistency, skipped meals, hydration, and energy/recovery patterns - trends and patterns, not a diet diary. Went back to the user with that distinction and they chose to build the real, DOCX-grounded version instead of the food-logging mockup.

That meant finding what real data already exists that maps to those DOCX phrases. It turned out there are five real check-in questions - daily, weekly, and monthly - that already ask operators about fueling, hydration, and meal consistency, all tagged as feeding into "Nutritional Readiness." One phrase in the DOCX, "quick/processed meal patterns," doesn't have a matching question anywhere - checked the entire question bank to be sure - so that part was left out rather than invented. Built a real trend view for Nutritionist providers on top of the other four, reusing a pattern that already existed elsewhere in the codebase for detecting whether a score is improving or declining over time, rather than writing new detection logic from scratch. It's wired into the Nutritionist's existing dashboard endpoint - no new endpoint needed - and it only appears for that one role; Mental Performance and Chaplain use the same underlying endpoint but never see this data.

To verify it for real, inserted two genuinely different weekly answers three weeks apart for a test operator - one at the worst end of the scale, one at the best - using the same scoring function the real check-in flow uses, not hand-picked numbers. The Nutritionist's dashboard correctly read that as an improving trend, correctly counted the one low-scoring answer as a flag, and correctly left the Mental Performance dashboard untouched when checked separately.

## 2026-08-13 (PT/IM injury taxonomy)

Next item off the gap-analysis list after the redaction work: PT/IM's injury tracking. Checked it against the DOCX before touching anything and found it wasn't a clean yes or no - it was both at once. The DOCX does describe a real injury record with fields like status, a limitation flag, a rehab strategy summary, and SCS coordination status - and most of that already existed in the codebase under different names, just not all of it. Three fields were genuinely missing and got added. The DOCX's quarterly injury report, on the other hand, turned out to already exist - built and verified earlier in this same project, before this pass ever started. What didn't exist anywhere, in the DOCX or the code, were the things the frontend mockup implied: diagnosis codes, a severity scale, "days out" tracking. The DOCX is explicit that this app isn't a diagnosis tool, so those don't have any grounding there either. Asked the user directly whether to build severity and days-out anyway as a deliberate business decision rather than a requirements-driven one, and they said yes - so those went in too, clearly marked as a choice made outside the DOCX rather than something it called for. Days-out isn't stored as a number that could go stale; it's calculated fresh every time from the date the injury was reported, the same way the existing "days in phase" figure already worked. Extended the quarterly report to show all of it, plus one honest real count of how many injuries fall into each severity level - not a rate, not a percentage, just what's actually there. The CSV and PDF exports picked up the new columns automatically, without needing a single line of export code touched, which was worth confirming directly rather than assuming.

## 2026-08-13 (ascend-admin gap analysis + medical-record field redaction)

The user pointed out a second, separate project in the same workspace - `ascend-admin`, a Next.js frontend with its own auth and admin dashboard, both entirely local mock data with no real backend calls anywhere. First instinct was to ask whether to wire it up to the real backend; the user said no, that's the frontend dev's job, and redirected to something more useful right now: read each of its dashboard pages for real and write down what backend endpoints they'd actually need, what's already built, and what's missing.

Read all 9 role dashboard pages plus the role-selection landing page in full (not just screenshots - the actual TSX, which turned out to reveal things a screenshot never would, like a question-registry count that doesn't match the real backend's, and two different pages quoting different mock numbers for the same SCS hours figure). Cross-referenced every real data need against the real, already-built backend routes and wrote it all up in `docs/ASCEND_ADMIN_ENDPOINT_GAP_ANALYSIS.md`. The short version: Admin and Leadership are fully covered already - every tab on both has a real endpoint behind it. The real gaps cluster around PT/IM (there's no injury diagnosis/severity/taxonomy anywhere, so nothing can compute a quarterly injury-rate report from it), Nutrition (food/macro logging doesn't exist as a concept at all), and one dashboard - "Plan" - that has no backend role behind it whatsoever.

Asked which gap to tackle first; the user picked "Provider notes," the clinical/pastoral note-taking idea that shows up on the MP, PT/IM, and Purpose Coach pages. Before building anything, checked it against the actual requirements DOCX, which turned out to say the opposite of what the mockup implies: the app is explicitly supposed to *not* become a medical record or "official clinical documentation," chaplain documentation included. So instead of building something the requirements document actively warns against, went back with that finding and got redirected to the one thing in the same neighborhood that the DOCX does call for: masking sensitive record fields before they're shared outside the clinical team.

Built that for real. Non-clinical roles like SCS have never had any access to medical records at all - an admin can widen that per-record if there's a real reason to, but until now doing so handed over the raw record with nothing held back. Now that grant comes with masking by default: the filename and the stated reason for the upload are hidden, and so is the free-text note on every entry in that record's access history. Anyone who genuinely needs to see one of those fields can request it with a real reason (same 12-character minimum already used elsewhere in this system), and every reveal is logged - but it's a one-time look, not a permanent unlock; the next time they open the record, it's masked again. PT/IM and Admin, the two roles the requirements never intended this for, are completely unaffected - checked that directly. Live-tested with four fresh accounts standing in for each real role in this flow, including someone trying to reveal a field with too short a reason and someone trying to reveal a field that isn't allowed to be revealed at all - both correctly rejected.

## 2026-08-11 (Correction, caught by being asked to double-check the Briefings screenshot)

Asked directly whether anything got missed against the screenshot, the same kind of check that caught the RBAC matrix mistake earlier in this project. This time it caught something smaller but still real: the "Mission Readiness" template's fourth section had been built from the template card's short blurb text, which mentioned "OFT pass rate" - but the same screenshot also showed a fully worked example of that exact template, sections and all, and that concrete example's fourth section was actually "By-flight comparison," not anything about OFT. The blurb was marketing copy, abbreviated and not even listing all five real sections; the worked example was the actual, literal answer, and it should have been trusted over the summary. Fixed the template to match the real example exactly, then built a fresh briefing from the corrected template to confirm the by-flight section produces real, accurate prose - it did, referencing the exact same flight data already verified earlier in this series. Small mistake, but exactly the kind that's worth catching by actually re-checking rather than assuming the first pass got it right.

## 2026-08-10 (Leadership workspace, fifth and final screen - "Briefings")

The last screen in this series, and it turned out to be the most different from everything that came before it. Every earlier Leadership screen was fundamentally the same kind of problem underneath - real numbers, presented in a new shape. This one asked for something the app had never actually done before: taking real numbers and turning them into sentences a commander could read.

That distinction mattered enough to check carefully before building anything. The screenshot's preview text read like something a person wrote - "Composite OPS lifted 8 points to 76. Recovery program rollout in March is the dominant signal." - not like a dashboard number with a label next to it. Every individual fact behind those sentences was real and already sitting in the database by this point in the series, but the *sentence itself*, the act of turning a JSON blob into readable prose, didn't exist as code anywhere. So this wasn't a "is this real or fabricated" question in the usual sense - it was closer to "does the app have any mechanism for doing this at all," and the honest answer was no, not yet.

The good news was that a real mechanism for something adjacent already existed: the app already calls Claude for real, today, to write summaries of a person's daily check-in - complete with a tested fallback for when the API key is missing or the call fails outright. That's not a small thing to have already proven out; it means the tricky parts (how to structure the prompt, how to parse Claude's response safely, what to do when it doesn't come back clean) were already solved once. Extending that same pattern to a new kind of content - cohort-level briefing paragraphs instead of one person's check-in summary - was a real, deliberate choice to build on tested ground rather than start a second, parallel AI integration from scratch.

Given the two decisions - real AI, and the full feature including templates and a save/send workflow - the actual build mostly fell into two halves. One half was assembling real section types purely from data that already existed: composite trend, driver snapshot, by-flight comparison, OFT readiness. The other half, the risk section, took a bit more care - it pulls from the same real tiered L2/L3/L4 warning system built earlier in the session, scanning real recent entries to answer "what's the worst thing that's actually happened recently," rather than guessing or hardcoding a severity.

Verification here leaned harder on proving *behavior* than just proving *numbers*, because a document with a live/frozen state has real ways to get that wrong. So the tests were built around exactly the states that would catch a mistake: create a note, confirm a still-draft briefing picks it up on the next look (proving "live" actually means live, not cached); send the briefing, then add another note and confirm it does *not* show up (proving "frozen" actually means frozen, not still quietly live). Both directions held. The AI call itself got a similar two-sided check - not just "did it produce something," but deliberately breaking it with an invalid key to watch the real fallback catch the failure and produce an honest sentence instead of crashing or returning something blank.

That same close testing is exactly what caught the one real bug this pass - a PDF rendering crash. A library quirk left a text cursor sitting in the wrong spot after the first paragraph was drawn, so the second paragraph had almost no room to render into and blew up. It's worth noting this wasn't a new class of mistake - the exact same library behavior was already worked around elsewhere in this codebase's PDF code, just not carried over into this new function. Found by literally trying to open the PDF, not by inspecting the code and assuming it was fine.

That's five for five on this series. Every Leadership screen got the same treatment: check the DOCX and the code before assuming anything, report plainly what was real and what wasn't, ask before building anything genuinely new or ambiguous, and never call something verified without an independent number to check it against. A few real bugs turned up along the way, a few honest "this doesn't exist yet" moments got surfaced instead of quietly built around, and the fabricated "recovery program" thread that kept reappearing across three different screens never once got built into something real, because it never had anything real behind it to build.

## 2026-08-10 (Leadership workspace, fourth screen - "Reports")

Fourth screen in the Leadership series, and the first one that felt less like extending a single feature and more like tying several already-built pieces together into something that looks like a real report library. Recent reports table, filter chips, a few report templates with "Use" buttons - a lot of surface area, but most of the underlying pieces already existed by this point in the series.

Three things genuinely didn't exist yet, though, and each one got the same treatment as always - checked first, reported honestly, decided explicitly rather than assumed. A status vocabulary on the report rows (Ready, Sent, Archived, In review, Draft) that only loosely overlapped with what the system already tracks for report approvals. A "template" concept - a saved, reusable report configuration you can just click "Use" on - that had never existed; every report before this got generated by calling a fixed type name directly, nothing reusable about it. And search, which simply wasn't there at all - the closest thing was an exact-match filter on report type.

Given the go-ahead on all three, the by-flight breakdown for the OFT report ended up being the smallest, most satisfying piece - since flight-level cohort resolution had already been built twice over for the Aggregate and Trends screens, adding it to the OFT report was just calling the same real logic a third time, not writing anything new. The three new report templates followed the same pattern: each one is just two or three already-built real methods called together and returned as one dict. Nothing new to compute, just new ways of presenting numbers that already exist.

The status field needed a bit more care, because there's a real, if slightly loose, tension worth naming: the export log's own docstring says it's "written once, never modified," in the spirit of an audit trail. But the second-reviewer approval workflow already updates that status after creation - approve or reject changes a stored value on an existing record. So adding one more mutable field for a real, useful purpose isn't introducing a new problem, it's following the same pattern the code already uses in practice, just being honest about that in the comment instead of pretending the model is stricter than it actually is. The new states settle into two groups: two of them (in-review, ready) track automatically alongside the existing approval workflow, and the rest (sent, archived, draft) only ever change because someone explicitly said so through a new endpoint - nothing quietly flips a report to "sent" on its own.

Verification turned up something worth calling out on the search feature specifically, because it's the kind of test that's easy to skip and easy to get a false sense of confidence from if you do. It would have been simple to just search for a report by a word that's literally sitting in its title and call that proof search works - but that doesn't actually prove the harder part, which is resolving a flight's real name from its ID and matching against that too. So the real test was deliberately awkward: a report titled one thing, searched for by its assigned flight's name, a name that appears nowhere in the title itself. It matched - which means the flight-resolution path is actually doing real work, not just quietly succeeding because the easy path also happened to match.

Everything else came back clean on the first pass: the annual schedule advanced by exactly a year, the by-flight numbers matched real pre-existing test data exactly, and the approval-to-ready transition fired the instant a genuinely different admin approved the export, re-confirming the two-person rule still holds under the new field. No bugs this time.

## 2026-08-10 (Leadership workspace, third screen - "Trends")

Third screen in the new Leadership series, and it reused most of what the previous "Aggregate" screen had just built - the same hero trend, the same driver cards, the same month-over-month math. But three pieces were genuinely new, and one of them turned out to be a different kind of feature entirely from everything built in this app so far.

The first two new pieces were fairly ordinary extensions: a period selector that needed day-level granularity where before there was only month-level, and a "cohort trend breakdown" that the screenshot again dressed up in an invented three-way label scheme instead of the app's real five-band system - same pattern as the labeling mixup from the previous screen, resolved the same way, by asking rather than guessing.

The third piece was different. The screenshot showed a timeline of dated, titled, narratively-written entries - "Winter surge, spike in overuse injuries," "Recovery program rollout," things like that. Every other real feature built across this whole session, without exception, has been a computation over data the app already tracks - an average, a count, a trend line. This is not that. This is something a human has to sit down and write, on purpose, about something that happened. There's no query that produces "SCS-led recovery protocol activated; physical and mental driver curves inflected upward" - somebody has to know that happened and choose to say so. That's a genuinely different category of feature, and it was worth naming that difference out loud rather than quietly treating it like just another aggregate to compute. It also, satisfyingly, explained a loose thread from the previous screen - the "recovery program" mentioned there with no definition anywhere turned out to be a forward reference to this exact timeline, which simply hadn't been built yet at the time.

Given the go-ahead to build it as a real feature, it became a small, ordinary CRUD - a title, a note, a date, who wrote it - with one deliberate asymmetry: any Leadership user can add an entry, but only an Admin can remove one. That split isn't from any spec, it's just the same reasoning applied elsewhere in this app already - anyone can contribute to a shared record, but only oversight roles can make something disappear from it.

Verification leaned hard on independent cross-checks again, the same habit that's paid off all session. The day-level numbers, the band counts, the window filtering on the new annotations, the permission split on delete - every one of them got checked against a second, independent calculation or a second account with different privileges, not just trusted because the first call returned a 200. Everything matched. No bugs this time - a clean pass, helped by how much of this screen was really just the previous screen's real infrastructure wearing a new query on top.

## 2026-08-10 (Leadership workspace begins - "Aggregate readiness")

New screenshot series started right after the last one wrapped up - the Leadership workspace, five surfaces this time instead of six. The first screen, the Index/landing page, turned out to need nothing new at all - the org-wide leadership dashboard already covered it. The second screen, "Aggregate readiness," was a different story entirely - one of the densest single screens shown all session, with something like nine distinct widgets packed onto it.

Went through the same check-first routine as always, and it turned up a real mixed bag: some pieces already had solid real backing (OFT pass rates, the assessment completion targets, the SCS/PT-IM annual hour targets), but several pieces were flatly invented - a citation to a DOCX section that doesn't exist, a "recovery program" concept with no definition anywhere, and a target number on the hero card that didn't correspond to anything real. Reported all of that plainly rather than building around it quietly.

The biggest finding was a labeling collision worth explaining, because it's the same lesson as the Exports screen's L-prefix problem from a few days ago, just showing up in a new place. The screenshot's driver cards used labels like "High," "Watch," and "Stable" - and at first glance this looks like it could just be a straightforward new score-band system. But looking closer at how those labels paired with their deltas (a declining score tagged "Watch," a slightly-rising one tagged "Stable"), it became clear these aren't score levels at all - they're describing which direction a number is moving, a completely different concept from the app's real, already-used Ready/Monitor/Caution/Action Needed/High Priority bands. Rather than silently picking one interpretation, I laid out the distinction and asked directly. The answer was to keep using the app's one real vocabulary rather than let a second one - even a well-intentioned one - creep in and eventually get confused with the first.

Three real pieces of new infrastructure got greenlit after asking: a genuine per-flight breakdown (which turned out to need less new plumbing than expected - the codebase's org-unit model already had a comment describing exactly this design, it just had never been wired up to anything), a real month-by-month history of readiness scores (previously the app only ever kept a single current snapshot per cohort), and three specific metrics that had no tracking at all before today - feedback-session completion, a real scheduled-vs-worked hours comparison for the coverage staff, and a rolling 30-day "who's coming due for their annual fitness test" count.

Verification is where this pass earned its keep, the same way it always has. Rather than trust each new number in isolation, every single one got cross-checked against something independent - and one of those cross-checks turned up something genuinely interesting, not a bug exactly, but a reminder about assumptions. I set up a clean two-month test: five real test accounts, a controlled score for July, a controlled score for August, expecting a specific month-over-month delta I'd calculated by hand. The real number that came back didn't match my hand calculation - so instead of assuming the code was wrong, I went and independently queried every real record behind that number, and found that two of my five test accounts already had real activity from earlier in the day that I hadn't accounted for. The system had been averaging every real data point correctly the whole time; my mental math had just missed some of them. That's exactly the outcome this kind of cross-checking is supposed to catch either way - a real bug, or a wrong assumption - and this time it was the second one, which is just as important to catch before reporting something as verified.

Every other check landed exactly as predicted: k-anonymity correctly hid a 3-person group and correctly showed a 5-person one; a risk-heatmap cell's color matched an independently recomputed value exactly; a scheduled-hours entry produced exactly the right percentage and missed-reason breakdown; and the "due soon" count moved by exactly one in each direction as real test records crossed the real 30-day boundary from both sides, proving the edge itself was right, not just the middle of the range. No real bugs surfaced in the new code this time - a clean pass, which happens sometimes when the groundwork (reusing real, already-tested patterns for k-gating and score bands instead of inventing new ones) is solid going in.

Three more Leadership surfaces are still ahead - Trends, Reports, and Briefings - each waiting on its own screenshot. Two scope decisions for those got made today, ahead of time, so they won't need to be re-asked when those screenshots arrive: Reports will get real Weekly/Monthly/Annual cadences built alongside the existing Quarterly one, and Briefings will be built as genuine new scope from data the app already tracks, not skipped.

## 2026-08-10 (Parts HH-QQ - closing out everything left on the list)

After the Control Plane series wrapped up, I was asked "কি কি বাকি" (what's left) and gave a full honest accounting of every open item across the whole backend - not just the Control Plane screens. The answer was "ok complete kore felo" (finish it), which set ten separate parts in motion in one sitting: the remaining medical-record fields, the Scope matrix's per-user drill-down, real admin-configurable recommendation thresholds, a real OFT leadership metrics report, a real PRS evidence export, real roster-based messaging authorization, real group threads, real real-time messaging, real email delivery plus honest SMS/push scaffolding, and exposing the audit-log retention policy.

Two of those items were real policy or infrastructure decisions, not engineering choices, so I asked before building instead of guessing: the audit-log retention window (answer: 7 years), and what to do about SMS/push/email having zero real credentials anywhere in this project (answer: build the real integration code anyway, and separately write a message asking the client for the actual credentials).

I researched before building anything, specifically to avoid duplicating or contradicting work that already existed. That paid off a few times: the assessment-completion compliance rollup I thought might be missing already existed, so the actual gap was narrower than it first looked (just the PRS evidence export artifact, not the rollup itself); `TeamAssignment` already had the exact right shape to become messaging's real authorization source, it just had never actually been *used* for authorization, only for cosmetic thread labeling; and the existing `ScoringConfig` model turned out to be an almost exact template for the new recommendation-threshold config, right down to the versioned/effective-date pattern.

The messaging roster piece deserved the most care, for the same reason the scheduled-exports second-reviewer gate did a pass earlier: it would be easy to accidentally create a bypass. So group-thread creation checks *every pair* of participants against the same real `_can_message` rule direct messages use - not just creator-to-each-participant - so a group thread can't be used to route around the roster gate that direct messages now enforce.

Live verification ran into something genuinely outside my control partway through: MongoDB Atlas itself became unreachable for about 40 minutes - confirmed by testing both a fresh script and the already-running server, both failing the same real SSL handshake error. Rather than fake the verification step or just skip it and hope, I started a background check that polled every 60 seconds and picked verification back up automatically the moment Atlas came back - so nothing got marked "done" without an actual passing check against real data.

That live-verification pass is exactly where it earned its keep again. Two real bugs turned up:

The first was mine, from this same pass - a route-ordering mistake. FastAPI matches URL patterns in the order they're registered, and I'd added the new PRS evidence export route *after* an existing generic `/reports/{report_type}/export` route, which meant the generic one silently swallowed it and returned "Unknown report type" instead of the real evidence file. An easy fix once caught, but it wouldn't have been caught without actually calling the endpoint for real.

The second was not mine, and older - a real bug that had been sitting in the coverage-log code since before this session even started. The per-provider coverage-log listing endpoint compared a string ID from the URL against a field that's stored as a real MongoDB ObjectId, and those two types never match each other in a database query - so that endpoint had been silently returning an empty list for every single provider, for as long as it's existed, with no error and no obvious symptom. I only found it because I cross-checked my new PRS evidence export's real rows against that older endpoint's output as an honesty check, and got 2 real rows from one and 0 from the other for the exact same provider. Fixed both call sites that had the same mistake, then verified the two endpoints agree with each other now.

One more real, worth-documenting thing came out of testing email for real: the Resend integration itself works - a direct test send actually succeeded - but Resend's own sandbox mode only lets a new account send to its own address until a real domain gets verified, which I confirmed by triggering an actual safety-boundary-flagged support request and watching a real, honestly-logged failure come back from Resend's API rather than a fabricated success. That's now written down as an extra thing to ask the client for, alongside the SMS/push credentials.

Twelve verification steps, every one a real call against real data, no shortcuts: the Scope drill-down against a real two-level org unit; a real threshold change confirmed to actually change what the recommendation engine does (then deleted again afterward, since leaving it in place would have silently changed behavior for every real user in the database going forward); the OFT report's numbers matched by-hand math for two real test records; the (bugfixed) PRS evidence export matched its own source data exactly; messaging correctly refused an unrelated pair and correctly allowed the same pair once a real assignment existed between them; a real three-person group thread worked, and a non-participant correctly got a "not found" rather than a "forbidden" - which matters, since a "forbidden" would have confirmed the thread exists at all; a live WebSocket connection received a real message the instant it was sent from a completely separate request; narrowing a medical record's access list correctly flipped a real reviewer from allowed to blocked on the same record; SMS and push both honestly reported "not configured" instead of pretending to succeed; and the audit-log stats endpoint now shows the real, agreed-on 7-year retention figure. A final sweep across 8 older, unrelated endpoints confirmed none of tonight's changes broke anything already working.

## 2026-08-09 (System admin screen - the last one in this series)

Sixth and last screenshot in the Control Plane series - the "System" module tile. This one had the same discipline applied as every screen before it, and it paid off twice: once by finding a real gap nobody had noticed, and once by finding that a claim I was asked to "make real" actually wasn't a gap at all - and building it as asked would have introduced a real bug.

The good find first: the DOCX has a short, easy-to-miss line about tracking weekend/RSD support coverage separately from regular hours. Checked the coverage-hours model built earlier this session, and it turns out that flag was already there - `is_weekend_rsd` - captured on every single logged session since that feature was built, just never actually added up and shown anywhere. That's the best kind of gap to find: no design decision needed, just surfacing data that was already being collected correctly.

The rest of the screen needed the usual reporting-and-asking, and got four answers, three of which were straightforward builds: a real diagnostics check (an actual database ping with real measured latency, real status for each background job, instead of a fabricated table of made-up microservices with fake version numbers and latency figures); a real 72-hour expiry for export approvals that nobody reviews in time; and a real inactivity list for accounts that haven't logged in for two weeks - which needed a closer read of the screenshot itself, since it said the grace period applied to "inactive accounts," not to pending deactivation approvals, so building it as an account-activity check rather than a second approval-timeout mechanism was the more accurate reading, not just the easier one.

The fourth answer is the one worth explaining properly. The screenshot claimed a specific pair of questions - one weekly, one monthly, both about stress - needed opposite scoring treatment: one "reverse," one "direct." I was asked to build that as a real feature. Instead of just adding the flag the screenshot implied, I went and read the actual question text and actual answer options for every single one of the 20 real weekly and monthly questions first. What I found: the scoring code already has full support for reversed questions, already wired up, just never triggered by any real question - because every single question in this app, without exception, already lists its answer options from worst to best. The "how high was your stress" question lists "Very high" first specifically so the existing scoring math treats it as the worst answer, same as every other question. There's no actual scoring problem here for either of the two specific questions the screenshot called out. If I'd added the flag the screenshot suggested, it would have flipped a correctly-working score into a backwards one - the exact opposite of a fix. So I didn't touch it, and said so plainly on the System screen instead of silently building nothing or silently building something wrong.

I want to be clear about why this mattered enough to stop and check rather than just doing what was asked: the instruction was "build real reverse-scoring," and the honest way to follow that is to first confirm there's something real to build. There wasn't, for these two questions specifically - and adding the flag anyway would have been the second time in one afternoon I nearly shipped a mistake by trusting a screenshot's specific claim over what the actual code does. The RBAC matrix correction from earlier today was the same lesson from a different angle - checking assumptions against what's real, not against what looks plausible.

Live-verified everything else the normal way: logged real RSD hours for a real provider and watched them show up correctly, separately, in the real quarterly report; the diagnostics endpoint returned a real ping time, not a placeholder; a real export request forced past its real 72-hour window got auto-rejected exactly as designed, while one still within the window was left alone; a real dormant account showed up in the inactivity list and a real active one didn't; and three real test groups with three different score-drop sizes each landed in the correct real severity tier, with exactly one alert per group rather than one per tier crossed. 34 out of 34 checks passed. Noticed a real Atlas connectivity blip in the background job logs afterward, but it happened after testing finished and the job's own error handling caught it cleanly - logged as a failed run, didn't take the app down - which is exactly what that handling was built to do.

That's the Control Plane series done - Overview, Roles & RBAC, Scope matrix, Audit log, Exports, System, all six screens. What's left is smaller and already tracked on its own: the medical-record fields still only partially built, and the Scope matrix's per-user drill-down view that never got its own screenshot.

## 2026-08-09 (Exports admin screen)

Fifth screenshot in the Control Plane series - the "Exports" module tile. The DOCX has almost nothing to say about this one specifically (2 lines: PDF and CSV where practical, plus the export-log fields already built earlier this session), so most of what the screenshot showed needed the same check-first treatment as usual: recurring scheduled exports, a PPTX format, a list of "9 required contract reports" with due-dates and named approvers, and risk labels using an `L1`/`L3`/`L4` prefix.

That last one is worth explaining a bit more, because it's a subtler kind of problem than the others - not "this doesn't exist," but "this name already means something else." This project already has a real L0-L5 escalation catalog from the DOCX itself (Table 20 - Normal, Low Signal, Pattern Concern, Persistent Concern, High Priority, Safety Boundary), used for readiness/wellbeing escalation, nothing to do with exports. The screenshot reused the same `L` prefix for a completely different concept - export risk. Building that literally would mean two unrelated systems sharing a naming scheme, which is exactly the kind of thing that causes real confusion later (someone sees "L4" on an export and might reasonably assume it means the same thing as an L4 safety escalation). Flagged it and built a real risk tier with different names entirely - low/elevated/high - derived from the same real sensitivity and row-count data, just without borrowing a name that already belongs to something else.

For the other three: got explicit answers to build real scheduled exports, add real PDF export (not PPTX - that's presentation-software generation, a different kind of problem entirely), and only show the 5 report types that actually have a real report builder behind them, rather than inventing due-dates and named certifiers for the other 4.

**Real PDF** turned out to be simple - added `fpdf2`, a pure-Python PDF library that needs no system-level dependencies (worth choosing carefully here; some PDF libraries need GTK or similar installed separately, which would have been a much bigger ask on Windows). It reuses the exact same row-extraction logic the CSV export already had, so there's one shared understanding of "what a report's rows are," not two.

**Scheduled exports** got the most care, because this is the kind of feature where it would be easy to accidentally create a loophole. The whole reason restricted exports need a second reviewer is that nobody should be able to pull sensitive data without another person's sign-off - and a "just schedule it to run automatically" feature is exactly the kind of thing that could quietly bypass that if I wasn't careful. So a scheduled restricted report doesn't skip the gate - it creates the exact same pending confirmation an on-demand one would, and only aggregate report types complete on their own. Verified this directly: forced a restricted schedule's next-run time into the past, ran the job for real, and confirmed it produced a pending approval rather than a finished export - then did the same for an aggregate schedule and confirmed that one really did complete immediately. Also deliberately left out the screenshot's "a scope change pauses any non-compliant schedule" line - there's no real condition in this system that defines what would make a schedule "non-compliant" in the first place, so building an auto-pause for an undefined trigger would just be inventing behavior with nothing real behind it. Pausing stays something an admin does on purpose.

**Live-verified** a real PDF came back with an actual PDF file signature, not just a renamed CSV; a scheduled restricted export correctly gated and a scheduled aggregate export correctly didn't; a paused schedule correctly never ran even past its due time; and the Exports overview screen's report list showed exactly the 5 real report types with nothing fabricated attached to them. 27 of 27 checks passed, nothing in the server log.

## 2026-08-09 (Correction: RBAC matrix was wrong, not just the medical-record fields)

Right after catching and fixing the medical-record completeness overstatement, got asked directly whether any of the other Control Plane screens had similar mistakes. Rather than just say "no" from memory, went back and actually checked - and found a real one, worse than the medical-record gap.

The RBAC matrix (`GET /roles/matrix`) and part of the Scope matrix's coverage map had the same root problem: I'd built them from `ROLE_PERMISSIONS`, a dict that already existed for a completely different purpose (what `GET /roles` shows), on the assumption that it would double as an accurate map of who can actually do what. It doesn't. When I originally live-tested this, I only checked 2 of the matrix's 9 rows - the 2 that happened to line up correctly. I never checked the other 7 against what the real routes actually allow.

Printing the whole matrix and comparing it row by row to the actual `require_roles(...)` calls in the route files turned up real, clear mismatches: the matrix said PT/IM, Admin, and IDMT couldn't view their own dashboard, when the real dashboard routes have no role restriction at all - anyone logged in can. It said Admin and Superadmin couldn't send a message, view a caseload, view aggregate trends, or author a plan, when the actual routes for every one of those explicitly include admin-level roles. And two of the nine rows - "Send IDMT handoff" and "View Authorized Performance Summary" - didn't correspond to anything real at all. There's no endpoint anywhere in this codebase that sends an IDMT handoff or generates a performance summary; the medical-record service's own code comment already admits summaries aren't built yet. Keeping those two rows in a "real" matrix would have been the same mistake as the medical-record thing, just in a different spot.

Fixed it by throwing out the permissions-dict approach entirely and rebuilding the matrix directly from each capability's actual route gate, with the specific route cited right next to each entry so it's auditable going forward - if someone questions a cell, the comment says exactly which endpoint it came from. Dropped the two fabricated-looking rows rather than force them to map to something. Also fixed the same class of bug in the Scope matrix's coverage map, where Admin/Superadmin were wrongly excluded from "unit visibility" for the same reason.

This time I didn't just trust the matrix's own printed output - I made real HTTP calls against the real routes and compared: a real PT/IM account really can load their own dashboard, a real Admin account really can open the Leadership dashboard and really can assign a recommendation to someone, all of which the old matrix said "no" to. All matched the corrected version. The cells that were already right (Admin/Superadmin needing a second reviewer to deactivate someone, only Admin/Superadmin being able to run an export) were re-checked and still hold.

The honest lesson here: building a "real, not fabricated" data view isn't safe just because every individual number traces back to something in the database - if the *shape* of the mapping is inferred rather than read directly off the thing it claims to represent, it can look completely real and still be wrong. Spot-checking 2 cells out of 9 and calling the whole matrix verified was the actual mistake, not just the specific wrong cells.

## 2026-08-09 (Audit log admin screen)

Fourth screenshot in the Control Plane series - the "Audit log" module tile. This one was different from the last three in a good way: the core piece (an append-only audit log with real event types already being written all over the codebase) already existed. This screen was about exposing it properly and closing a couple of gaps, not inventing a whole new system.

Checked against the DOCX and our own `docs/AUDIT_LOG_RULES.md` first, the same way as every screen this session, and found something worth calling out specifically: our own rules document already lists "recommendation assignment or override" as something that must be audited - but nothing in `RecommendationService` was actually doing that. Assign, dismiss, complete - none of it was logged. That's not a new feature request, that's a real gap between what our own rules say and what the code does, so closing it was the right move regardless of the screenshot. Separately, the DOCX's own medical-record data dictionary (line 147/211) names "retention status," "disposition status," and "approved access level" as required fields - `MedicalRecord` had none of them either. Also a real, pre-existing gap, not new scope.

Four other things in the screenshot weren't grounded in the DOCX or our rules doc at all: a "7 yr" retention figure, "Live tail" real-time streaming, an automated "Threshold warning" flagging system, and a richer medical-record event lifecycle (authorization/expiration/denial/quarantine). Reported all four plainly. For the retention window specifically, I didn't offer to just pick a reasonable-sounding default the way I did for things like account access expiration earlier - our own docs call it an open policy question, and a wrong guess on audit retention has real compliance consequences in a way a UX default doesn't, so I left it out of the response entirely rather than making something up. For the other three, the answer was build them for real.

**Live tail** turned out to be a good real-time fit: Atlas is already a replica set, so MongoDB's own change-stream feature works without adding a single new dependency - a WebSocket client connects, and the moment a real audit event gets inserted anywhere in the app, it streams straight to the browser. This isn't polling dressed up to look live, it's actually live. Building it did surface a real bug worth mentioning: in this project's installed pymongo version, `.watch()` itself is a coroutine you have to await before you get the actual stream object - I'd first written it as if `.watch()` returned something you could hand straight to `async with`, and it failed immediately with a fairly opaque `TypeError`. Fixed by awaiting it first, then closing the stream explicitly in a `finally` block. Verified for real afterward: connected a real WebSocket client, made a real role-change request in a separate call, and watched the exact entry arrive over the socket.

**Threshold warning** got a real, deliberately narrow version: once a day, compare each real unit's average score per readiness component today against roughly a week ago (reusing check-in history that's already being recorded for other reasons), and if it drops by more than a chosen threshold, write a real advisory audit entry. The one thing I was careful about here is privacy - this only ever runs for a unit that meets the same real, admin-configurable k-anonymity minimum every other unit-aggregate view in this app already respects, so it can never become a way to infer something about one specific person. Verified with a real 3-person test unit and a genuine 25-point drop - it fired correctly, and a unit under the cohort minimum correctly didn't get one.

**Medical-record lifecycle**: since the DOCX already calls for retention/disposition/access-approval fields that didn't exist, added them for real rather than half-implementing just enough to match the screenshot. The more interesting change is what happens to a blocked file type on upload now - before, it just got rejected with a 400 and nothing was kept, no record of the attempt existed anywhere. Now it's stored as a real quarantined record with a real audit trail entry, blocked from download for everyone including PT/IM and Admin, and can't be marked reviewed until it's out of quarantine (which isn't built yet - deliberately left as a next step, not silently skipped). Review also now records a real approve/deny outcome instead of one generic "reviewed" status that didn't distinguish the two.

**Correction, caught when directly asked to confirm this was actually complete**: it wasn't - "closes the gap" overstated it. The DOCX's line 147 data dictionary names 7 fields for medical records; only 2 of them (retention status, review status) actually got built here. `sensitivity_level` and `source` don't exist on `MedicalRecord` at all; there's no formal `consent/authorization status` field distinct from the free-text `access_reason` justification; and `approved access level` is still just a hardcoded role set in code (`VIEW_ALLOWED_ROLES`), not a real per-record field. Flagged plainly rather than left as an implied "done" - now tracked as its own open item in `TASKS.MD`.

**Category rollup** was its own small puzzle - the screenshot's 6 category names don't map cleanly onto our actual event-type vocabulary, especially "Medical-record access" and "Downloads," which don't come from the same collection as everything else (`AuditLog`) - they come from a separate `MedicalRecordAccessEvent` collection built specifically for that purpose earlier this session. Stated the whole mapping explicitly in code rather than leaving it implicit, so it's easy to correct later if the categorization isn't quite right.

**CSV export** for the audit log reuses the exact same second-reviewer approval gate already built for the injury report in an earlier pass - registered `audit_log` as another `"restricted"` report type and got the whole request/approve/download flow for free, no new gating logic anywhere. That also happens to be what makes the screenshot's "Exports ... 2nd reviewer" column genuinely true for audit-log exports specifically, not just something that looks true.

**Live-verified**: real filtering and free-text search over the audit log; the two previously-always-zero categories (Recommendation changes, Resolved/archived) actually moved after a real assign-then-dismiss; the audit-log export correctly stayed pending until a different reviewer approved it, then downloaded as a real CSV; the WebSocket live tail received a real event in real time; the threshold job fired correctly for a real qualifying unit and correctly stayed silent for one below the cohort minimum; the quarantine behavior blocked download and review exactly as intended; a denied review correctly landed in the new `reviewed_denied` state. Full regression, 31 out of 31 checks passed, nothing in the server log.

## 2026-08-09 (Scope matrix admin screen)

Third screenshot in the Control Plane series, and the biggest gap yet between what Figma showed and what actually existed. This also finally resolved something flagged as an open mystery two screens ago: the Overview screen's "Scope matrix: 10 x 5" tile - turns out it's 10 roles times 5 non-self scope columns (Flight, Caseload, Opt-in, Wing k>=5, Global), visible now that the actual Scope matrix screen showed all 6 columns including Self.

Checked against the DOCX first (zero hits for Wing, Flight, Detachment, cohort, driver visibility, inheritance, conditional pathway, k=, provider assignment window, revocable - nothing) and then against the real system, and this screen turned out to lean on Figma-invented specifics even more than the previous two: a full org hierarchy with specific-looking unit names ("432 WG", "Det 1 Operations"), a cohort-size selector that doesn't exist (we had one fixed constant, not a configurable one), a driver-visibility toggle system that doesn't exist at all, and a "Conditional pathway matrix" with specific approval/enablement dates ("Approved - 14 Jan 2026") that are certainly just mockup content - nothing in this project tracks pathway approval history.

Reported all of it plainly and asked what to do about each piece. This time, unlike some earlier screens, the answer for all three was "build it for real" rather than skip - so this became the biggest single build yet in the Control Plane series, and went through `EnterPlanMode` again given the size.

**Org unit hierarchy**: a real `OrgUnit` model with `unit_type` (wing/detachment/flight) and `parent_id`, admin-created through new endpoints, with a helper that climbs the parent chain to resolve a full ancestor path. Deliberately additive rather than a migration - the existing flat `unit_id` string field on `User` is untouched; it just now *can* point at a real hierarchy node if an admin sets one up, same as it always could point at any string before.

**Configurable cohort-k and driver visibility** - this is the part I'm most satisfied with, because it's not just a stored setting that looks real but does nothing. The new `RoleScopeConfig` actually got wired into two places that already existed: the k-anonymity gate that decides when a Unit Report or personal Wellness Report becomes available (previously always exactly 5, now whatever an admin sets per role, with 5 as the honest fallback for roles nobody's configured yet), and the SCS/Specialist provider dashboards, which now genuinely hide a component's score field if that component isn't in the calling provider's configured visible list. Verified this two ways: set Airman's k to 3, watched a real 3-person test unit's report flip from unavailable to available, then confirmed it went back to unavailable after restoring k to 5. Separately, hid Physical Readiness from SCS's visibility list and confirmed a real SCS provider's dashboard stopped showing that field for every one of their real assigned operators.

**Admin coverage map**: I want to flag something explicitly here rather than let it pass quietly - the screenshot's per-role pattern is asymmetric in a way our system genuinely isn't. It shows Nutrition getting one kind of visibility that Mental Performance and Purpose don't, but our real permission model treats those three specialist roles identically on purpose (that symmetry was a deliberate design choice from an earlier pass). Rather than fake an asymmetry to match the picture, the new coverage-map endpoint reflects our actual, symmetric data, and says so in the code.

**Conditional pathway matrix**: built the parts that have a real source - staffing counts (real active-account counts per specialist role), real opt-in counts, and a data-access description derived from the same specialist-component mapping already in use elsewhere - and left out the approval/enablement dates entirely rather than inventing plausible-looking ones. A missing field is honest; a fabricated date that looks exactly like real audit history is the kind of thing that could genuinely mislead someone later.

The "Plan" role showed up in this screenshot too, same as the last one - stayed consistent with the earlier decision and left it out again.

**Live-verified**: the org hierarchy resolved a real 2-level ancestor path correctly; the cohort-k change had a real, observable effect on a real k-anonymity gate in both directions; the driver-visibility change had a real, observable effect on a real dashboard response; the coverage map's self-counts matched the Roles & RBAC catalog's counts exactly; the pathway matrix's staffing number matched an independent direct database count, and confirmed no date fields snuck in anywhere. 28 of 29 checks passed cleanly - the one that didn't wasn't a bug, it just used a test operator who happened to have no real component scores recorded at all, so there was nothing to observe on the "restore" side of that particular check (the "hide" side of the same test already proved the mechanism works). Full regression, nothing broke.

## 2026-08-09 (Roles & RBAC admin screen)

Next screenshot in the same Control Plane series - the "Roles & RBAC" module tile that was deferred from the Overview screen. This one showed a role catalog table, an RBAC matrix, an "Accounts & onboarding" panel, and a "Contract Question Registry" of 46 rows.

Before building anything, checked it against the DOCX (same discipline as the Overview screen - zero hits for "cluster", "access expiration", "annual review", "consent" beyond the existing medical-record field, "program manager") **and** against this project's actual question banks and role code, since this screenshot leaned heavily on specific content (question IDs, routing, role names) rather than just UI chrome. That second check turned up real mismatches: a "Plan" role in the catalog that doesn't exist among our 10 real roles; a cluster/scope classification layer that doesn't exist on our (flat-string) roles; an access-expiration/annual-renewal concept and a "purpose consent (separate)" tracker, neither of which exist; and a Contract Question Registry claiming 46 questions (O1-O20 + D1-D6 + W1-W10 + M1-M10) with per-question routing, reverse-scoring, versioning ("2026.7"), and validation-status - our real question count is 40, there's no 6th "Recovery" driver or "Compliance" driver in our actual 5-component system, and nothing tracks reverse-scoring, versioning, or per-question validation anywhere.

Reported all of this plainly and asked what to do about each one. Got 4 clear answers: skip the "Plan" role entirely (Figma placeholder); build the cluster/scope classification as real new metadata; build both access-expiration and purpose-consent as real tracking (not skip, this time); and build a simplified, real version of the question registry rather than either fabricating the missing 6 rows or skipping the whole panel.

Went through `EnterPlanMode` again given the size - explored first and found real infrastructure to reuse rather than duplicate: `GET /roles` already existed and already returns the real `ROLE_PERMISSIONS` dict; `TeamAssignment` already has a real `"disabled"` status and the existing Chaplain-pathway toggle already writes a real audit event with old/new status, so **purpose consent needed no new model at all** - it's a real read over data the prior session's My Support Team feature already produces, not a new tracking system. That was a genuinely nice find - "purpose consent withdrawn" turned out to already be represented in existing data, just never surfaced as its own summary before.

**Role cluster/scope**: added `ROLE_CLUSTER`/`ROLE_SCOPE` dicts to `app/core/roles.py`, explicitly commented as a new classification we're defining, not something the DOCX specifies - the honesty here is in the comment, not in pretending it came from somewhere authoritative.

**Access expiration**: new `User.access_expires_at` field, set to a year out at registration (own reasonable default, stated as such), with a manual `POST /admin/users/{id}/renew-access` to extend it. Deliberately did **not** build actual automatic silent renewal - if the date always resets itself before anyone reaches it, tracking it becomes theater rather than a real access-control signal, so this is documented as a scope decision (same pattern as PDF export never being half-built) rather than quietly building something that wouldn't actually do anything.

**Purpose consent**: `active_count` from real `TeamAssignment` state, `withdrawn_count` from real `support_pathway_toggle` audit events - specifically requiring both a *currently* disabled assignment and a genuine explicit disable event, so a user who never opted in isn't miscounted as "withdrawn," and a user who disabled then later re-enabled correctly moves back into `active_count` rather than staying stuck as withdrawn forever. Verified this exact behavior live in both directions.

**RBAC matrix**: 9 real capability rows mapped to the real `ROLE_PERMISSIONS` strings, `full` or `none` - except one cell deliberately set to `gated`: Admin/Superadmin's "Deactivate user," which genuinely does require a second reviewer now, from the prior Control Plane pass. No fabricated "conditional" tri-state invented just to match the screenshot's 3-color legend - our data doesn't cleanly support a third state beyond that one real gate.

**Question registry**: real 40 questions (not 46), each with its real `readiness_component` and a `routing` field derived by extending the existing specialist-to-component mapping (Nutritionist/Mental Performance/Chaplain, from the provider-dashboard work) to cover Physical/Sleep -> SCS. Onboarding questions do carry real `reverse_scored`/`provider_route` fields already stored in `question_bank.py` - included them as-is rather than hiding them, but noted honestly that `provider_route` is almost always `"PT/IM"` regardless of component in the real data, so it isn't actually a useful per-specialist signal the way the screenshot implied.

**Live-verified**: `GET /roles/catalog`'s SCS member count matched an independent direct database count exactly (not just trusting the endpoint's own math); the RBAC matrix correctly showed `gated` only for Admin/Superadmin's deactivate-user cell and `none` for Airman; registering a fresh user produced a real `access_expires_at` about a year out, and renewing it extended that date; toggling a real test user's Chaplain pathway off moved them into `withdrawn_count`, and re-enabling it moved them back into `active_count` with neither number double-counting; the question registry correctly reported exactly 40 questions with real per-question data, spot-checked against the actual question bank files. Full regression: 36 checks, all passing.

## 2026-08-09 (Admin/Superadmin Control Plane Overview - second-reviewer gating, reversibility, real system health)

User shared a Figma screenshot of an Admin/Superadmin "Control plane" Overview screen (stat cards for audit entries/pending confirmations/roles configured/system health, 6 module tiles, a recent-activity feed, and a pending-confirmations table with Review buttons) and asked for it to be built, labeling it for the admin/superadmin role.

Before building anything, checked it against the DOCX first (the discipline established all session): searched paragraphs and tables for "uptime", "monitoring", "system health", "downtime", "availability", "approval", "second review", "dual control", "rollback", "undo", "peer review", "four-eyes" - zero matches for every one of them. Reported this honestly rather than quietly building either the system-health percentage or the second-reviewer/reversibility workflow as if they were DOCX requirements. Asked the user directly what to do about both (they double-checked by asking to search again, which came back the same - zero matches). Decision: build system health as a **real approximation** (not fabricated), and build the second-reviewer/reversibility workflow as **real new scope** (not skipped).

Given the size, this went through `EnterPlanMode` again - explored `ProviderDashboardService.get_admin_dashboard` (already existed, already real, the right place to extend rather than building a competing endpoint), the existing self-service-only `DeactivationRequest` flow (confirmed no admin-initiated deactivation of *another* user existed anywhere, and no caseload/reassignment concept existed at all), and the export flow (`ReportExport.export_log_status` existed but was dead - always `"completed"`, never set to anything else anywhere in the code). Plan written and approved before any code.

**Real system health**: `app/core/scheduler.py`'s daily reminder job now writes a real `SchedulerJobRun` row (`success`/`failed`, with timing) every time it actually runs, instead of only logging. `GET /dashboard/admin`'s new `system_health` field computes a genuine 30-day success percentage from these rows - and returns `{"percentage": null, "label": "insufficient_data"}` honestly when zero rows exist yet (a fresh install, or before today's job has run), rather than inventing a number to fill the space. Verified live: `insufficient_data` before any run existed in the DB, then `100.0 / "Healthy"` immediately after manually triggering one real run.

**Second-reviewer confirmation queue + reversibility** (`PendingConfirmation` model, new `AdminConfirmationService`): three destructive actions now require a genuinely *different* Admin/Superadmin to approve before they take effect - everything else (ordinary role changes, Airman self-deactivation, aggregate-report exports) is completely untouched and keeps working exactly as before:
- **Admin-level role changes** - the existing Superadmin-only *request* restriction from the prior pass stays, but `PATCH /admin/users/{id}/role` now creates a pending confirmation instead of applying the change immediately.
- **Deactivating a provider/admin-level account** - new `POST /admin/users/{id}/deactivate`, scoped to accounts that can actually hold a real caseload (SCS, PT/IM, the 3 specialist roles, Leadership, Admin/Superadmin - Airmen keep using the existing self-request flow, they have no caseload to worry about). The `consequence_summary` shown to the reviewer is a real count of `TeamAssignment` rows pointing at that provider, not a guess. On approval, the account is deactivated **and** every one of those `TeamAssignment` rows gets its `provider_user_id` nulled - which closes a real gap found while exploring this: previously, deactivating a provider left their assigned users silently pointing at an inactive account forever. Those now-null assignments will get picked up automatically by the auto-assignment retry logic fixed in the prior pass, the next time anything touches them.
- **"Restricted"-sensitivity report exports** - currently just `injury` (its rows are per-Airman identifiable); `assessment_completion`/`utilization`/`prs_qcp` are `"aggregate"` and keep exporting immediately, completely unchanged. The export route now returns a 202 pending response instead of CSV bytes for restricted reports; a new download endpoint serves the CSV only once approved, re-running the report builder live at download time rather than storing sensitive CSV bytes at rest.
- The two-person rule is enforced in code, not just in the UI: `approve`/`reject` both check `reviewer.id != requested_by` and return 403 on a self-approval attempt.
- `revert` restores a role change or reactivates a deactivated account from a stored "before" snapshot - deliberately not offered for exports, since there's nothing meaningful to undo once a CSV has already been generated; the approval gate itself is the safety mechanism there.

`GET /dashboard/admin` also picked up `roles_configured` (a real `len(SUPPORTED_ROLES)`, currently 10 - matches exactly what the screenshot showed) and `pending_admin_confirmations`, kept as its own field separate from the pre-existing `pending_deactivation_count` (a different, older, self-service queue - conflating the two would misrepresent what's actually pending and why). While already touching this method, fixed something the exploration turned up: it was loading the *entire* `AuditLog`/`ReportExport` collections into memory on every call just to sort and slice the top 10/5 in Python - now does that at the query level instead.

**Live-verified end-to-end** using two real pre-existing Admin-level test accounts as requester/reviewer: requested an admin-level role change, confirmed it did NOT apply immediately, confirmed the requester could not approve their own request (403), confirmed the second reviewer's approval actually changed the role, then confirmed `revert` restored the original role. Requested deactivation of a real PT/IM test account that genuinely had 2 real `TeamAssignment` rows pointing at it - confirmed the consequence summary read "2 caseloads reassigned" (the real number, not a placeholder), confirmed the account stayed active until the second reviewer approved, then independently confirmed via a direct database query (not just trusting the API's own response) that both `TeamAssignment` rows actually got nulled - then reverted the deactivation to restore the test account for future passes, confirming revert works for this action type too. Exported the `injury` report - got a 202 pending response instead of CSV, confirmed download was blocked (403) before approval and worked (200, real CSV) after a second reviewer approved it - while the `utilization` report kept exporting immediately, unchanged, throughout. `system_health` moved from `insufficient_data` to `100.0 / "Healthy"` after triggering one real scheduler run. Full regression sweep across dashboards/reports/confirmations endpoints, all 200.

**Not built this pass, on purpose**: the 5 module tiles' own sub-pages (Roles & RBAC, Scope matrix, Audit log, Exports, System) - only the Overview screen's own cheap-and-real tile counts are wired up. "Scope matrix: 10 x 5" specifically was left unbuilt since the meaning of the "5" dimension isn't known and guessing at it would mean fabricating a shape - that tile waits for its own screenshot, same screen-by-screen approach used all session.

## 2026-08-09 (Atlas restored - full live verification, 1 more real bug found and fixed)

User confirmed Atlas connectivity was fixed. Ran the complete live pass on everything from the previous entry, using a temporary `SUPERADMIN_EMAIL` test account (removed from `.env` again afterward, left blank for the user to set when actually ready to create a real Superadmin).

**One more real bug, only findable live**: `TeamService._get_or_create_assignment`'s auto-assignment retry logic - the part that re-checks for a provider when an existing `TeamAssignment` record has `provider_user_id: None` - was gated to `pathway["always_available"]`. That condition made sense when only SCS/PT-IM had real roles, but it meant an *optional* pathway's assignment record created before this pass (back when Nutritionist/Mental Performance/Chaplain were still `role: None`) would never retry, no matter how many real providers registered afterward - permanently stuck on `provider: null`. Reproduced directly: toggled Nutritionist on for the test operator, provider stayed `null` even with a real Nutritionist account already registered. Fixed by removing the `always_available` condition from both places it appeared - the retry-on-read path and the initial-creation path - so every pathway with a missing provider and a real role retries now, not just the 2 locked-on ones.

Verified live, in order:
- `SUPERADMIN_EMAIL` bootstrap: registered that exact email requesting `"role": "Airman"` in the payload - got `DWS Superadmin` back, confirming the override actually overrides rather than just defaulting.
- The core restriction the user asked for: Superadmin promoted a fresh account to `DWS Admin` successfully; that new Admin then tried to promote a *different* user to Admin and correctly got 403 ("Only a Superadmin can grant or remove an admin-level role"); the same Admin could still freely change a non-admin role (to SCS) - confirming the restriction is scoped to admin-level roles specifically, not a blanket lockout.
- All 5 of the self-review bug fixes, re-verified live with the real Superadmin account rather than just trusting the earlier static analysis: `GET /support/requests/assigned` (200, not empty), `PATCH` a request's status (succeeded, not 403), a Level-5 safety-boundary-flagged request's notification independently confirmed sitting in the Superadmin's own `notifications` collection entry, viewing a real medical record uploaded by someone else (200, not 403), and a deactivation request's notification independently confirmed reaching the Superadmin.
- After the 6th-bug fix: registered a real Nutritionist account, confirmed the operator's already-existing (previously stuck) assignment picked up the real provider on the very next `GET /support/team` call; separately registered a real Mental Performance account and had a *brand-new* operator toggle that pathway on for the first time, confirming the creation-path fix works too, not just the retry path.
- The Mental Performance provider then assigned a real action to their patient via `POST /recommendations/{id}/assign` (the endpoint extended to the 3 new roles earlier this pass) - and that exact action correctly showed up as `assigned_action_title` on `GET /dashboard/specialist` when queried as that same provider.
- All 5 dashboards independently confirmed against real data: SCS dashboard via a real provider's own assignments, and separately via Superadmin's org-wide view (which correctly showed *all* SCS-assigned operators across every provider, including real OPS scores, reconditioning status, and an active risk flag - not an empty list); PT/IM dashboard showed a real operator's real reconditioning phase/clearance/limitation flag; Leadership dashboard's aggregate OPS (67.6), band distribution, component averages, and support-requests-by-pathway (now correctly including the 3 new specialist pathways) all matched what was actually in the database; Admin dashboard's account-by-role breakdown correctly counted the new Superadmin and specialist-role accounts created during this same test run. Wrong-role 403 confirmed on every one of the 5 dashboards.
- Full regression: 39 endpoints (21 operator-facing, 18 admin/superadmin-facing) - all 200, confirming the ~19-site `require_roles` sweep and the 6 bug fixes didn't break anything already working.

## 2026-08-09 (Superadmin/Admin hierarchy + 3 specialist roles + 5 provider dashboards - built, live verification pending)

User shared a "Role directory" sign-in screenshot for the provider/admin side of the app and, separately, described the access model they wanted: a Superadmin who can create Admin accounts, and a plain Admin who has everything Superadmin has *except* that. They also asked for dashboards for SCS, PT/IM, Leadership, Nutritionist, Mental Performance, and Chaplain, with the dashboard code kept in its own folder even though it shares the API with everything else, and no CAC/SSO for this login screen - normal email/password only, same as the rest of the app.

Given the size of this (a real permission hierarchy plus 5 new dashboards), this went through `EnterPlanMode` first - explored the current role system, found the exact `require_roles` call-site count, confirmed the DOCX's Table 25 dashboard shapes, then wrote a full plan to `mellow-stargazing-rossum.md` and got explicit approval via `ExitPlanMode` before writing any code. Two things were clarified with the user before finalizing the plan: the Superadmin role name (`DWS Superadmin`, matching the existing `DWS Admin` convention) and how the first Superadmin gets created (a `SUPERADMIN_EMAIL` env var checked at registration, not a manual DB insert), and whether the Specialist Dashboard should be one shared endpoint or three separate ones (one shared, matching the DOCX's own single generic "Specialist Dashboard" table row, filtered by whichever specialist role is actually calling it).

**Part A - Superadmin/Admin hierarchy.** Added `ROLE_SUPERADMIN = "DWS Superadmin"` and `ADMIN_ROLES = (ROLE_ADMIN, ROLE_SUPERADMIN)` as the single source of truth for "has admin-level access." Every `require_roles(ROLE_ADMIN)` call site - roughly 19 of them, spread across `admin/routes.py`, `roles/routes.py`, and the multi-role tuples in `assessments`/`oft`/`recommendations`/`records`/`support_requests` routes - now uses `*ADMIN_ROLES`, so a Superadmin automatically has every permission an Admin has. Bootstrap is deliberately minimal: `AuthService._resolve_role` checks the registering email against `SUPERADMIN_EMAIL` and, if it matches, assigns `DWS Superadmin` regardless of what role was requested - documented in `.env.example`, not a hidden mechanism. The one real restriction lives in `AdminUserService.change_role`: granting or removing an admin-level role now requires the caller to actually be a Superadmin - a plain Admin gets 403 trying to create or demote another Admin, which is the entire point of the hierarchy the user asked for. Every other role change (Airman/SCS/PT-IM/specialist/Leadership/IDMT) stays available to both, unchanged from before.

**A genuinely valuable side effect of this pass**: self-reviewing the new role for correctness (rather than only trusting `require_roles`) surfaced 5 real bugs that would have silently broken for a Superadmin account, because they check `user.role` directly inside a service rather than at the route-level gate: `SupportService.list_assigned_requests` (would have returned an empty list - it only special-cased the literal string `"DWS Admin"`), `SupportService.update_request_status` (would have 403'd), `SupportService._notify_safety_flag` (a Superadmin would never have been notified about a Level 5 safety-boundary flag - the most consequential of the five, given what that notification exists for), `MedicalRecordService.VIEW_ALLOWED_ROLES` (403 viewing any medical record), and `AccountManagementService`'s deactivation-request notifier, which used a bare `"DWS Admin"` string literal instead of even the `ROLE_ADMIN` constant. All five fixed to check membership in `ADMIN_ROLES` instead of equality against one specific role string. None of these were hit by a real request yet (Atlas was already down by the time this was found) - caught by deliberately grepping for every direct role-equality check in the codebase after finishing the mechanical `require_roles` sweep, not by assuming the mechanical sweep alone was sufficient.

**Part B - Nutritionist, Mental Performance, and Chaplain become real roles.** These were `role: None` in `support_pathways.py`, explicitly documented as "not yet formal system roles" every time this session touched My Support Team. Now real (`ROLE_NUTRITIONIST`, `ROLE_MENTAL_PERFORMANCE`, `ROLE_CHAPLAIN`, values identical to their pathway keys by design, matching how `"SCS"`/`"PT/IM"` already work) - which means the auto-assignment logic already built for My Support Team (`TeamService._find_active_provider`) now works for real across all 5 pathways instead of just the 2 locked-on ones, with zero changes to that logic itself. Also found and fixed a related gap while wiring up the Specialist Dashboard: `POST /recommendations/{user_id}/assign` only allowed SCS/PT-IM/Admin, so a real Nutritionist/Mental Performance/Chaplain account had no way to assign an action to their own patient - extended the same endpoint to the 3 new roles.

**Part C - 5 provider dashboards (DOCX Table 25).** `SCS Dashboard`, `PT/IM Dashboard`, `Specialist Dashboard`, `Leadership Dashboard`, `Admin Dashboard` - none of these existed before this pass; only end-user screens (Home/Trends/Wellness Report/etc.) did. Built as `app/services/provider_dashboard_service.py`, one method per dashboard, composing already-built services rather than re-deriving anything: `OFTService`, `ReconditioningService`, `ReportsService`, `UtilizationService`, and `CredentialService` all get reused directly. Every dashboard is built from data that's actually being tracked - where the DOCX's "Must Answer"/"Required Views" columns imply something with no real source (e.g. "rehab strategy summary" as structured data, "corrective actions"), it's left out rather than invented, same discipline as every other report built this session. SCS/PT-IM dashboards give a real provider their own assigned operators; an Admin/Superadmin calling the same endpoint gets an org-wide view across every provider of that role instead of an empty list, since an admin account has no personal "my assigned operators" to show. Leadership Dashboard reuses the exact k-anonymity aggregate-only principle already established in `DashboardService.get_unit_report`, just organization-wide instead of per-unit.

**Folder structure, per explicit instruction**: the 5 provider dashboards live in `app/modules/dashboard/` (singular) - a genuinely separate module folder from the existing end-user `app/modules/dashboards/` (plural) - mounted at a separate `/dashboard` prefix under the same shared `/api/v1` API, so the two concepts never get mixed in the codebase even though the API surface is unified.

**The "Role directory" sign-in screenshot itself** needed no new backend work: CAC/PIV and Unit SSO were explicitly ruled out by the user in favor of the same normal email/password flow the rest of the app already uses (`POST /auth/login`, extensively tested all session) - there's no separate "provider login" mechanism to build, just a different frontend entry point pointed at the same endpoint. The screenshot's "Last used ... from this device" line maps directly to the already-built `GET /users/sign-in-history` (real IP/User-Agent, no fabricated device fingerprinting).

**Status**: code-complete, compiles cleanly, self-reviewed carefully (including the 5-bug sweep above), but **not live-verified** - Atlas connectivity failed again mid-pass (`SSL: TLSV1_ALERT_INTERNAL_ERROR`, confirmed directly against the driver, not a code issue - the 3rd such outage this session). Needs the same full live pass - real requests, independent database confirmation of side effects, full regression - once connectivity is restored, before this is actually done rather than just built.

## 2026-08-07 (Messaging attachments)

Went back through the full "what's left per the screenshots" list one more time and found one item that had genuinely changed status since it was last looked at: message attachments (the 📎 icon on the messaging thread screen) had been deferred earlier this session with the reasoning "storage-provider decision pending" - but that decision has since been made and built, for medical records. Revisiting it now, there was no real reason left not to build it.

Reused the exact same `FileStorageService` built for medical records (local disk, Fernet-encrypted, keyed off `SECRET_KEY`) rather than building a second storage layer - just gave it a `namespace` parameter so message attachments and medical records land in separate subdirectories on disk and never mix. `Message` gained three new fields for the attachment's name/storage path/size. `POST /messaging/send` had to change from a JSON body to multipart form fields to allow an optional file alongside the text - the same shape change the medical-records upload endpoint already went through, for the identical reason. Added `GET /messaging/message/{id}/attachment` for download, gated by the same thread-participants-only boundary already enforced on message content and the trace endpoint. Allowed types PDF/JPG/PNG/HEIC, 20MB cap, reusing the same malware-scan stub (still explicitly not real AV).

One quiet fix worth calling out: `get_message_trace`'s `attachment_count` field has existed since the "Audit & decisions" pass and was always hardcoded to `0`, documented at the time as "there is no attachment subsystem." That's no longer true, so this also makes that field honest instead of permanently zero.

Verified live: a plain message with no attachment still sends correctly (confirming the multipart-form change didn't break the common case); sent a real PDF attachment, downloaded it back, and the decrypted bytes matched the original exactly; a user outside the thread correctly got 403 trying to download it; a blocked `.exe` attachment correctly 400'd; the trace panel's `attachment_count` correctly flipped from `0` to `1` for the attached message; independently confirmed on disk that the two storage namespaces stayed separate (`storage/medical_records/` vs `storage/message_attachments/`); full regression across the messaging/records/support/dashboards/notifications/checkins endpoints stayed green.

## 2026-08-07 (Scheduler, admin scoring config, role/unit assignment, credential/equipment/utilization/coverage tracking, 4 quarterly reports + CSV export - built, live verification pending)

User gave a specific list to complete: background scheduler, ops_scoring admin config, admin role/unit assignment, provider credential tracker, Equipment & Supply Gap Tracker, quarterly reports (Utilization/Injury/PRS-QCP/Assessment Completion), Report Templates and Exports, role-change audit logging. Built all of it. Partway through, **MongoDB Atlas connectivity failed again** - confirmed directly against the driver (`SSL: TLSV1_ALERT_INTERNAL_ERROR`, identical failure mode to an earlier session pass), not caused by anything in this pass - nothing here touches connection configuration. This is being reported honestly: everything below compiles cleanly and was self-reviewed carefully, but the live end-to-end verification this project has insisted on for every other pass this session **could not happen yet**.

**Background scheduler** - the single biggest structural gap called out earlier ("cadence-due reminders don't fire proactively, only when a user's own request happens to touch the relevant code"). Added `app/core/scheduler.py`: an in-process APScheduler job running once daily that calls the *same* reminder methods the request-triggered paths already use (made two of them public on `CheckinService` rather than duplicating their dedup logic), plus two genuinely new methods - `OFTService.remind_due_soon()` and `AssessmentService.remind_due_soon()` - since OFT/assessment reminders previously only fired once, at scheduling time, never as the date actually approached. Also added `CredentialService.remind_expiring_soon()` for the new credential tracker below. Explicitly documented as in-process/single-instance, not a distributed task queue - multi-instance deployment would need Celery/RQ, still an open item.

**Admin-configurable OPS scoring weights/thresholds** - added `ScoringConfig` (versioned, effective-dated, matching the DOCX's own "Scoring Configuration" data-dictionary fields exactly) and `ScoringConfigService`. The interesting part was making this actually *do* something rather than being a config screen nobody reads: `calculate_ops`/`build_score_band` in `app/core/scoring.py` gained optional `weights`/`thresholds` parameters (defaulting to the exact unchanged hardcoded values, so nothing behaves differently until an Admin creates a config), and all 4 real places OPS actually gets computed - daily/weekly/monthly check-in submission and both onboarding paths - now fetch the active config first.

**Admin role/unit/provider assignment** - `GET /admin/users`, `PATCH /admin/users/{id}/role`, `PATCH /admin/users/{id}/unit`, `POST /admin/users/{id}/assign-provider`. This closes a DOCX line word-for-word: "Assign roles, coaches, specialists, teams, units, reporting groups" - there was previously no way to do any of this without touching the database directly. Every change writes a real `AuditLog` entry, which also closes the separately-flagged "role-change audit logging" gap - it didn't exist in any form before this.

**Provider credential tracker** (DOCX 1.4.7 PT/IM/SCS Qualifications) - `ProviderCredential` model; `status` (current/expiring_soon/expired/no_expiration) is always computed live from `expiration_date`, never stored, so it can't go stale.

**Equipment and Supply Gap Tracker** (DOCX 8.7) - `EquipmentGap` model with exactly the fields the DOCX names: item, supply need, priority, requested by, date identified, status, whether included in a report.

**Utilization Event tracking** - `UtilizationEvent`, fields taken verbatim from the DOCX data dictionary (event_type, opportunity_offered, actual_use, date, staff_lead, attendance_count, notes) - this didn't exist at all, and the Utilization Report needed it.

**Coverage-hours log** - `CoverageLog`, feeds the PRS/QCP report's coverage-hours-vs-target calculation.

**Four quarterly reports**, each one computed from real data with no invented numbers - and each report's own docstring says plainly what DOCX-implied content is deliberately left out because there's no real source for it, rather than filling the gap with a placeholder:
- *Injury Report*: real `ReconditioningPlan` injury flags/clearance/phase + real limitation-flagged `WorkoutLog` entries in the window. Does not include free-text rehab strategy summaries (that content lives in provider messaging, which isn't aggregated here).
- *Assessment Completion Report*: real cohort completion rates (operators grouped by time-since-account-creation) against the DOCX's 50%-by-6-months / 90%-by-12-months targets. Does not include "feedback sessions" - not tracked anywhere in this backend.
- *Utilization Report*: real `UtilizationEvent` aggregates by type, attendance, actual-use rate.
- *PRS/QCP Support Report*: real logged coverage hours per SCS/PT-IM provider against the DOCX's fixed 2,080/512 annual-hour targets and 95% evidence threshold, plus the assessment-compliance figures. Does not include "corrective actions" or "issue categories" - no real source.

**Report Templates and Exports** (DOCX section 12) - CSV only. No PDF rendering library was added to this project; rather than half-implement PDF (pick a library, never test its actual rendering), this is documented as a deliberate scope decision - CSV satisfies the DOCX's own "PDF and CSV **where practical**" qualifier without a new heavyweight dependency. `GET /admin/reports/{type}/export` writes a `ReportExport` log entry *before* returning the file, matching the DOCX's own export-log data dictionary field-for-field (report_type, date_range, generated_by, recipient_role, export_format, sensitivity_level, export_log_status) - this is a real, working implementation of the "Each report export must create an export log entry" requirement, not a stub.

**A real bug was found and fixed via self-review**, not live testing, since Atlas was already down by the time this was noticed: the new scheduler's `User.find(User.is_active == True)` and the injury report's `WorkoutLog.find(..., reported_limitation == True)` both used a boolean-equality query pattern this project has explicit prior evidence of being unreliable in Beanie 1.30.0 - `TeamService._find_active_provider`'s own comment says it "deliberately avoid[s] a `== True` Beanie comparison edge case." Both were rewritten to fetch-then-filter-in-Python before either was ever exercised by a real request, consistent with that established precedent rather than waiting to rediscover the same bug.

## 2026-08-07 (Atlas restored - full live verification of the above, 1 more real bug caught)

The user fixed Atlas connectivity again shortly after the previous entry. Ran the complete live pass this project insists on for everything.

**One more real bug, this time only catchable live**: two `WorkoutLog` documents already in the database still had the *old* field values from before this session's earlier `intensity` (string -> int 1-5) and `activity_type` ("conditioning" -> "cardio") schema change. Reading either document now raised a Pydantic validation error inside Beanie's own parsing, which crashed the brand-new injury report with a 500 - and, more importantly, would eventually have crashed the *already-shipped, already-tested* `GET /workouts` and `GET /workouts/summary` for this same test operator too, the next time their query happened to touch one of these two rows. Static self-review couldn't have caught this - the code was correct, the *data* was stale. Root-caused from the actual server traceback rather than guessed, then fixed with a one-time migration on just the 2 affected documents (`intensity` mapped light/moderate/high -> 2/3/4, `activity_type` conditioning -> cardio) - confirmed live afterward that both the previously-passing workout endpoints and the new injury report all return 200.

Then verified everything else from the previous entry, in order: role change, unit assignment, and a manual provider-assignment override, each independently confirmed via a direct `audit_logs` collection query (not trusting the API's own success response) showing the correct `role_changed`/`unit_assigned`/`provider_assigned` entries with accurate old/new values; created a real `ScoringConfig` with deliberately different weights, manually calculated what the resulting OPS score should be (70.0, versus 55.0 under the old default weights, for the same component scores), submitted a real daily check-in, and confirmed the dashboard showed exactly 70.0 - proof the admin config genuinely changes live scoring, not just a form that saves to a collection nothing reads - then restored the DOCX default weights afterward so the rest of testing wasn't skewed; credential creation correctly derived `expiring_soon` from a 30-day-out expiration date; equipment gap logged and status-updated; a utilization event logged; coverage hours logged; all 4 quarterly reports loaded correctly after the workout-data fix; CSV export downloaded with correct content, and its `ReportExport` log entry independently confirmed; the scheduler's daily job was run manually end-to-end against all 15 real users with no errors, and its new credential-expiring notification was independently confirmed sitting in the database, addressed to the right provider.

Finished with a full regression sweep: 33 endpoints across every module this project has ever built (21 operator-facing, 12 admin-facing) - all 200. The earlier self-review fix for the `== True` Beanie boolean-query pattern (scheduler, injury report) held up cleanly under all of this live traffic - no related errors anywhere, worth the caution taken before this was ever hit by a real request.

## 2026-08-07 (Records module: Medical Records, Reconditioning Plan, Fly Away Kit, Records home, Data-use summary)

12 screenshots arrived at once covering an entire "Records" section. Before building anything, flagged the two pieces that were previously deferred as open decisions in `TASKS.MD` and asked the user directly rather than guessing: (1) medical-record file uploads need a real storage backend, which had never been chosen; (2) the Fly Away Kit shows emergency contact phone numbers and a "Reconditioning Plan," neither of which this backend had any real source for. User decided: local-disk storage (encrypted at rest, migratable to cloud later) for uploads, and Admin-configured per-unit emergency contacts (never fabricated) plus a new PT/IM-and-SCS-authored Reconditioning Plan model.

**Workouts** - re-checked the already-built module against the screenshots and found real mismatches, not just missing polish: `intensity` was a 3-level light/moderate/high enum, but the "Log workout" screen shows RPE 1-5 buttons explicitly labeled "Rate of Perceived Exertion" - changed to a 1-5 integer. `conditioning` didn't match the screen's "Cardio" badge - renamed. The screen has a "Custom title (only for 'Other')" field that didn't exist at all - added, required only when `activity_type` is `"other"`. `notes` was capped at 500 chars, the screen shows "0/280" - fixed. The screen's helper text says "limit flags may create a SCS review item" - added a small deterministic keyword scan (pain/injury/hurt/sore/strain/sprain/limitation) that sets `reported_limitation` and notifies the assigned SCS, matching the DOCX's own "notes/limitations" field and "SCS views: ... reported limitations" line. The "Workout saved" screen shows "5 of 6 planned workouts completed - streak 2 weeks" - there is no "planned workout" schedule in this backend (the DOCX explicitly says not to overbuild a fitness programming platform here), so rather than fabricate one, `workouts/summary` now reports something real instead: how many of the last 6 *logged* sessions were completed, and how many consecutive weeks (ending this week) have at least one logged session.

**OFT** - already very close; the only real gap was the screen's "Next event scheduled in 38 days" relative phrasing, which the backend only had as an ISO date. Added `next_scheduled_relative`. Everything else - the `current`/`not_current`/`exempt` status headline, items passed/total - was already an exact match, confirmed by reading the existing service code rather than assuming.

**Assessments** - checked the screenshot's result bands ("Strong"/"Steady"/"Flagged"), subtitle labels ("Above target"/"On target"/"Needs focus"), and all 5 assessment-type names against the existing `RESULT_BAND_LABELS`/`ASSESSMENT_TYPE_LABELS` dicts - word-for-word matches already. No changes needed.

**Reconditioning Plan** (new) - `ReconditioningPlan` model, one active plan per user, upserted by whichever of SCS/PT-IM/Admin is authoring it (`PUT /records/reconditioning-plan/{user_id}`) and read by the operator (`GET /records/reconditioning-plan`). Honestly reports `available: false` until a real provider actually creates one - no default plan is fabricated for a user who hasn't been assigned one.

**Fly Away Kit** (new) - `GET /records/fly-away-kit` combines three genuinely real sources: the unit's Admin-set `EmergencyContactConfig` (every field null until an Admin actually enters a number - the screen's "SCS on-call: 555-XXXX" placeholder-style numbers were never treated as real data to copy), the real `ReconditioningPlan`, and the actual auto-assigned PT/IM provider already used by My Support Team (reused, not re-derived). Admin sets contacts via `PUT /admin/emergency-contacts/{unit_id}`.

**Records home** (new) - `GET /records/home`, the 6-category aggregator screen ("What's on your records?"): My Uploads, Workouts Log, OFT Status, Reconditioning Plan, Assessments, Fly Away Kit. Each category's one-line subtitle is pulled from its real module; an empty category honestly says "No records yet"/"Not available" rather than being padded with placeholder content.

**Medical Records** (new, the largest piece of this pass) - `MedicalRecord` (metadata: document type, file name/type/size, status, access-reason, uploader) plus an append-only `MedicalRecordAccessEvent` log (every upload/view/download/review recorded, matching the DOCX's explicit audit-logging requirement for medical records). `FileStorageService` writes files to local disk, Fernet-encrypted at rest with a key derived from `SECRET_KEY` (so it rotates together with the same key already flagged elsewhere in this project as an unrotated placeholder needing a real value before production) - swapping to S3/GCS later only means replacing this one service, nothing above it changes. `scan_file_stub` is explicitly documented as *not* a real antivirus integration - it only rejects a short list of obviously executable extensions (`.exe`, `.bat`, `.sh`, etc.); real malware scanning is not implemented. Routes: `POST /records/uploads` (multipart - document type, a 12-character-minimum access-reason enforced server-side, 50MB cap), `GET /records/uploads` (list, with type filter + filename search), `GET /records/uploads/{id}` (full detail + access log), `GET /records/uploads/{id}/file` (decrypted download), `POST /records/uploads/{id}/review` (PT/IM or Admin only). **Access control directly implements a specific DOCX line**: "The SCS should not receive unrestricted raw medical records by default" - so raw record access (detail view and file download) is restricted to the record's owner, PT/IM, and Admin; SCS gets a hard 403, not a UI-only restriction. Every upload is auto-routed to the assigned PT/IM via a real notification, reusing the exact `TeamAssignment` auto-assignment logic already tested for My Support Team.

**Data-use summary** (new) - `GET /records/data-use-summary`, static structured policy content. Adapted from the screenshot's own copy with the same no-CAC correction applied consistently across every Records-section screen this session (identity is described as coming from the Ascend account, not "CAC-sourced"). The "what we audit" list was rewritten to name only things this backend actually does today (medical-record access events, support-pathway toggles, message sends, login success/failure, the OPSEC and Level-5 safety-boundary scans) - the screenshot's "Every role change" and "Every scoring/config change" audit claims were deliberately dropped, since neither role-change auditing nor admin-configurable scoring exists in this backend yet, and claiming otherwise would be exactly the kind of overclaiming this project has been careful to avoid all session.

Also added `python-multipart` to `requirements.txt` (FastAPI raised `RuntimeError: Form data requires "python-multipart" to be installed` on first startup attempt after adding the upload endpoint - installed and pinned it, not worked around).

Verified live, in order: workout logging with the new RPE/cardio/custom_title fields, the "other"-without-custom_title 422, and the limitation-keyword flag - independently confirmed via a direct database query that the assigned SCS actually received the notification (not just that the API claimed to send one); registered a real PT/IM test account, confirmed it auto-assigned exactly like the earlier-tested SCS auto-assignment, and had it author a real reconditioning plan; confirmed the Fly Away Kit correctly stayed empty/null until a unit_id and admin-set contacts existed, then correctly combined all three real sources once they did; uploaded an actual PDF file end-to-end, downloaded it back, and confirmed the decrypted bytes matched the original exactly; independently read the raw file directly off disk and confirmed it's genuinely encrypted, not plaintext; confirmed SCS gets a real 403 trying to view record detail while PT/IM can view and review it; confirmed a blocked `.exe` extension 400s and a too-short access-reason 400s; full regression across 15 endpoints (records, workouts, OFT, assessments, support, dashboards, notifications, messaging, profile) stayed green throughout.

## 2026-08-07 (Sign-in & activation: real login audit trail + deactivation queue)

- same correction as the Profile screen, applied consistently here: the "Sign-in & activation" screenshot's CAC/PIV method label, EDIPI number, backup codes, IP-based "USAF base gateway" classification, and device/geolocation detail ("iPhone 14 Pro · iOS 17.5", "Pentagon / Arlington, VA") don't apply - no CAC in this project's real auth, and none of those concepts appear in the docx either. What *is* real and docx-required in their place: "login/activity tracking, access logs" (the Secure Login and Account Management module's own wording) and a "deactivation queue" (named directly in the Admin Panel module) - both genuinely missing before this pass, both built now.
- `AuthService` now writes a real `AuditLog` entry on every login attempt - success and failure - capturing the actual request's IP address and `User-Agent` header (nothing invented: no device model parsing, no IP geolocation lookup, no base/gateway classification). A failed attempt against an email that doesn't exist isn't logged (there's no real user id to attach it to); a failed attempt against a real account (wrong password) is, tied to that real user.
- added `activation_date`/`deactivation_date` to `User` - these are literally named fields in the docx's own "User Profile" data-dictionary row (`activation_date, deactivation_date`), previously untracked. `activation_date` is set at registration (and reset if a previously-deactivated account re-registers/reactivates); `deactivation_date` is set only when an Admin approves a deactivation request - never by the user themselves.
- built the full deactivation flow end to end, matching the docx's admin-led model (never self-service): `GET /users/sign-in-history` (last sign-in + up to 5 most recent real login events), `POST /users/deactivation-requests` (queues a request, rejects a second pending request from the same user, notifies every active Admin), `GET /admin/deactivation-requests` (Admin-only queue view), `POST /admin/deactivation-requests/{id}/approve` (deactivates the account for real - `is_active: false`, `deactivation_date` stamped) and `.../reject` (account stays active). Nothing is ever deleted - approved/rejected requests remain in the collection, matching the screenshot's own "No record is ever deleted" line, which is now actually true of the implementation rather than just asserted copy.
- verified live end to end: logged in successfully, then deliberately with a wrong password - both showed up correctly in `sign-in-history`, most-recent-first, with the real request IP and User-Agent; registered a brand-new account and confirmed `activation_date` was set (a pre-existing test user correctly showed `activation_date: null`, since that field didn't exist yet when that document was originally created - expected, not a bug); ran the full deactivation sequence - request succeeds, a second request while one is pending correctly 400s, a non-admin correctly gets 403 on the Admin queue, Admin approval succeeds, and then - the real proof - the now-deactivated account's next login attempt was genuinely rejected with 403 "This account is inactive"; independently confirmed `deactivation_date` via a direct database query rather than trusting the API's own response; full regression across 10 endpoints stayed green throughout

## 2026-08-07 (Profile & Settings: no-CAC identity model, editable preferences)

- the "Profile & settings" screenshot leads with "Identity is read from your CAC" and shows `[CAC]` tags on Name/Rank/Unit, an EDIPI (CAC-issued military ID number), and a footer claiming "Two-person rule applies to role elevation." The user corrected the premise directly: this project's real auth is email/password, not CAC/PKI smart-card authentication - and separately, none of CAC, EDIPI, rank/grade, or "two-person rule" appear anywhere in the docx search, confirming these are screenshot mockup realism rather than actual requirements.
- rebuilt the Identity section around what's actually true for this system: `full_name`/`role`/`unit_id` remain admin/provisioning-set (matching the docx's "Account creation should be admin/provisioning-led" model - not self-edited, not CAC-read), and asked the user how to handle Rank & grade / EDIPI specifically since docx doesn't require either. Decision: skip EDIPI entirely (a CAC-issued number this system has no way to source or verify - fabricating one would be worse than omitting it), but add `rank_grade` as an optional, user-editable, self-reported field, since it's descriptive text rather than a verified credential.
- added real editable settings - `PATCH /users/profile/settings` for `rank_grade`, `theme_preference`, `notifications_enabled` - matching the screenshot's own subtitle ("Theme and notifications are the only locally controllable settings on this surface"), now actually true instead of just asserted copy.
- extended `GET /users/profile`: `assigned_scs`/`assigned_ptim` reuse the already-tested `TeamService.get_my_team` auto-assignment (real provider names, not fabricated), `communications_preference` is derived from the existing onboarding-consent boolean `wellness_recommendations_opt_in` - documented as a 2-state proxy, because investigating this surfaced that the docx's actual "Are you willing to receive app-based recommendations and support prompts? No/Yes-limited/Yes-regular" question was never added to the 20-question onboarding bank in the first place (a real, separate gap now on record rather than silently worked around), and `sign_in_activation` (is_verified/member_since/last_login_at, all real fields already tracked).
- **explicitly not built, each with a reason**: "Records home" (no document/records module exists yet - the same open "storage provider" decision already blocking medical records and message attachments elsewhere in this project); the actual text behind "Data-use summary"/"Privacy notice" (no approved policy document exists to serve - the already-tracked consent metadata, `data_use_consent`/`policy_version_accepted`/`policy_acknowledged_at`, is exposed instead of inventing legal copy); "App version · build" (a mobile client's own binary version isn't something the backend can authoritatively state); a Sign Out endpoint (JWT auth here is stateless with no token-revocation store, so sign-out is purely a client-side token discard - a backend endpoint would have nothing real to do).
- verified live: fetched the profile before any changes (defaults: `rank_grade: null`, `theme_preference: "dark"`, `notifications_enabled: true`, `communications_preference: "Limited"`, `assigned_scs` correctly showing the real Tsgt Becker test account); called the settings PATCH with a rank/grade, `theme_preference: "light"`, `notifications_enabled: false` and confirmed all three persisted on the next fetch; full regression across 9 endpoints stayed green. (Also independently reconfirmed the earlier "Â·" mojibake pattern is a Git-Bash/curl shell-argument encoding quirk, not a backend bug: sending a middle-dot character through a `curl -d` argument failed to parse, but the identical request with only ASCII characters succeeded cleanly - consistent with the same false alarm caught two passes ago.)

## 2026-08-07 (Audit & decisions trace panel + L0-L5 routing level taxonomy)

- the "Audit & decisions" screenshot is a debug/dev overlay, not an end-user screen: a "Thread Source" section (source_type, plan_link_id, question_id, readiness_driver, route_level, assigned_to), a "Last Send · Audit Row" section (message_id, audit_event_id, audit_timestamp, attachment_count, opsec_scan, role_scope), and an explicitly-labeled "[OPEN] MESSAGING BEHAVIORS" list of 8 items in `[Open]` chips: Transport (WebSocket vs polling), Push notifications (APNs/FCM), Read receipts, Typing indicators, Group threads vs 1:1, Attachment types, Retention window, and "Crisis-keyword vs L5".
- **the bottom list was deliberately not touched.** It's labeled open/undecided by the screenshot itself - these are architecture/product questions (which transport, which push vendor, 1:1 vs group, retention policy) that need a real decision, not something for the backend to resolve on its own. Worth noting to the user: "Read receipts" is listed as open even though per-message `is_read` tracking already exists and works (built earlier this session) - either the screenshot predates that work or "open" there refers to a UI/display convention still undecided, not the underlying data being missing.
- the top two sections, by contrast, describe a real and buildable traceability feature, so that's what got built. While researching `route_level`, found DOCX Table 20 ("Routing Threshold Rules") - a full L0-L5 escalation taxonomy (Normal -> Low Signal -> Pattern Concern -> Persistent Concern -> High Priority -> Safety Boundary) that had never been formalized anywhere in this backend, despite already having built an L5-equivalent (the safety-boundary keyword flag from the previous pass) without connecting it to the other 5 levels. Added `app/core/routing_levels.py` with the table verbatim, and `GET /messaging/routing-levels` to expose it.
- added `route_level` to `Recommendation`. This required an honest compromise: the DOCX's L1-L4 triggers are multi-day pattern conditions (e.g. "two low scores within 3 days", "monthly decline >15%") that the current rule engine (`app/core/recommendation_rules.py`) doesn't structurally track - it only has simple moderate/high single-vs-repeated-check-in thresholds. Rather than either fabricate a full pattern-tracking engine or skip `route_level` entirely, mapped what exists onto the closest DOCX level (moderate -> L2, high -> L4) and documented this in both the model's usage and `routing_levels.py`'s own docstring as a **directional approximation, not a precise re-derivation** - so nobody mistakes it for full DOCX conformance later. Provider-assigned actions get `route_level: null`, since a human provider decided directly rather than a rule engine escalating automatically.
- built the actual trace: `Message` gained `source_type` (`user_initiated` vs `provider_plan_link`) and an optional `related_recommendation_id` (settable when sending a message that's a reply about a specific assigned action/plan link); every send now writes an `AuditLog` entry recording the OPSEC scan result, attachment count, and role scope; `GET /messaging/message/{id}/trace` (403 if you're not part of that thread) reconstructs the full panel shape from real data. Three fields are deliberately `null`/fixed rather than invented to match the screenshot's stylized mock values: `plan_link_id` is the real Mongo-generated Recommendation id, not a formatted code like "7G4A-2K9R-REYE"; `question_id` is always `null` because a Recommendation is generated from an aggregate component score, not any single check-in question, so there's no real per-question id to report; `attachment_count` is always `0` because no attachment subsystem exists (same open decision as document uploads elsewhere in this project); `opsec_scan` reports only the actual server-side scan result, never a claimed client-side pass this backend has no way to verify (the screenshot's "passed (client + server)" was **not** copied verbatim for this reason).
- verified live: a plain message (no plan link) traces with `source_type: "user_initiated"` and all plan-link fields `null`; assigned a real Sleep Readiness action from an SCS test account, then sent a message referencing it via `related_recommendation_id` - traced correctly with the real plan_link_id, `readiness_driver: "Sleep Readiness"`, and `route_level: null` (correctly reflecting that this was provider-assigned, not rule-engine-triggered); an Admin account not part of the thread correctly got 403 requesting the trace; `routing-levels` correctly returned all 6 entries; full regression across 9 endpoints stayed green throughout

## 2026-08-07 (Messaging thread: pathway context + OPSEC scan-preview)

- verified the messaging thread screenshot ("Tsgt B. Becker · SCS · Strength & ... · Assigned · active pathway", "Do not share schedules, tactics, unit movement, or OPSEC content. Keywords auto-flag before send.", "0 / 2000", "OPSEC scan on send · server re-validates") against the existing build. The 2000-char cap, per-message read receipts, and server-side OPSEC block-on-send were already exactly right - no changes needed there, and the footer copy already accurately describes what the backend does (scan happens server-side on send, exactly as "server re-validates" says).
- found one real gap: the thread header's pathway context ("SCS · Strength & Conditioning...", "Assigned · active pathway") had nothing behind it - `get_thread` only ever returned the raw counterparty user id/role, with no link back to the support-pathway/assignment concept built for My Support Team. Fixed: `MessagingService.get_thread` now cross-references the 5-pathway catalog against the other party's role and attaches `pathway_context` (pathway_key, label, role_title, and the real `TeamAssignment.status`) when they're a recognized provider (SCS/PT-IM) - `null`, not a guess, when messaging someone who isn't (e.g. Admin).
- added `POST /messaging/scan`: a side-effect-free OPSEC scan preview so the screen's "Keywords auto-flag before send" client-side behavior has one real backend source of truth for the blocked-term list, instead of the client having to hardcode its own duplicate copy that could drift from the server's.
- **flagged, not built**: the screen's attachment icon (📎) in the message composer. Docx has a general "no photo/video upload unless specifically enabled and approved" caveat, and this project already has an explicit open decision in `TASKS.MD` ("storage provider for documents") that nothing else - onboarding uploads, medical records - has been built against either. Building message attachments now would mean picking a storage architecture unilaterally for a feature docx treats as approval-gated; left this exactly where the existing document-upload gap already is rather than building a one-off.
- verified live: a real thread with the auto-assigned SCS provider correctly returned `pathway_context: {pathway_key: "SCS", label: "Fitness", role_title: "Strength & Conditioning Specialist", status: "locked_on"}` (this is exactly what would render as "SCS · Strength & Conditioning Specialist" / "Assigned · active pathway" on the screen); a thread with a non-provider (Admin) correctly returned `pathway_context: null`; scan preview correctly flagged `"mission"` without creating a message; an over-2000-char preview correctly 422s; full regression across 7 endpoints stayed green

## 2026-08-07 (Level 5 Safety Boundary flag + provider-side support request view)

- the "Request support" screen's disclaimer ("This is for performance support. It is not crisis care or medical advice.") maps directly to the docx's "Level 5 - Safety Boundary" routing rule: "User language indicates emergency, self-harm, violence, medical emergency, or urgent operational risk -> Show approved safety/emergency language and route outside app... Do not let AI or app decide care." That rule wasn't implemented at all - a user could type self-harm language into the context field and it would just become a normal, unprioritized support request.
- the docx also says this kind of logic "must be approved separately before launch" - flagged this to the user before building anything. The user clarified "Do not let AI or app decide care" means the app's AI shouldn't generate the care/response, a human should - and confirmed they hold the project-approval authority here as the developer, so this was explicitly authorized before building.
- added `app/core/safety_boundary.py`: a small, deliberately conservative, deterministic keyword list ("suicide", "want to die", "hurt myself", etc., word/phrase-boundary matched, no AI involvement in detection). On a match, `SupportService.create_request` still creates the request normally (never blocks it), sets `priority_flag: true` + `safety_flag_terms`, skips the OPSEC block for that message (a coincidental OPSEC word should never stop someone in crisis from submitting), writes an `AuditLog` entry, notifies the pathway's assigned provider and every Admin account via a new `safety_boundary_priority_flag` notification family, and returns a generic, publicly known emergency-resource notice in the response (911 / Veterans-Military Crisis Line 988, press 1). This is explicitly an interim conservative safety net, not the docx's still-outstanding "approved safety/emergency language and DWS/Government routing procedure," which needs real organizational sign-off on exact wording/process - documented as such in the module docstring so it isn't mistaken for that finished procedure later.
- also closed a related gap found while building this: nothing could ever change a `SupportRequest.status` away from "open" - added `GET /api/v1/support/requests/assigned` (role-gated: SCS/PT-IM see requests routed to their own role via a direct `pathway_key == role` match since the pathway keys and role constants are identical strings; Admin sees all, with requester name attached) and `PATCH /api/v1/support/requests/{id}/status` (open/in_progress/resolved, 403s if the request isn't routed to the caller's role) - this also means `follow_up_status` on the My Team screen can now actually progress instead of being permanently stuck on "open".
- verified live end-to-end: submitted a near-miss phrase ("wanting to die about") that correctly did NOT flag (documented as an inherent false-negative risk of exact keyword matching, not a bug); an exact match ("want to die", separately "hurt myself") correctly set `priority_flag: true` and returned the safety notice; independently queried the database directly (bypassing the API) and confirmed both the Admin account and the real auto-assigned SCS provider ("Tsgt Becker") received the priority notification, and both audit log entries exist with the correct matched terms; Admin successfully updated a flagged request to `in_progress`; an unrelated SCS provider correctly got 403 trying to update a Chaplain-routed request; an invalid status value correctly 400s; the operator's My Team screen correctly reflected the updated status; full regression across 9 endpoints stayed green throughout.

## 2026-08-07 (Request support screen)

- docx re-check on the "Request support" screenshot: "Pick a topic. We'll route to the right specialist." plus a request-support button on all 5 pathway cards with no visible precondition. Cross-referenced against the docx's own "My Support Team" requirement line - it lists a request-support button for all 5 pathways unconditionally; only the "message option" is gated "if enabled". This meant the existing `POST /support/requests` gate (only pathways the user had opted into/enabled) was actually too strict for this screen and needed loosening, not tightening.
- `SupportService.create_request` now validates against the full 5-pathway catalog (`get_support_pathway`) instead of the user's filtered "available" list - any of Fitness/Injury-Recovery/Nutrition/Mental Performance/Purpose is requestable regardless of prior opt-in state. `get_pathways_for_user` (the separate "Talk to your team" listing screen) is untouched and still scopes to available pathways.
- added an OPSEC content scan on the optional context message (reusing the same `scan_for_opsec_terms` used by messaging/notifications) - the screen's own copy ("Do not share operational schedules, tactics, unit movement, or OPSEC content") implies enforcement, not just a passive warning.
- fixed the context field's max length from 500 to 280 to match the screen's own "0 / 280" counter.
- **flagged for the user, not silently resolved**: the screenshot's routing subtext shows both "Fitness" (`Plan, training, OFT.`) and "Injury-Recovery" (`Rehab, return-to-performance.`) arrowing to "→ PT/IM". The docx explicitly groups Fitness under the Strength and Conditioning Specialist (SCS) role (`"...request support through My Support Team...; Strength and Conditioning Specialist / Fitness"`), matching how this backend has always routed it (SCS pathway, `specialist_route: "SCS"` on auto-generated recommendations). Kept routing as-is (docx-grounded) rather than changing it to match a screenshot detail that looks like it may be a label typo - flagged for confirmation instead of guessing.
- verified live: request submitted to a pathway never opted into (Chaplain) now succeeds (previously 400'd); OPSEC-blocked context correctly rejected with `blocked_terms: ["mission"]`; a message over 280 chars correctly 422s; an unknown pathway key still correctly rejects; the new request's `follow_up_status` correctly appears on `GET /support/team`'s Chaplain entry while its `status` stays `"disabled"` (request and enable-state are independent, as intended); also separately confirmed the "Â·" mojibake I initially saw in a terminal print of the Chaplain `role_title` was a Git-Bash console rendering artifact of my own test command, not a real bug - the raw HTTP response bytes are correct UTF-8, checked directly.
- full regression pass (9 endpoints across support/dashboards/checkins/notifications/messaging) stayed green

## 2026-08-07 (My Support Team: assigned actions, follow-up status, and an opt-in reconciliation bug)

- re-checked the built `GET /support/team` response against the DOCX's own required-element list for this module ("short description, request support button, message option if enabled, **assigned actions**, **follow-up status**, and role-based privacy boundaries") and found the last build had shipped 4 of 6 - `assigned_actions` and `follow_up_status` were missing from the response entirely.
- added both, sourced from data that already existed elsewhere in the backend (no new fabrication): `follow_up_status` is the user's most recent `SupportRequest` for that pathway (id/status/created_at); `assigned_action` is the active `Recommendation`/"Assigned Action" whose `specialist_route` matches the pathway key (id/title/status/due_date). Both are `null` when nothing exists yet, matching the established "never invent state" pattern.
- while live-testing this, found a real, separate bug: enabling an optional pathway (e.g. Nutrition) from the My Team toggle did **not** actually let the operator submit a support request to it - `POST /support/requests` was checking a completely different, older "opted in" source (the operator's onboarding question-18 answer), which had never been reconciled with the newer `TeamAssignment` toggle state built in the previous pass. Reproduced live: toggled Nutrition to `enabled`, then `POST /support/requests` still 400'd with "This support pathway is not available to you."
- fixed by making `SupportService.get_pathways_for_user` (and therefore `create_request`) treat a pathway as available if *either* the onboarding opt-in *or* an `enabled` `TeamAssignment` says so; also made a brand-new optional-pathway assignment seed its initial state from the onboarding opt-in, so choosing a pathway during onboarding shows up correctly the first time My Team is opened, without a redundant manual toggle. Documented as a one-time seed, not a live sync, since that's the honest scope of the fix.
- verified live: `follow_up_status`/`assigned_action` populate correctly after a real support request and a real provider-assigned action (registered a second SCS test account to call the role-gated `POST /recommendations/{user_id}/assign`); the toggle-then-request bug reproduced as a 400 before the fix and confirmed as a 201 after; full regression pass (`support/pathways`, `support/requests`, `support/team`, `dashboards/home`, `dashboards/wellness-report`, `recommendations/active`, `checkins/daily`, `notifications`) stayed green throughout.

## 2026-08-07 (My Support Team: real provider assignment + audit-logged pathway toggles)

- docx confirms the "My Support Team" module (line 39-40: short description, request-support button, "message option **if enabled**", assigned actions, follow-up status, role-based privacy boundaries) - directly grounds this build, including the messaging-gated-by-enabled-state concept. `IA-10 §3` (cited on the screen for the messaging-channel policy) is **not** in the DOCX - not fabricated; the underlying claim (in-app messaging only in v1, no push/email) is already true of what's built, so it's honestly reflected without inventing the source document.
- added `AuditLog` (`docs/AUDIT_LOG_RULES.md`'s suggested fields: event_type, actor_id/role, target_entity, summary_message, metadata_payload, outcome_status, append-only) - the screen's "Toggle changes are logged" line needed a real audit trail, not a decorative claim
- added `TeamAssignment` (one record per operator+pathway) and `app/services/team_service.py`:
  - `GET /api/v1/support/team`: all 5 pathways with status (`locked_on` for SCS/PT-IM, `enabled`/`disabled` for the 3 optional ones), a `messaging_available` flag, and the assigned provider if one exists
  - `POST /api/v1/support/team/{pathway_key}/toggle`: enable/disable an optional pathway, rejects attempts to disable a locked-on one (400), writes an audit log entry every time
  - **SCS/PT-IM auto-assignment is real, not fabricated**: looks up the first active user with that system role (`ROLE_SCS`/`ROLE_PTIM`) and assigns them; if none exists yet, honestly reports `provider: null` rather than inventing a name like "tsgt.becker". Nutrition/Mental Performance/Chaplain have no formal system role yet (matches the DOCX explicitly calling them "authorized support pathways, not required contract positions") so they can never auto-assign a provider until that's added - stated as a real limitation, not hidden.
  - `messaging_v1_supported` (True only for SCS/PT-IM) reflects the screen's "Send a message · v1.1" graying on the 3 optional pathways - messaging itself isn't hard-blocked server-side for those (no DOCX/screen text demands a hard block, only that the button reads differently), just flagged so the client can gray it out correctly
- verified live against Atlas: `GET /support/team` correctly showed `provider: null` for SCS/PT-IM before any provider existed; toggling Nutrition on returned the right state and **the audit log entry was independently queried from the database and confirmed present** ("Enabled the Nutrition support pathway." with correct before/after metadata); disabling SCS correctly rejected with 400; an unknown pathway correctly 404s; then registered a real SCS-role user ("Tsgt Becker") and confirmed a **newly registered operator's SCS pathway auto-assigned to that real account** - proving the assignment logic works when a provider actually exists, not just the empty-state path; full regression check of the pre-existing `support/pathways`, `support/requests`, `dashboards/home`, and `dashboards/wellness-report` endpoints passed

## 2026-08-07 (wellness report screenshot re-check: 1 real gap found)

- re-compared the Wellness Report screen against the built endpoint, specifically the "What stands out" card colors (green/orange/red = 3 distinct severities). The screenshot's example has Mental=69 (already under the 70 "worth a conversation" threshold) still rendered orange, not red - but the shipped logic conditionally flipped `lowest_driver`'s severity to `"notable"` (the same value used for the dip card, i.e. red) once the score dropped under 70, which would have shown the wrong color for that exact case.
- fixed: `lowest_driver` is now always `severity: "watch"` regardless of the exact score; `"notable"` (red) is reserved exclusively for the mid-period dip pattern, matching the screenshot's 3-color scheme (trending-up=positive/green, lowest-driver=watch/orange, dip=notable/red)
- verified offline (Mental=69 synthetic case) and live against Atlas (no regression for the already-passing Spiritual=75 case)

## 2026-08-06 (Personal Wellness Report - "Your readiness story")

- reviewed the "Wellness report" screen against the DOCX: no DOCX section names this screen or its "what stands out" concept - it's a presentation layer over already-DOCX-verified data (OPS, band, component scores, the 70-point moderate threshold). Footer text clarifies this is the *personal* report; the k-anonymity *unit-level* aggregate remains the separate `dashboards/unit-report` endpoint built earlier - confirmed these are two different screens sharing similar footer copy, not the same feature.
- added `GET /api/v1/dashboards/wellness-report`: 30-day average OPS + delta, band with a short narrative, per-driver 30-day averages, and up to 3 "standout insights" generated by real pattern detection over stored `OpsSnapshot` history (`app/services/dashboard_service.py:_build_standout_insights`):
  - **trending up**: a component where >=3 of the last 4 weekly buckets beat its 60-day baseline average
  - **lowest driver**: the lowest-averaging component over the period, with a "worth a conversation" note if under the DOCX's 70-point moderate threshold
  - **mid-period dip**: a weekly bucket well below the period average followed by a later bucket recovering back near it
  - the list holds however many of these are actually supported by the data (0-3) - never padded to a fixed count with invented content
- caught and fixed two things before going live: (1) a self-inflicted editing slip - accidentally dropped the `target_date` parameter from the pre-existing `_nearest_component_score` while inserting the new code next to it, caught on review immediately after the edit, before compiling; (2) a capitalization bug in the trending-up sentence ("three of the last four weeks" should read "Three of the last four weeks" at sentence start), caught by reading the offline test's actual printed output rather than assuming the format was right
- verified offline before touching the live DB: trending-up and lowest-driver detection against synthetic 60-day data (both fired correctly), sparse-data safety (a single snapshot correctly produces zero insights, not fabricated ones), and band-narrative logic (dip-aware vs generic)
- verified live against Atlas: correctly showed only 1 real insight ("Spiritual is steady but lowest", severity "watch" since 75 >= 70) for a test user with one day of real data - no padding to 3, exactly as designed - plus a full regression check of `home`/`trends`/`drivers/{component}`/`monthly-review` to confirm nothing broke

## 2026-08-06 (Atlas connectivity restored - live E2E of the new pass, 2 more real bugs found)

- user fixed the Atlas Network Access/IP allowlist; connection confirmed restored and re-verified (`database_connected: true`)
- ran full live end-to-end tests through everything built in the previous pass (weekly/monthly check-ins, workouts, messaging, monthly review), which found 2 more real bugs:
  1. **`OpsSnapshot` unique `(user_id, snapshot_date)` index broke on same-day cross-cadence submission**: submitting a weekly check-in and then a monthly (or daily) check-in on the same calendar day crashed with `DuplicateKeyError`, because both tried to `.insert()` a new snapshot for today. Fixed with `_upsert_todays_snapshot`: finds and updates today's existing snapshot in place if one exists, else inserts - correct behavior anyway, since `_compute_current_component_scores` already recomputes the full merged current-OPS state regardless of which cadence triggered it, so a same-day resubmit should replace today's snapshot with the freshest merged state, not create a parallel row. Verified live: submitted weekly then daily for the same user same day - daily's fresh Physical/Sleep/Mental/Nutritional (100 each) correctly overrode weekly's values, while Spiritual Readiness (which daily never asks about) correctly still came from weekly's answer (75) rather than reverting to baseline; confidence correctly computed as "high" (5/5 fresh components).
  2. **Stale private-method references**: `monthly_review_service.py` called `dashboard_service._get_recent_snapshots` / `._build_driver_overview` using their old underscored names from before those two methods were renamed to public - an oversight in the same pass, not caught by `compileall` since Python doesn't check attribute existence at compile time. Fixed by updating the two call sites.
- after both fixes, verified live: `GET/POST checkins/weekly` (+submit, all W1-W10), `GET/POST checkins/monthly` (+submit, all M1-M10, including a same-day-already-submitted 400 correctly rejecting a duplicate), `POST/GET workouts` + `workouts/summary`, `messaging/send` (both a safe message and one correctly OPSEC-blocked on "mission"), `messaging/threads`, `messaging/thread/{id}` (correctly marks read on view), the `message_received` notification firing and categorized under "updates", and `dashboards/monthly-review` (correct weights/deltas, daily check-in cadence %, workout summary, OFT `no_record` status, empty provider notes for a user with none) - plus a full regression re-check of `dashboards/trends`, `dashboards/drivers/{component}`, and `dashboards/home` to confirm the method-rename refactor didn't break anything already working
- server restarted with all fixes; `docs/E2E_TEST_STATUS.md` updated to move this pass's endpoints from "not tested" to "tested"

## 2026-08-06 (Weekly/Monthly check-ins, Workout logging, Messaging, Monthly Review)

- reviewed the "Monthly review" screen against the DOCX and found it opened much larger, real scope: exact W1-W10 and M1-M10 weekly/monthly question banks (DOCX "Weekly/Monthly Check-In - Required Questions, Answer Choices, and Scoring"), a formal "9. Workout and Activity Logging" module, and a formal "10. Messaging" module - none of which existed yet, only the daily/Day-0 check-in did
- **Weekly + Monthly check-in submission**: `app/core/weekly_checkin_question_bank.py` / `monthly_checkin_question_bank.py` (exact DOCX wording, options, and scoring for W1-W10/M1-M10); `GET/POST /api/v1/checkins/weekly`, `/weekly/submit`, `/monthly`, `/monthly/submit`, reusing the existing `CheckinAnswer` model with `cadence="weekly"/"monthly"`. W2 ("Did pain, injury, profile, or limitation affect your training this week?") is scored across all 4 options per the DOCX, not flag-only like the daily D6 equivalent.
- **OPS scoring cascade** (`checkin_service.py:_compute_current_component_scores`): replaced the daily-only-plus-baseline merge with the DOCX's actual "Assessment Cadence Weighting" rule - today's daily answer, else latest weekly answer if <=10 days old, else latest monthly answer if <=45 days old, else onboarding baseline (marked stale). This matters most for Spiritual Readiness, which the daily bank never covers but weekly/monthly do (W7/W8, M7/M8) - it can now actually refresh instead of being permanently baseline-locked. Verified backward-compatible: with no weekly/monthly data (the only case tested live so far), behavior is identical to the old logic.
- **Workout/Activity Logging** (DOCX section 9): `WorkoutLog` model with the DOCX's exact fields (date, activity type, duration, intensity, completion status, notes, optional session rating); `POST/GET /api/v1/workouts`, `GET /workouts/summary`. Kept deliberately simple per the DOCX's own instruction not to overbuild a fitness programming platform here.
- **Messaging** (DOCX section 10): `Message` model (two-party threads via a stable sorted-pair `thread_key`); `POST /api/v1/messaging/send`, `GET /threads`, `GET /thread/{other_user_id}`. Every message body is screened with the same OPSEC keyword scanner built for notifications - directly required by the DOCX here. Stated limitation: proper role-based thread visibility needs a provider-assignment/roster system that doesn't exist yet, so any active user can currently message any other active user; the client is expected to only offer a user's actual assigned providers.
- **Monthly Review** (`GET /api/v1/dashboards/monthly-review`): composes real data - 30-day driver deltas and average-OPS delta (reused from Trends), daily check-in cadence %, workout summary, OFT status, and recent provider messages as "notes for you". `review_status` is always `"draft"` - the screen's "signed off by PT Knox", immutable/locked-on-publish workflow needs a real provider identity and sign-off action that don't exist, so this is honestly represented as ungenerated/unsigned rather than faked. Medical records ("1 MRI upload...") intentionally excluded - out of scope per the medical-records-governance module.
- refactored two `DashboardService` methods (`get_recent_snapshots`, `build_driver_overview`) from private to public since `MonthlyReviewService` now legitimately reuses them, instead of reaching into underscore-prefixed internals
- **verification status - important caveat**: all new code compiles, `init_db()` succeeded once with the new models before this happened, and everything not requiring a live DB was verified (weekly/monthly question banks match the DOCX exactly including W2's full 4-tier scoring, the cascade decision logic across 5 scenarios, workout schema validation, messaging OPSEC scan). **Live end-to-end testing against Atlas could not be completed this pass**: the MongoDB Atlas connection started failing with `SSL: TLSV1_ALERT_INTERNAL_ERROR` (TLS handshake failure) partway through this work, reproduced directly against the driver with a 15s timeout, consistent across two retries a few minutes apart - not a code issue (nothing in this pass touched TLS/connection settings besides what already worked for many prior successful connections), most likely an Atlas Network Access/IP allowlist change or a network-side issue. Needs the user's attention before the newly-built endpoints can get the same live verification the rest of the backend has had.

## 2026-08-06 (added docs/E2E_TEST_STATUS.md)

- added `docs/E2E_TEST_STATUS.md`: an endpoint-by-endpoint checklist of what has actually been verified with a real HTTP request against the connected Atlas database versus what has only compiled or passed a mocked unit test, with a prioritized list of what to test next (full onboarding flow including the untested `reuse_existing=True` AI-caching branch on `baseline/complete`, D6 follow-up text, multi-day check-in sequences, and everything requiring a non-Airman role since no SCS/PT-IM/Admin test user has been created yet)
- linked from `docs/TESTING_STRATEGY.md`

## 2026-08-06 (continued E2E sweep: 3 more real bugs found - tz mismatch, silently-swallowed Claude errors, AI output overriding internal fields)

- did not stop at "3 bugs fixed, seems fine" - deliberately kept exercising previously-untested endpoints (onboarding, driver detail, trends, unit report, recommendation dismiss, support request create, onboarding answer, notification mark-all-read) after being asked to double check, and found three more real, previously-invisible bugs:
  1. **`TypeError: can't subtract offset-naive and offset-aware datetimes`** on `GET /dashboards/trends` (and would have hit `/dashboards/home` too, on the next call, once a real `OpsSnapshot` existed). Root cause: the Mongo client was never configured with `tz_aware=True`, so every datetime read back from MongoDB came back naive while every datetime created in-process (`datetime.now(timezone.utc)`) is aware - any subtraction between the two raises. Fixed at the root in `app/core/database.py` (one `tz_aware: True` client option) rather than patching every call site defensively.
  2. **The Claude AI integration had never once produced a real summary, and nobody could tell.** `AIInsightsService._call_claude` calls `json.loads(text)` directly on Claude's response, but Claude wraps JSON in a ```` ```json ```` markdown fence by default; the resulting `JSONDecodeError` was caught by a bare `except Exception` and silently swallowed, always falling back to the deterministic stub summary. Confirmed by reproducing the exact prompt/parse path directly against the real Anthropic API. Fixed by stripping the code fence before parsing (`_strip_json_code_fence`) and by logging (`logger.warning(..., exc_info=True)`) instead of silently swallowing future failures.
  3. **`status`/`model_name` in the stored AI insight were being overwritten by whatever Claude happened to put in those JSON keys** (observed: `model_name: "daily_checkin"`, `status: "Action Needed"` - neither a valid value) because the system prompt asked Claude to include them and `parsed.setdefault(...)` only fills gaps, not overrides. These are internal bookkeeping fields, not something the model should control. Fixed: narrowed the system prompt to only request `title, summary, signals, action_items`, and now set `status`/`model_name` unconditionally server-side.
- re-verified with fresh test users after each fix: `dashboards/trends`, `dashboards/home`, and `dashboards/drivers/{component}` all return correct `last_updated_label`/`last_updated_at` values; a full daily-check-in submit now returns a genuine Claude-generated summary (`status: "generated"`, `model_name: "claude-sonnet-4-6"`, contextually correct signals like "Mental Readiness scored 50 - below threshold" and "Spiritual Readiness not completed")
- also re-verified (all HTTP 200/201, real data, no errors): `onboarding/intro`, `onboarding/questions`, `onboarding/answer`, `recommendations/active` + dismiss, `support/requests` create, `notifications/read-all`, `dashboards/unit-report` (correctly gated - `available: false`, no unit assigned)

## 2026-08-06 (CRITICAL: connected real MongoDB Atlas, found and fixed 3 backend-breaking bugs)

- user provided a real MongoDB Atlas connection string; added `.gitignore` (none existed - `.env`, containing this password and the Anthropic API key, was not protected from being committed) and updated `MONGODB_URL`/`MONGODB_TLS` in `.env`
- **this was the first time any endpoint in this project was ever exercised through a real HTTP request against a real database.** All prior verification this session (and, it turns out, before it) was either `python -m compileall`, direct Python calls into service methods bypassing FastAPI's request/response layer, or mocked unit tests. Doing a real end-to-end test immediately surfaced three critical bugs, none introduced this session:
  1. **Password hashing was completely broken** (`passlib` 1.7.4 + `bcrypt` 5.0.0 incompatibility - `bcrypt` added stricter 72-byte validation that breaks `passlib`'s internal backend self-test). No user could ever have registered or logged in. Fixed by pinning `bcrypt>=4.0.0,<4.1.0` in `requirements.txt` (the standard fix for this well-known passlib/bcrypt incompatibility) and reinstalling.
  2. **Every single endpoint in the entire backend was broken at the response layer.** `success_response()` (`app/common/utils/responses.py`) returned an `ApiResponse` Pydantic instance, but every route declares `-> dict[str, Any]`; FastAPI's response validation rejected every response with a 500, even though the underlying service logic had already succeeded (visible in the error payload: real tokens, real Mongo ObjectIds). One-line fix: `.model_dump(mode="json")` before returning.
  3. **The `(A) & (B)` Beanie query-combination pattern used everywhere in this codebase does not work with Beanie 1.30.0** (`Eq`/`GTE` etc. don't implement `__and__`; raises `TypeError` at query-build time). This wasn't a pattern I introduced - it was already used in the pre-existing `onboarding_service.py`, which I copied faithfully when building every new module this session. Found and fixed all **19 occurrences across 8 files** (`assessment_service.py`, `checkin_service.py`, `dashboard_service.py`, `notification_service.py`, `onboarding_service.py`, `profile_service.py`, `recommendation_service.py`, `support_service.py`), converting `Model.find((A) & (B))` to `Model.find(A, B)` - Beanie/PyMongo AND multiple positional filter arguments implicitly.
- after all three fixes, ran a real end-to-end test against the live Atlas database: register -> login -> `GET` dashboard/home, checkins/daily, checkins/weekly/gate, support/pathways, assessments/me, users/profile (all 200) -> `POST` checkins/daily/submit (full write path: answers persisted, OPS snapshot created, recommendation engine correctly triggered "Mental Readiness score 50 is below 55" at "high" severity, AI summary stub generated, current OPS computed as 53.75/"Action Needed") -> `GET` notifications confirmed both the check-in-open and recommendation-created notifications were actually created and correctly categorized
- left 2-3 throwaway `e2e-*@ascend.mil` test users (and their check-in/snapshot/recommendation/notification records) in the connected database from this verification pass - flagged to the user, not deleted unilaterally
- this is the most consequential finding of the whole project so far: without a real database connection, these three bugs would have shipped completely undetected, since every prior verification method in this project's history avoided the exact code path that was broken

## 2026-08-06 (Trends screen: driver weights, OPS history, monthly cadence, k-anonymity report)

- reviewed the Trends screen and extended `GET /api/v1/dashboards/trends?days=7|30|90`:
  - **Driver overview**: added `weight` (reused `READINESS_COMPONENT_WEIGHTS`, already docx-confirmed .25/.20/.20/.20/.15) and `has_daily_trend` per component - Spiritual Readiness correctly shows `has_daily_trend: false` since the daily bank never asks about it (matches the mock's Spiritual card rendering as a static weight bar with no sparkline, not a coincidence - confirms the earlier baseline-fallback design was right); `delta_vs_prior_period` now respects the selected 7/30/90-day range instead of being hardcoded to 7 days
  - **OPS history (30 days)**: `ops_history` returns each day's stored band plus a `dominant_driver` (the lowest-scoring component that day - the "weakest link" interpretation of the mock's "driver mix" cell coloring, since the DOCX doesn't define "dominant driver")
  - **Next windows**: added OFT (reused existing logic, deduplicated into a shared `_build_oft_window_item` also used by Home's Upcoming) and a new "Monthly review" item - `app/core/cadence.py:next_monthly_review()` rolls every 30 days from `monthly_cadence_start_date`; explicitly documented as inferred, unlike weekly's DOCX/screenshot-confirmed fixed Tuesday-0600 anchor, because no source ever specified a fixed monthly rule
- **k-anonymity unit aggregate report**: the "Open full wellness report" card's "k-anonymity protected (k >= 5)" copy supplied the exact missing policy parameter that made me decline to build the Driver Detail screen's "Peer cohort" percentile earlier. Added `unit_id` to `User` (docx-grounded: `docs/AUTH_FLOW.md`'s Auth Payload Direction already lists "organization or unit context" as expected, though nothing previously used it) and `GET /api/v1/dashboards/unit-report`: returns unit-level averages (OPS, band distribution, per-component averages) only when >=5 users share a unit - otherwise returns `available: false` with the operator count and reason, never partial/individual-level data, matching the DOCX's repeated "aggregate only" leadership-view rule
- no admin/self-service way to assign `unit_id` yet - intentionally left as a data model addition only; the report will correctly show "insufficient cohort" until units are populated
- verified with `python -m compileall`, a route dump (new `/dashboards/unit-report`), `init_db()` with the new `unit_id` index, and smoke tests: driver overview weights match the OPS formula exactly, Spiritual's `has_daily_trend` is correctly `false`, dominant-driver picks the lowest score of the day, monthly cadence rolls correctly across cycle boundaries (including the exact-boundary day), and the k-anonymity threshold is correct at the k=5 boundary itself (not off-by-one)
- restarted the local dev server

## 2026-08-06 (self-review: 2 real bugs found and fixed)

- **OPSEC scanner false-positived on ordinary English words**: `scan_for_opsec_terms` used plain substring matching, so "mission" also matched inside "submission"/"permission"/"commission", and "classified" matched inside "unclassified" (blocking a sentence for saying something was explicitly *not* classified). Fixed to word-boundary regex matching. Verified with 8 realistic safe sentences (all pass now) and 6 realistic unsafe ones (all still caught)
- **"coordinates" was too broad a blocked term**: even with word-boundary matching, "Dr. Lee coordinates with your SCS" would have been blocked - "coordinate(s) with SCS/PT-IM" is routine clinical-coordination language used throughout the DOCX itself. Narrowed the blocked term to "grid coordinates" (the actual OPSEC-sensitive meaning), keeping "grid reference" as the other map-reference term
- **OFT `current_status` was mislabeled**: when a user had an OFT scheduled but no completed test yet, `current_status` returned `"not_scheduled"` even though `next_scheduled_date` was set - directly contradictory. Fixed to distinguish `"no_record"` (nothing scheduled or completed) from `"scheduled"` (a future test exists, none completed yet) from the actual completed status (`current`/`not_current`/`exempt`)
- found via a deliberate self-review pass (re-testing my own code with realistic inputs, not just the happy path) after being asked to confirm there were no issues, rather than a passive "looks fine" answer

## 2026-08-06 (Notifications screen: real triggers + OPSEC scan)

- reviewed the Notifications screen against `docs/NOTIFICATION_RULES.md` (its 9 named families, its User-Facing core triggers, its "avoid spamming" delivery rule) and rebuilt the notification pipeline to match
- `app/core/notification_rules.py`: the 9 DOCX families plus `message_received` (a distinct named trigger there, not grouped under another family), a `category` mapping ("reminders"/"records"/"updates") for the screen's filter tabs - `category` is our own inference, DOCX does not define UI tabs - and a best-effort OPSEC keyword scan matching the screen's own warning ("Notification copy is scanned before deep-link handoff")
- `notification_service.notify()` now rejects (`400`) any title/body containing a blocked term (mission, deployment, coordinates, convoy, etc.) - this actually matters for `assign_action`, where a provider free-types instructions that become notification copy
- **walked back the earlier "cadence-due reminders need a real scheduler, so we don't build them" position** - this screen showed "Daily check-in is open", "Weekly check-in opens Sunday", and "Your OFT is scheduled for tomorrow" all firing at plausible real moments (on check-in open, on schedule action), which don't need a cron job:
  - `checkin_service.get_daily_checkin()` fires a `check_in_reminders` notification the first time a user loads today's check-in and hasn't answered yet (deduped per day)
  - `checkin_service.get_weekly_gate()` fires one when the next Tuesday-0600 open is within 2 days (deduped per cadence cycle)
  - `oft_service.schedule()` and `assessment_service.schedule()` now fire a reminder immediately when an SCS/Admin schedules an event, phrased relatively ("tomorrow"/weekday name/date) via new `app/core/cadence.py:phrase_relative_date`
  - deliberately did **not** copy the mock's "components begin 0600" into the OFT notification - there's no real per-test start-time field behind it, and stating an unconfirmed operational time in every notification is exactly the kind of inaccurate "operational schedule" content the screen's own OPSEC warning is about
- `GET /api/v1/notifications` now accepts `category`/`unread_only` query params (matching the All/Unread/Reminders/Records tabs) and returns `total_count`, `unread_count`, and `category_counts`
- "Reconditioning plan updated/assigned" and "Recommendation from your PT/IM" notification items on the screen are already covered by the existing `assign_action`/`evaluate_and_generate` notify() calls - no new code needed there
- explicitly not built: "New message from your PT/IM" (no messaging module exists), "Record review needed"/"Medical record upload submitted" (no medical-records module exists - Phase 3 scope per `docs/PRODUCT_SCOPE.md`), and "Profile update available — unit & contact details ready to review" (not in DOCX at all, and there's no unit/roster field on `User` to source it from)
- verified with `python -m compileall`, a route dump, `init_db()`, and mocked smoke tests: category mapping, OPSEC scan (safe text passes, "mission"/"convoy" blocked), unknown-family rejection, daily-reminder dedupe (second call same day fires nothing), and weekly-reminder relative-date phrasing
- restarted the local dev server

## 2026-08-06 (Driver Detail, Weekly gate, Assessments taxonomy, Assigned Action, OFT items)

- reviewed 5 more screenshots linked from Home (Driver Detail, weekly check-in locked gate, Assessments list, Assigned action detail, OFT currency detail) and built the backend for each
- **Weekly cadence fix**: discovered the weekly gate screen's own copy ("Cadence resets every Tuesday at 0600 local") contradicts how `weekly_cadence_start_date` was being used (rolling +7 days from a per-user start date). Added `app/core/cadence.py` with `next_weekly_open()` computing the next fixed Tuesday-0600 anchor; updated the dashboard's "Upcoming: Weekly check-in" to use it. Added `GET /api/v1/checkins/weekly/gate` (informational only - weekly check-in submission itself still doesn't exist, only Day 0/daily does)
- **Driver Detail** (`GET /api/v1/dashboards/drivers/{component}`): score, DOCX score band, 30-day sparkline, 7d/30d delta, trend direction, "Try this" suggestions (`app/core/driver_detail_content.py`), and real data-backed "influences" (check-in punctuality always; recovery-trend direction for Sleep Readiness specifically, from the D3 recovery sub-question). Deliberately does **not** implement the mock's "Peer cohort" percentile or "Activity band" (steps) signals - both need infrastructure this project doesn't have (a defined comparison cohort with a k-anonymity minimum, and a wearable/steps data source); inventing numbers for either would show real users fabricated stats, which is worse than omitting the field
- **Assessments taxonomy + qualitative-only results**: expanded `assessment_type` from 2 values to 6 (`quarterly_readiness_check`, `strength_reassessment`, `cardio_reassessment`, `recovery_baseline` added) with display-title mapping; added `result_band` (strong/steady/flagged -> "Above target"/"On target"/"Needs focus") enforcing the screen's explicit privacy rule ("Canonical scoring is backend-only, raw scores are not displayed"); `GET /assessments/me` now returns `{completed (last 5), completed_total, active}` matching the list screen
- **Assigned Action** (extends `Recommendation`): added `assigned_provider_name/role`, `due_date`, `instructions`, `steps` (title/description/completed); new SCS/PT-IM/Admin-gated `POST /api/v1/recommendations/{user_id}/assign`, self-service `GET /api/v1/recommendations/{id}` and `POST .../steps/{index}/complete`; a provider-assigned action now always supersedes an auto-generated coaching nudge (new `"assigned"` severity rank, highest priority) but an auto-nudge never overrides a still-active assigned action - matches a human provider's action taking precedence over the rule engine. "Send a question about this" (messaging) intentionally not built - separate governed module
- **OFT items passed**: added `items_passed`/`items_total` to `OFTRecord` and the record-result/status endpoints, matching "Items passed: 5 of 5"
- verified with `python -m compileall`, a full route dump (5 new endpoints: `checkins/weekly/gate`, `dashboards/drivers/{component}`, `recommendations/{id}` GET, `recommendations/{id}/steps/{index}/complete`, `recommendations/{user_id}/assign`), `init_db()` after the model field extensions, unit tests of the Tuesday-0600 cadence math (including the exactly-at-open-time edge case), mocked tests of Driver Detail's score/band/delta/trend/support-routing computation, mocked tests confirming a provider-assigned action supersedes an active auto-recommendation and fires a notification, and assessment display-title/result-band-label serialization
- restarted the local dev server on port 8010 after this change

## 2026-08-04 (remaining Home-screen gaps: Trends, Profile, Support requests, Notifications, OFT, Assessments)

- built all 6 remaining gaps identified from the Home dashboard screenshot review, in the requested order:
  1. **Trends** - `GET /api/v1/dashboards/trends?days=1..180` returns full dated OPS + per-component history from `OpsSnapshot` (the compact Home preview only showed 14 days of raw sparkline numbers; this is the "See all"/"Trends" tab source)
  2. **Profile** - `GET /api/v1/users/profile` aggregates identity, current OPS/band, onboarding baseline, opted-in support pathways, and consent/policy fields into one payload (`app/services/profile_service.py`)
  3. **Support-request submission** - `SupportRequest` model + `POST/GET /api/v1/support/requests`; validates the requested pathway is actually available to the user before creating it
  4. **Notifications** - `Notification` model + `GET /api/v1/notifications`, `POST .../read`, `POST .../read-all`; event-driven only (fired when a recommendation is generated or a support request is submitted) since no background scheduler exists in this project - `docs/NOTIFICATION_RULES.md`'s cadence-due reminders (check-in/assessment/OFT due) are explicitly not faked here
  5. **OFT** - `OFTRecord` model + `app/services/oft_service.py`, per DOCX section 8.2: SCS/Admin-only `POST /api/v1/oft/{user_id}/schedule` and `.../record`, user-facing `GET /api/v1/oft/me` (current status, latest pass/fail, next scheduled date, annual test count)
  6. **Assessments** - `Assessment` model + `app/services/assessment_service.py`, per DOCX section 8.1 (Operator Assessment Tracking): SCS/Admin-only schedule/complete endpoints, user-facing `GET /api/v1/assessments/me`
- wired OFT and the initial assessment into the Home dashboard's "Upcoming" list (`_build_upcoming` is now async) - they only appear once an SCS/Admin has actually scheduled one; still no fabricated dates
- explicitly out of scope, stated rather than silently skipped: cohort-level assessment compliance rollups (50%/90% within 6/12 months), monthly OFT leadership metrics report, reconditioning plan management, and provider-side support-request reply/assignment - these are leadership/provider-facing modules, not part of the end-user Home screen this pass was scoped to
- verified with `python -m compileall`, a full OpenAPI route dump (15 new endpoints confirmed: assessments x3, notifications x3, oft x3, support x2, dashboards/trends, users/profile), an `init_db()` run confirming all 11 document models register without conflict, and mocked smoke tests confirming: recommendation creation fires a notification, support request creation fires a notification and rejects unavailable pathways, and the dashboard's Upcoming list correctly includes/excludes OFT and assessment items based on real scheduled/completed state

## 2026-08-04 (home dashboard build)

- built the Home dashboard end to end, per user request after the band-label fix, covering all four pieces: dashboard summary endpoint, driver-trend history, recommendation engine, and support-pathway listing
- `app/models/ops_snapshot.py` (`OpsSnapshot`): one snapshot per daily check-in (`ops_score`, `ops_band`, `confidence_level`, `component_scores`, `stale_components`), created inside `checkin_service.submit_daily_checkin`; powers both driver-trend sparklines and the OPS trend delta ("^+4" style)
- `app/models/recommendation.py` (`Recommendation`) + `app/core/recommendation_rules.py`: DOCX section 4 rule table (5 components x moderate/high copy, matching the DOCX's low-sleep/high-stress/poor-meal/low-activity/low-purpose examples); `app/services/recommendation_service.py` implements the DOCX trigger rule (score <55 once = high, <70 repeated across two check-ins = moderate), keeps one active recommendation at a time, only supersedes it when a strictly worse trigger appears, and exposes dismiss/complete
- `app/core/support_pathways.py` + `app/services/support_service.py`: SCS ("Fitness") and PT/IM ("Injury-Recovery") are always-available; Nutritionist/Mental Performance/Chaplain are included only if the user opted in during onboarding (question 18); `availability_status` is a static "Available" placeholder since no staffing/roster module exists yet
- `app/services/dashboard_service.py` + `app/schemas/dashboard.py`: aggregates all of the above into `GET /api/v1/dashboards/home` - greeting, current OPS block (score/band/band-meaning/confidence/trend-delta/last-updated), today's check-in card, per-component driver trends, the five-driver component scores, the active recommendation, a 2-tile support preview, and an "Upcoming" list
- new routes: `GET /api/v1/dashboards/home`, `GET /api/v1/recommendations/active`, `POST /api/v1/recommendations/{id}/dismiss`, `POST /api/v1/recommendations/{id}/complete`, `GET /api/v1/support/pathways`
- scope choice, stated explicitly rather than guessed: the "Upcoming" list only includes the weekly check-in (computed from `weekly_cadence_start_date`); OFT and "Initial HPO/H2F assessment" items from the screenshot are intentionally left out because the `oft` and `assessments` modules don't exist yet and there is no real schedule data to source them from - fabricating dates would have been a guess, not an implementation
- verified with `python -m compileall`, an OpenAPI route dump (5 new endpoints confirmed), an `init_db()` run confirming Beanie accepts the 2 new document models without conflict, and standalone smoke tests of the recommendation trigger/priority logic and the dashboard service's pure aggregation helpers (band meaning, greeting, relative-time label, driver trends, support preview, upcoming) using fake data - could not run a full DB-backed HTTP request end to end (no local MongoDB or Docker daemon available in this environment)

## 2026-08-04 (home dashboard screenshot review)

- reviewed the Home/dashboard screenshot against the DOCX; confirmed current OPS (78), confidence ("Medium confidence"), and band ("Monitor") match the DOCX's readiness-band table and the existing confidence-calculation rules exactly
- found and fixed a real bug: `build_score_band` was returning placeholder labels (`optimal`/`stable`/`strained`/`high_risk`/`critical`) invented before the DOCX's actual "Readiness Bands and User-Facing Labels" table (line ~582-606) had been read; now returns the DOCX labels (`Ready`/`Monitor`/`Caution`/`Action Needed`/`High Priority`) verbatim - confirmed 78 now maps to "Monitor", matching the screenshot
- documented the band table in `docs/OPS_SCORING.md`
- the rest of the Home screen (driver-trend sparklines, the radar/five-drivers chart, the "Today for you" recommendation card, "Talk to your team" support routing, "Upcoming" weekly/OFT/assessment cadence) maps to DOCX sections 4 (Recommendation Engine), "My Support Team", and 11 (Dashboards), none of which are built yet - these depend on the still-unbuilt `recommendations`, `support_requests`, `oft`, and `assessments` modules (all `[ ]` in `TASKS.MD`), so no dashboard aggregation endpoint was built this pass pending scope confirmation

## 2026-08-04 (checkins - D6 injury follow-up)

- added the missing "Explain your injury, or limitation" follow-up sheet for D6: when "Something to flag" is selected, a required free-text answer (120 char max) is now requested, validated, and persisted, mirroring onboarding's `text_sheet` follow-up pattern
- `app/core/checkin_question_bank.py` D6 now carries a `follow_up` definition; `CheckinAnswer.follow_up_answer`, `DailyCheckinAnswerInput.follow_up_answer`, and `DailyCheckinQuestion.follow_up`/`follow_up_required`/`current_follow_up_answer` added end to end
- the follow-up text now flows into the provider flag note and the AI payload so PT/IM and the daily-checkin AI summary actually see the injury description, not just the "Something to flag" label
- verified with a standalone validation smoke test (missing text rejected, over-120-char text rejected, valid text trimmed and stored, "None" selection requires nothing)

## 2026-08-04 (checkins - AI reuse fix)

- fixed a bug in the just-added daily check-in AI wiring: `AIInsightsService.generate_onboarding_summary`'s "return cached summary if one already exists" short-circuit is correct for onboarding (a one-time event) but was silently applied to `daily_checkin` too, meaning only the user's first-ever daily check-in would call Claude - every day after that would return the first day's stale cached summary
- added a `reuse_existing` parameter (default `True`, preserving onboarding's behavior unchanged); `checkin_service.py` now passes `reuse_existing=False` so every daily submission gets a fresh Claude call and its own new `AIInsight` record
- verified with a mocked `AsyncAnthropic` client across two simulated days: Claude call count went 1 -> 2 and two distinct records were created, confirming the fix

## 2026-08-04 (checkins)

- reviewed `1. Final Ascend App Requirements (AC).docx` (first-use flow, daily/weekly/monthly check-in question banks, OPS scoring rules) against the Day 0 check-in screenshot and `wellness-backend`'s daily check-in service before building
- implemented the `checkins` module: `app/core/checkin_question_bank.py` (D1-D6 daily bank matching the screenshot), `app/models/checkin_answer.py`, `app/schemas/checkin.py`, `app/services/checkin_service.py`, `app/modules/checkins/routes.py`, wired into `app/api/router.py` and `app/core/database.py`
- current OPS now computes on daily submit by merging today's 4 answered components with the onboarding baseline's Spiritual Readiness score (marked stale), reusing `app/core/scoring.py` unchanged
- generalized `AIInsightsService.generate_onboarding_summary` (new `insight_type`/`title_subject`/`score_subject` params, defaults unchanged) so the daily check-in reuses the same Claude-backed summary pipeline as onboarding instead of duplicating it
- renamed `User.day0_daily_checkin_status` default from `not_started` to `pending` and `current_ops_status` default from `locked` to `inactive` to match the literal trace values shown on the Day 0 screenshot; added `day0_daily_checkin_timestamp`, `current_ops_score`, `current_ops_band`, `current_component_scores`, `weekly_cadence_start_date`, `monthly_cadence_start_date` to `User`
- verified with `python -m compileall`, an OpenAPI route dump confirming `/api/v1/checkins/daily` and `/api/v1/checkins/daily/submit` are registered, and an in-memory smoke test of the scoring/merge/confidence logic (no local MongoDB was available to run a full DB-backed request)
- updated `docs/CHECKIN_FLOWS.md` and `TASKS.MD` to reflect the implemented daily/Day-0 flow; weekly/monthly cadence and admin-configurable OPS weights remain open (`ops_scoring` module)

## 2026-08-04 (later)

- reviewed auth screen mockups (sign in, sign up, account-created, forgot password, OTP verify, new password) against `docs/AUTH_FLOW.md` and the auth module implementation
- added password strength validation (1 uppercase + 1 number, on top of existing 8-char minimum) to `RegisterRequest.password` and `ResetPasswordRequest.new_password` in `app/schemas/auth.py`, matching the requirements checklist shown on the New Password screen
- updated `docs/AUTH_FLOW.md`: resolved the OTP-vs-link open question (OTP confirmed), documented that OTP resend reuses `POST /forgot-password` (no separate resend-reset-code endpoint), and flagged "Continue with Google" as UI-only with no backend implementation yet

## 2026-08-04

- reviewed the existing Ascend Markdown docs before making changes
- updated auth and environment docs to use login email as the primary identifier
- documented Gmail API setup through Google Cloud Console for Ascend email delivery
- implemented MongoDB + Beanie startup wiring and initial document models for Ascend
- implemented the first real auth module pass with JWT, password hashing, verification codes, reset flow, and authenticated current-user lookup
- implemented the first requirements-driven roles and permission layer after reviewing the final Ascend DOCX
- added backend support for the login screen flow: resend verification code, set new password, remember me, and frontend auth-screen notices/help metadata
- implemented first-use onboarding intro, consent capture, and step-progress endpoints for the welcome and before-we-begin mobile screens
- completed the screenshot-aligned onboarding question bank, answer persistence, follow-up handling, baseline OPS scoring, and AI-ready payload generation
- added onboarding question, answer, baseline preview, and baseline complete endpoints to Swagger on port 8010
- added AI insight logging and onboarding summary stub endpoints for dashboard/provider views
- wired the onboarding summary flow to Claude via the Anthropic SDK with a stub fallback

## 2026-08-02

- initialized `ascend-backend` folder structure
- created module directories under `app/modules`
- added planning and architecture docs under `docs/`
- added repository workflow docs: `AGENTS.MD`, `MEMORY_CHAT_SCOPE.MD`, `PRODUCTION_READINESS.MD`, `RULES.MD`, `TASKS.MD`, and `UPDATE.MD`
- added initial FastAPI bootstrap with settings, router composition, response helpers, and starter health/auth/users/admin routes
- added a dedicated reference guide for learning from `wellness-backend`
- added a Figma + DOCX workflow rule set for screen-by-screen backend planning

## How To Use

Add short dated progress notes here after meaningful milestones so the repository has a lightweight running history.
