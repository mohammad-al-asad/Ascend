# Tasks

## Current Phase

Onboarding flow completion and screenshot parity alignment.

## Active Tasks

- [x] create base folder structure
- [x] create core planning docs
- [x] create governance and workflow docs
- [x] create FastAPI bootstrap files
- [x] create shared config and router setup
- [x] choose database strategy
- [x] add MongoDB + Beanie foundation setup
- [x] implement auth foundation
- [x] implement roles and permission foundation
- [x] add login-screen auth support endpoints and config
- [x] implement first-use onboarding intro and consent APIs
- [x] scaffold phase-1 modules
- [x] review DOCX against Figma screen-by-screen
- [x] update Markdown docs from Figma findings
- [x] implement backend from approved screen mapping

## Phase-1 Module Targets

- [x] auth (email/password only, no CAC/PKI - real login audit trail via `AuditLog` on every attempt, `activation_date`/`deactivation_date` per DOCX's User Profile data dictionary)
- [x] users (sign-in history + admin-led deactivation-request queue - see `admin` line)
- [x] roles (`DWS Superadmin` - superset of `DWS Admin` via `ADMIN_ROLES`, bootstrapped only via `SUPERADMIN_EMAIL`, exclusively able to grant/remove admin-level roles, live-verified end to end including the plain-Admin-gets-403 restriction - + 3 new real roles for Nutritionist/Mental Performance/Chaplain, previously `role: None`, live-verified auto-assignment for both a pre-existing stuck record and a brand-new toggle)
- [x] onboarding
- [x] checkins (daily/Day 0, weekly W1-W10, monthly M1-M10 all built - the daily/weekly/monthly OPS staleness cascade is implemented)
- [x] ops_scoring (`ScoringConfig` versioned/effective-dated admin config, wired into all 4 real OPS-computation call sites; live-verified a real weight change produces the exact manually-calculated OPS score end-to-end)
- [x] dashboards (Home, Trends, Driver Detail, Monthly Review, personal Wellness Report, k-anonymity unit report for end users - all live-verified; k-anonymity minimum is now a real, admin-configurable per-role value (`RoleScopeConfig`) instead of a fixed constant, live-verified in both directions)
- [x] dashboard (separate module/folder, `app/modules/dashboard/` (singular), the 5 DOCX Table 25 provider dashboards - SCS/PT-IM/Specialist (shared endpoint for Nutritionist/Mental Performance/Chaplain)/Leadership (org-wide aggregate)/Admin, all composed from already-tracked data via `provider_dashboard_service.py`; Admin/Superadmin get an org-wide view on SCS/PT-IM dashboards instead of an empty "my assigned users" list; all 5 live-verified with real data + wrong-role 403s; SCS/Specialist dashboards now filter component-score fields by a provider's real admin-configured `visible_components`, live-verified)
- [x] recommendations (rule-based engine per DOCX section 4 + provider-assigned "Assigned Action" with steps; admin-configurable thresholds now real - `RecommendationThresholdConfig`, versioned/effective-dated exactly like `ScoringConfig`, live-verified a real config change is picked up by `evaluate_and_generate`/`_pick_candidate`; assign/dismiss/complete now real-audit-logged, closing a documented `docs/AUDIT_LOG_RULES.md` gap)
- [x] support_requests (pathway listing + request submission open to all 5 pathways + Level 5 safety-boundary priority flag + provider-side assigned-requests view/status update + My Support Team assignment/toggle/follow-up-status/assigned-action)
- [x] notifications (event-driven in-app only, triggered by check-ins/recommendations/support requests/messages; **correction: this line was stale** - cadence-due reminders from `docs/NOTIFICATION_RULES.md` are generated proactively too, via the `scheduler` line below, built later the same session - push/SMS/email delivery still not built, in-app only)
- [x] oft (SCS/Admin scheduling + result recording + user status view + `next_scheduled_relative`; monthly leadership metrics report now real - `OFTService.get_leadership_metrics_report`, registered as the 6th real report type in `REPORT_BUILDERS`, live-verified against hand-calculated pass/fail/avg-items/government-entry-compliance/annual-count-distribution numbers for 2 real records)
- [x] assessments (SCS/Admin scheduling + completion + user status view for DOCX section 8.1; cohort-level 50%/90% compliance rollup already existed via `ReportsService.get_assessment_completion_report` - confirmed, not a gap; real PRS evidence export now built separately - see `admin` line)
- [x] profile (`GET /users/profile` + `PATCH /users/profile/settings` - identity is admin/provisioning-set (no CAC in this project) + user-editable rank_grade/theme/notifications, assigned SCS/PT-IM, communications preference)
- [x] workouts (DOCX section 9 - RPE 1-5 intensity, custom_title for "other", limitation-keyword flag notifying assigned SCS, real adherence/streak; logging + summary only, no plan/program builder per DOCX's own instruction)
- [x] records (NEW module: Records home aggregator, Data-use summary, Reconditioning Plan (PT/IM+SCS-authored), Fly Away Kit (admin-set emergency contacts, never fabricated), Medical Records upload/list/detail/download/review with local-disk Fernet-encrypted storage, malware-scan stub explicitly not a real AV, SCS excluded from raw record access per DOCX; a blocked-extension upload is now stored as a real `quarantined` record and blocked from download rather than silently rejected with nothing kept, review now records a real `approve`d/denied outcome, records carry a real `access_expires_at` retention/disposition field per DOCX line 147/211's data dictionary. **DOCX line 147's data dictionary is now fully closed** (2026-08-10): `sensitivity_level`, `source`, `consent_status`, and a real per-record `approved_access_level` list all added to `MedicalRecord`; `review`/`_get_viewable` now check the record's own real list instead of the static `VIEW_ALLOWED_ROLES` constant (which is now only the default); new `PATCH /records/uploads/{id}/access-level` (Admin-only, audit logged) narrows a specific record's access - live-verified end to end: a real PT/IM reviewer could view/review a real uploaded record, an Admin then narrowed `approved_access_level` to exclude PT/IM, and the same PT/IM reviewer correctly got a real 403 on the same record afterward)
- [x] messaging (DOCX section 10 - direct messages, OPSEC-scanned + scan-preview, thread pathway context, per-message audit trace + L0-L5 routing-level catalog, encrypted file attachments (PDF/JPG/PNG/HEIC, 20MB, reuses the medical-records storage service under a separate namespace) live-verified. **2026-08-10: the remaining open items are now built.** Real roster-based authorization (`_can_message`, using real `TeamAssignment` data - allowed for Admin/Superadmin, a real assignment link either direction, or peer provider/admin coordination) replaces the old "any authenticated user can message any other" placeholder, live-verified: an unrelated Airman-to-Airman send correctly 403'd, then correctly succeeded once a real `TeamAssignment` existed. Real additive group threads (`MessageThread` model, `Message.thread_id`, 4 new routes) - every pair of participants is real-`_can_message`-checked at creation, live-verified: an unauthorized pair correctly 403'd creating a group, a real 3-party group (Airman/PT-IM/Superadmin) worked end to end, and a non-participant correctly got a 404 (not a leak) trying to view it. Real real-time delivery (`WS /messaging/live`, the exact MongoDB change-stream pattern from the Audit-log Live tail) - live-verified: a connected socket received the exact real message the instant it was sent via a separate HTTP call. Typing indicators explicitly not built (no real backend persistence need))
- [x] audit_logs (append-only `AuditLog` model + service; wired to support-pathway toggles, message sends, safety-boundary flags, login success/failure, and now recommendation assign/dismiss/complete - not yet every DOCX-listed auditable event; real `GET /admin/audit-log` search/filter/paginate + category rollup + CSV export (reuses the existing second-reviewer gate) + a genuinely real-time "Live tail" WebSocket via a MongoDB change stream, all live-verified; **retention window resolved 2026-08-10: real 7-year policy, client-confirmed**, exposed as metadata only (`retention_years` on `GET /admin/audit-log/stats`, live-verified) - deliberately not auto-deletion, since that would contradict this project's own append-only audit rule)
- [x] admin (overview placeholder + deactivation-request queue + emergency-contacts config + user role/unit/provider assignment (closes DOCX's "assign roles, coaches, specialists, teams, units" line, role-change/unit-assign/provider-override all live-verified audit logged) + provider credential tracker + Equipment/Supply Gap Tracker (8.7) + Utilization Event tracking + coverage-hours log + **6** quarterly/real reports (Injury, Assessment Completion, Utilization, PRS/QCP, Audit log, and now OFT Metrics - 2026-08-10), all live-verified + CSV **and now real PDF** export (via `fpdf2`, added 2026-08-09 Exports pass) with `ReportExport` audit log (DOCX section 12) + real scheduled/recurring exports (`ScheduledExport`, weekly/monthly/quarterly, restricted types still second-reviewer gated on a scheduled run) + second-reviewer confirmation queue (not DOCX-sourced, built per explicit request after a Figma "Control plane" screenshot: admin-level role changes, provider/admin-account deactivation, and "restricted" report exports all require a *different* Admin/Superadmin to approve before taking effect, with revert/reversibility for role changes and deactivations; live-verified including the self-approval-403 two-person-rule check and the real `TeamAssignment` caseload-count consequence summary) + real system-health metric (`SchedulerJobRun`-backed 30-day scheduler success rate, honestly `insufficient_data` until the job has actually run - not a fabricated uptime number) + real `GET /admin/system/diagnostics` (real MongoDB ping latency, real per-job scheduler health, AI-config presence - no fake microservice/session table) + real 72h export-confirmation auto-expiry + real 14-day account-inactivity surfacing (2026-08-09 System pass) + **real PRS evidence export** (2026-08-10, `GET /admin/reports/prs_evidence/export` - real per-entry `CoverageLog` rows for every SCS/PT-IM provider, the actual evidence artifact behind `meets_95pct_evidence`, live-verified against the source coverage-log endpoint row-for-row))
- [x] scheduler (`app/core/scheduler.py`, in-process APScheduler daily job proactively firing check-in/OFT/assessment/credential-expiring reminders that previously only fired when a user's own request happened to trigger them; manually run live end-to-end against all real users, credential-expiring notification independently confirmed in the database; single-instance only, not a distributed queue; now also runs a second real daily job, `unit_threshold_check` - a minimal real anomaly-advisory check, k-anonymity gated, live-verified to write a real `AuditLog` entry for a real 7-day component-score drop and correctly skip units below the cohort minimum - and a third real hourly job, `scheduled_exports`, executing real due `ScheduledExport` records, live-verified to route a restricted report type through the real second-reviewer gate and an aggregate one through immediate completion; a fourth real hourly job, `expire_stale_confirmations`, auto-rejects real pending export confirmations past their real 72h window, live-verified against a forced-past-due one while a fresh one stayed untouched; `check_unit_thresholds` now writes a real tiered L2/L3/L4 severity (reusing the real DOCX L0-L5 escalation names) instead of one flat threshold, live-verified against 3 real units with different drop magnitudes)

## Leadership Workspace (new screenshot series, started 2026-08-10)

- [x] **Index** (landing page) - no dedicated backend needed, real `GET /dashboard/leadership` already existed and covers it.
- [x] **Aggregate readiness** - real, k-anonymity-gated per-flight breakdown, monthly OPS trend, risk heatmap, feedback-session/scheduled-hours/OFT-due-soon aggregates. New `GET /dashboard/leadership/aggregate`, new `app/services/leadership_aggregate_service.py`. Real flight-level cohort linkage reuses the existing `User.unit_id`/`PATCH /admin/users/{id}/unit` mechanism - no new field needed, `OrgUnit.unit_type == "flight"` and the free-text `unit_id`-resolves-to-a-real-OrgUnit design were already in place, just never aggregated. Score-band labels reuse the real DOCX bands (`build_score_band`) rather than a second invented label system. New real fields: `CoverageLog.scheduled_hours`/`missed_reason` (additive), `OFTService.get_due_soon_count`, `ReportsService.get_feedback_session_summary`. Hero card's fabricated "Target 75" replaced with the real "Monitor" band threshold (70), explicitly flagged `target_is_approximated: true`. "Recovery program" tile and the footer's fabricated "§7.5" DOCX citation both omitted - no real concept/section exists for either. Live-verified extensively - see `docs/E2E_TEST_STATUS.md`. One real bug found live: none this pass (clean build).
- [x] **Trends** - real day-granularity trend (`LeadershipAggregateService.get_daily_trend`/`get_period_trend`, 7d/30d alongside the existing 3/6/12mo month-granularity), real 5-band distribution trend (`get_band_distribution_trend` - real Ready/Monitor/Caution/Action Needed/High Priority headcounts, not the screenshot's fabricated 3-bucket "High/Mid/Watch"), and a genuinely new feature: real admin/leadership-authored "Annotated events" editorial timeline (`LeadershipAnnotation` model + CRUD, `app/services/leadership_annotation_service.py`) - human-authored narrative content, not a computed aggregate, create open to Leadership too but delete Admin/Superadmin-only. New `GET /dashboard/leadership/trends`, `POST`/`DELETE /dashboard/leadership/annotations`. All live-verified, including an exact independent cross-check of both the day-level average and the band-distribution counts. No bugs found.
- [x] **Reports** - real Annual cadence added to `ScheduledExport.CADENCES` (was weekly/monthly/quarterly only); real by-flight OFT pass-rate breakdown (`OFTService.get_by_flight_metrics`, k-gated, folded into the existing `oft_metrics` report as `by_flight`); 3 new real report-type builders (`wing_weekly_ops`, `monthly_cohort_review`, `annual_wing_readiness`, all composing already-real `LeadershipAggregateService` methods, registered in all 3 required `REPORT_BUILDERS`/`REPORT_SENSITIVITY`/`REPORT_ROW_KEYS` locations); real new Leadership-facing report lifecycle (`ReportExport.lifecycle_status`: draft/ready/sent/archived/in_review, auto-synced with the existing second-reviewer `export_log_status`, explicit transitions via `PATCH /admin/reports/export-log/{id}/lifecycle`, audit logged); a real minimal report-template registry (`REPORT_TEMPLATES`, `GET`/`POST /dashboard/leadership/report-templates...`, "Use" creates a real `ScheduledExport`); real free-text search (`ReportExport` gained `title`/`flight_id`, `GET /admin/reports/export-log/search?q=...` matches by title, real resolved flight name, or report_type). All live-verified, including the auto in_review→ready transition on second-reviewer approval and search matching by a resolved flight name not present in the record's own title. No bugs found.
- [x] **Briefings** (final of 5 Leadership surfaces) - real executive-briefing composer. New `Briefing` model (title/outline/generated_content/draft-sent status), new `BriefingService`, real minimal template registry (`BRIEFING_TEMPLATES`: Mission Readiness/Recovery Rollout/Quarterly Wing Review, all built only from real section types - "recovery signal"/"FY-quarter comparison"/"next quarter" screenshot language dropped, not claimed). 7 real section types (`mission_context`/`composite_trend`/`driver_snapshot`/`by_flight`/`oft_snapshot`/`band_distribution`/`risk_recommendations`), all composed from already-real, already-verified Leadership methods - `risk_recommendations` scans real `AuditLog(threshold_warning)` severity + real `LeadershipAnnotation` entries. Real AI narrative generation - new `AIInsightsService.generate_briefing_section_narrative`, extends the exact real Claude-calling pattern already proven for check-in summaries (real deterministic stub fallback on no API key or failure, verified both paths). While a briefing is `"draft"`, content live-regenerates from current real data on every fetch; `send()` freezes it permanently, audit logged. New prose-capable PDF renderer (`render_prose_pdf`) since the existing one is strictly tabular. **One real bug found and fixed live**: `render_prose_pdf`'s `multi_cell` calls didn't pass `new_x="LMARGIN", new_y="NEXT"`, so the cursor stayed at the right edge after the first paragraph and the second call crashed with `FPDFException: Not enough horizontal space` - the exact same fpdf2 gotcha the existing tabular `render_pdf` already handles on its title cell, just never applied to the new method. Fixed and re-verified with real PDF bytes. All 10 verification steps passed live, including proving the AI call genuinely fires (fluent multi-sentence prose referencing exact real numbers, not the terser stub) and proving "live" regeneration by creating a new real annotation and confirming it appeared in the very next fetch of the same draft.

**This closes the full 5-surface Leadership workspace series** (Index, Aggregate, Trends, Reports, Briefings) - started and finished 2026-08-10. **Correction (2026-08-11)**: asked to double-check the Briefings screenshot, found `mission_readiness`'s 4th section had used `oft_snapshot` (matching the template card's abbreviated blurb) instead of `by_flight` (matching the screenshot's own concrete, worked "Mission readiness" Outline/Preview example - the stronger source of truth). Fixed and re-verified live.

## Pending / Deferred (waiting on more screenshots)

- [x] Admin/Superadmin Control Plane - **Roles & RBAC** sub-page (role catalog with real cluster/scope metadata + member/audit counts, RBAC matrix, purpose-consent summary, Contract Question Registry - see `docs/CHANGELOG.md`/`UPDATE.MD` 2026-08-09 entry for the full "Plan" role / cluster-scope / question-registry decision trail; **the RBAC matrix was initially built wrong** - derived from `ROLE_PERMISSIONS` instead of each capability's real route gate, only 2 of 9 rows were live-checked at the time and the other 7 were wrong for Admin/Superadmin/PT/IM in several places, plus 2 rows referenced actions with no real endpoint at all - caught when asked to double-check every screen, rebuilt from real `require_roles(...)` gates with a citation per row and re-verified against live endpoint calls, see the 2026-08-09 "Correction" entry in `docs/CHANGELOG.md`/`UPDATE.MD`)
- [x] Admin/Superadmin Control Plane - **Scope matrix** sub-page (the "10 x 5" mystery resolved: 10 roles x 5 non-self scope columns. Real org unit hierarchy (`OrgUnit`), real admin-configurable cohort-k + per-role driver-visibility (`RoleScopeConfig`, actually wired into the k-anonymity gate and the SCS/Specialist dashboards, not decorative), real coverage-map and partial-real pathway-matrix endpoints - see `docs/CHANGELOG.md`/`UPDATE.MD` 2026-08-09 entry; **the coverage map's `unit_visibility`/`aggregate_wing` columns had the same `ROLE_PERMISSIONS`-derived bug as the RBAC matrix** - wrongly excluded Admin/Superadmin - fixed in the same correction pass)
- [x] Admin/Superadmin Control Plane - **Audit log** sub-page (real search/filter/paginate + category rollup + CSV export reusing the existing second-reviewer gate + a genuinely real-time "Live tail" via a MongoDB change stream (not polling) + a new minimal-real threshold-warning scheduler job + closed a real documented gap where recommendation assign/dismiss/complete were never audit-logged + real medical-record quarantine/review-outcome/retention-expiration lifecycle fields - see `docs/CHANGELOG.md`/`UPDATE.MD` 2026-08-09 entry)
- [x] Admin/Superadmin Control Plane - **Exports** sub-page (real PDF export via `fpdf2` alongside CSV, real scheduled/recurring exports - weekly/monthly/quarterly, restricted report types still routed through the real second-reviewer gate, deliberately no fabricated "auto-pause on scope change" - real non-`L`-prefixed risk tier (low/elevated/high) to avoid colliding with the real L0-L5 escalation catalog, exports overview composing real pending/recent/schedules/5-real-report-types with no fabricated due-dates or approvals - see `docs/CHANGELOG.md`/`UPDATE.MD` 2026-08-09 entry)
- [x] Admin/Superadmin Control Plane - **System** sub-page, final Control Plane screen (real RSD weekend-coverage aggregation - a genuine pre-existing DOCX line 239 gap, `CoverageLog.is_weekend_rsd` was captured but never surfaced - + real diagnostics (DB ping, real per-job scheduler health, AI-config presence, no fake microservice/session table) + real tiered L2/L3/L4 threshold-warning severity reusing the real L0-L5 escalation names correctly + real 72h export-confirmation auto-expiry + real 14-day account-inactivity surfacing (re-read "applies to: Inactive accounts" correctly, not a confirmation-timeout) + investigated the screenshot's "W5 reverse/M5 direct" scoring claim in depth and found it **did not describe a real gap** - reverse-scoring already exists in code and every W/M question already scores correctly via worst-to-best option ordering, so nothing was changed there, avoiding introducing a real scoring bug - see `docs/CHANGELOG.md`/`UPDATE.MD` 2026-08-09 entry)
- [x] DOCX line 147's medical-record data dictionary - closed 2026-08-10, see `records` line above.
- [x] Admin/Superadmin Control Plane - **Scope matrix "Coverage · active selection" panel** - closed 2026-08-10: `RoleAdminService.resolve_scope(role, unit_id)` + `GET /roles/scope-resolve?role=X&unit_id=Y`, real `OrgUnitService.resolve_ancestors` path combined with that role's real coverage-map row, live-verified against a real 2-level org unit.

- [ ] **SMS/push notification delivery** - `app/services/sms_service.py`/`push_service.py` are real, working Twilio/FCM integrations, but no real credentials exist yet (honestly report `{"sent": false, "reason": "not_configured"}`, never fabricated). See `docs/CREDENTIALS_NEEDED.md` for the exact client ask. Email is different - live and wired via Resend (`app/services/email_service.py`), but real delivery is currently blocked by Resend's sandbox mode (only sends to the account owner's own address until a domain is verified) - confirmed live 2026-08-10 via a real triggered safety-boundary flag, which correctly attempted a real send and got a real, honestly-logged 403 from Resend, not a silent failure. Also needs a client decision - see `docs/CREDENTIALS_NEEDED.md`.

## Open Decisions

- [x] PostgreSQL vs MongoDB
- [x] SQLAlchemy vs Beanie
- [x] background job approach (in-process APScheduler, single-instance - would need Celery/RQ + a broker for multi-instance deployment)
- [x] storage provider for documents (local disk, encrypted at rest - dev/prototype choice; migrate to S3/GCS/Azure Blob before production, only `app/services/file_storage_service.py` needs to change)
- [x] **audit-log retention window** - resolved 2026-08-10: **7 years**, client-confirmed. See `audit_logs` line above.

## Notes

Onboarding baseline now includes question bank, answer persistence, score calculation, provider flags, and AI-ready payload generation.

**2026-08-10: Parts HH-QQ, the full "finish everything remaining" batch, planned and live-verified same pass.** Triggered by "ok complete kore felo" after a full honest accounting of every open gap across the backend. Two real policy/infra decisions were asked first rather than assumed: audit-log retention (client answered 7 years) and SMS/push/email delivery with no real credentials (client answered: build the real integration code, write a credentials-request doc for the client). Ten parts built: medical-record DOCX fields closed, Scope matrix per-user drill-down, admin-configurable recommendation thresholds, real OFT leadership metrics report, real PRS evidence export, real roster-based messaging authorization, real group threads, real real-time messaging (WebSocket), real live email via Resend + honest SMS/push scaffolding, and audit-log retention exposure + `docs/CREDENTIALS_NEEDED.md`.

Live verification hit a real infrastructure blocker mid-pass: MongoDB Atlas became unreachable (`SSL handshake failed` from both a fresh script and the already-running server) for roughly 40 minutes. Rather than guess or skip verification, a background poller watched for it to recover and the live-verification pass resumed automatically the moment it did - no part was marked verified without an actual passing live check.

Two real bugs were found and fixed *during* this live-verification pass (not introduced by it):
1. **Route-ordering collision**: `GET /admin/reports/prs_evidence/export` was silently swallowed by the earlier-registered generic `GET /admin/reports/{report_type}/export` (FastAPI matches path templates in registration order), returning "Unknown report type" instead of the real evidence CSV. Fixed by moving the specific route before the generic one.
2. **Pre-existing type-mismatch bug, unrelated to this pass**: `CoverageService.list_for_provider`/`total_hours_for_provider`/`total_rsd_hours_for_provider` compared a raw string `provider_id` (from the HTTP path param) against `CoverageLog.provider_id`, a real `PydanticObjectId` field - MongoDB never matches a string against a BSON ObjectId, so `GET /admin/coverage-logs/{provider_id}` had been silently returning an empty log for every provider since that endpoint was built. Found by cross-checking the new PRS evidence export's real rows against this endpoint and getting 2 vs. 0. Fixed with a shared `_as_object_id` coercion helper used in all three methods; re-verified both endpoints now agree exactly.

The Resend email integration is real and confirmed live (a direct test send to Resend's official test address succeeded), but real delivery to actual users is currently blocked by Resend's own sandbox restriction (send-to-self-only until a domain is verified) - confirmed by triggering a real safety-boundary-flagged support request and watching the real (honestly logged, not swallowed) 403 from Resend's API. Documented in `docs/CREDENTIALS_NEEDED.md` as an additional real client ask (verify a sending domain).

Full 12-step live-verification pass, all real HTTP/WebSocket calls against real data (no mocked checks): Scope drill-down against a real 2-level org unit; a real threshold config change confirmed picked up by the recommendation engine (then cleaned up so it doesn't affect real users going forward); OFT metrics matched hand-calculated numbers for 2 real records; PRS evidence matched the (bugfixed) source coverage-log endpoint row-for-row; messaging authorization correctly 403'd an unrelated pair and correctly allowed a real `TeamAssignment`-linked pair; a real 3-party group thread worked with correct non-participant 404 isolation; real-time WebSocket delivery received the exact live-sent message; medical-record access-level narrowing correctly flipped a real PT/IM reviewer's access from 200 to 403; SMS/push honestly reported `not_configured`; audit-log stats exposed the real `retention_years: 7`. Regression sweep on 8 pre-existing core endpoints, all 200.

**2026-08-09: Correction pass, triggered by being asked to double-check every Control Plane screen for mistakes.** The RBAC matrix (`GET /roles/matrix`) and part of the Scope matrix coverage map were built from `ROLE_PERMISSIONS` (a display-purpose dict, not an access-control map) instead of real route gates - only 2 of the matrix's 9 rows had ever been live-checked, and the other 7 were wrong in several places for Admin/Superadmin/PT/IM, plus 2 rows described actions with no real endpoint at all. Rebuilt both from real `require_roles(...)` gates with a citation per entry, re-verified against live HTTP calls to the actual routes this time (not just the matrix's own output). Full detail in `docs/CHANGELOG.md`/`UPDATE.MD`'s "Correction" entry.

**2026-08-09: System screen, planned and live-verified same pass - final screen in the Control Plane series.** Checked against the DOCX and got a real surprise: line 239 ("Track RSD weekend support coverage separately") is a genuine pre-existing gap - `CoverageLog.is_weekend_rsd` had been captured since that model was built but never aggregated anywhere, closed without needing to ask. Everything else on the screen (fake services/latency table, active sessions, "9 required contract reports"-style due-dates, tiered threshold rules, a "W5 reverse/M5 direct" scoring claim) wasn't grounded in the DOCX, reported plainly, 4 decisions - all "build real" except explicitly investigating the reverse-scoring claim first. That investigation is the most important part of this pass: read all 20 real weekly/monthly questions and their options, and found the screenshot's claim did not describe a real gap - the `reverse_scored` scoring mechanism already exists in `app/core/scoring.py` and is already wired up, but every single weekly/monthly question already lists its options worst-to-best, which the *default* (non-reversed) scoring path already handles correctly. Setting `reverse_scored=True` on W5 or M5 as the screenshot implied would have inverted currently-correct scores into wrong ones - a real bug that would have been introduced by literally following the screenshot. Did not touch the scoring flags; the System panel states the real, verified fact instead. Live-verified everything else: real RSD hours logged for a real PT/IM provider showed up correctly in the PRS/QCP report's new `rsd_coverage` field, separate from the main total; `GET /admin/system/diagnostics` returned a real measured MongoDB ping latency and real per-job scheduler health for all 4 jobs; a real restricted export confirmation forced past its real 72h `expires_at` was correctly auto-rejected by the new expiry job, while a non-expired one was correctly left untouched; a real account with `last_login_at` forced 15 days into the past correctly appeared in the real inactivity list, a recently-active one correctly didn't; 3 real test units with deliberately different score drops (6/12/25 points) correctly resolved to L2/L3/L4 severity respectively, with exactly one audit entry written per unit/component (not one per tier crossed). 34/34 checks passed. This closes out the Control Plane series - the remaining known gaps (partial medical-record DOCX fields, the Scope matrix's per-user resolved-view panels) are tracked separately above, not part of this screen.

**2026-08-09: Exports screen, planned and live-verified same pass.** Fifth screen in the Control Plane series. Checked against the DOCX (section 12 is 2 lines: "PDF and CSV where practical" + the export-log fields already built) - most of the screenshot's specifics (scheduled exports, PPTX, "9 required contract reports" with due-dates/named-approvers, `L1`/`L3`/`L4` export-risk labels) weren't grounded in it. Reported plainly; user chose real scheduled exports, real PDF (not PPTX), real distinctly-named risk tiers instead of the `L`-colliding labels, and showing only the 5 real report types instead of fabricating the other 4. Added `fpdf2` (pure-Python PDF, no system deps) as a new real dependency. `ScheduledExport`'s scheduler job routes restricted report types through the exact same real second-reviewer gate an on-demand export uses - never bypassed for a scheduled run; deliberately did not build the screenshot's "scope change auto-pauses a schedule" since no real trigger condition defines that in this system. Live-verified: real PDF bytes (`%PDF` header) returned and correctly re-downloadable later in the same format; a scheduled restricted (injury) export correctly created a real pending confirmation instead of completing immediately, while a scheduled aggregate (utilization) export correctly completed immediately; a paused schedule correctly stayed un-executed even past its due time; the Exports overview correctly showed exactly 5 real report types with no fabricated fields. 27/27 checks passed, no server errors.

**2026-08-07: Atlas connectivity restored, full live pass complete.** The scheduler/scoring-config/admin-management/reports batch above is now fully live-verified. One more real bug was caught in the process: 2 pre-existing `WorkoutLog` documents still had old string-based `intensity`/`activity_type` values from before an earlier schema change, crashing on read - fixed with a one-time data migration on just those 2 documents. Full regression: 33 endpoints, all 200.

**2026-08-09: Atlas connectivity restored, full live pass complete.** The Superadmin/specialist-roles/5-dashboards batch above (planned via `EnterPlanMode`/`ExitPlanMode`, approved before coding) is now fully live-verified, including a real Superadmin account created via `SUPERADMIN_EMAIL` bootstrap. A 6th real bug was caught live (on top of the 5 found via static self-review): `TeamService`'s provider auto-assignment retry was gated to locked-on pathways only, so a pre-existing optional-pathway assignment record never picked up a provider even after that pathway's role became real - fixed for both the retry-on-read and initial-creation paths. Full regression: 39 endpoints, all 200.

**2026-08-09: Admin/Superadmin Control Plane Overview, planned and live-verified same pass.** Triggered by a shared Figma screenshot; checked the DOCX first and confirmed system health % and the second-reviewer/reversibility workflow are not DOCX-sourced before building either (user chose to build both as real new scope, not skip or fabricate). Live-verified with two real Admin-level test accounts as requester/reviewer: role-change and deactivation confirmations correctly stayed un-applied until a *different* reviewer approved (self-approval 403'd), `revert` correctly restored both a role and a reactivated account from their stored snapshots, the real PT/IM test account's 2 real `TeamAssignment` rows were independently confirmed nulled in the DB after deactivation approval, the `injury` export correctly gated (202 pending, 403 download pre-approval, 200 CSV post-approval) while `utilization` kept exporting immediately unchanged, and `system_health` moved from `insufficient_data` to a real `100.0 / "Healthy"` after triggering one real scheduler run.

**2026-08-09: Audit log screen, planned and live-verified same pass.** Fourth screen in the Control Plane series. Core infrastructure already existed (`AuditLog`, ~15 real event types) - this was about exposing and extending it, not inventing a parallel system. Checked against the DOCX and `docs/AUDIT_LOG_RULES.md` first and found 2 real, already-documented gaps this pass closed: recommendation assign/dismiss/complete were never audit-logged despite `AUDIT_LOG_RULES.md` explicitly requiring it, and `MedicalRecord` had none of the DOCX's own required "retention status"/"disposition status"/"approved access level" fields. 4 more things (retention window, live streaming, threshold-warning auto-flagging, medical-record event richness) weren't grounded in either source - reported plainly, user chose to omit the retention figure (real open policy question) and build the other 3 for real. Live-verified: real filter/search/pagination over `AuditLog`; the "Recommendation changes"/"Resolved/archived" category counts actually moved after a real assign+dismiss (previously always 0, nothing logged them); audit-log CSV export correctly reused the exact same second-reviewer gate already built for the `injury` report (202 pending, blocked download pre-approval, real CSV post-approval); a real WebSocket client connected to `/admin/audit-log/live` and received a genuinely real-time entry (via a MongoDB change stream, not polling) the instant a real role change was made in a separate request - caught and fixed one real bug along the way (`.watch()` is itself a coroutine in this pymongo version, not directly `async with`-able); `check_unit_thresholds()` correctly wrote one real advisory entry for a real 3-person test unit with a genuine 25-point 7-day Physical Readiness drop, gated behind the real configured k-anonymity minimum; uploading a blocked file extension now correctly quarantines the record (stored, not silently rejected) and blocks download even for PT/IM; reviewing with `approve: false` correctly set `reviewed_denied`. Full regression, 31/31 checks passed, no server errors.

**2026-08-09: Scope matrix screen, planned and live-verified same pass.** Third and largest screen in the Control Plane series - resolved the Overview screen's "10 x 5" mystery (10 roles x 5 non-self scope columns) along the way. Checked against the DOCX and the real system first (zero hits for Wing/Flight/Detachment/cohort/driver-visibility/conditional-pathway anywhere), reported that essentially the whole screen was Figma-invented, and got 3 decisions to build real new infrastructure rather than skip. Live-verified: a real 2-level `OrgUnit` (Wing -> Detachment) resolved its `ancestor_path` correctly; setting a real `cohort_k=3` for the Airman role made a real 3-person test unit's k-anonymity-gated report open where it was correctly unavailable at the default k=5, and restoring k=5 correctly closed it again; excluding "Physical Readiness" from SCS's real `visible_components` correctly hid that field (`None`) on every real operator row on `GET /dashboard/scs`, restoring it correctly stopped forcing it to `None` (though the one real test operator happened to have no actual Physical score recorded either way, so that specific restore check couldn't distinguish "filtered" from "genuinely empty" - the hide-side of the test already proved the filter works); `GET /roles/scope-matrix`'s `self` counts matched the catalog's real counts exactly; `GET /roles/pathway-matrix`'s staffing count matched a real independent count of active Nutritionist accounts, with no fabricated approval/enablement dates present. 28/29 checks passed cleanly, the 1 non-pass was a test-script limitation (explained above), not a product bug. Full regression on the existing endpoint list, all 200.

**2026-08-13: `ascend-admin` frontend gap analysis + medical-record field redaction, planned and live-verified same pass.** The `ascend-admin` Next.js frontend (a separate repo/project, its own auth and admin store both 100% local mock - explicitly out of scope for integration per the user, who redirected to "just look at each dashboard and build the backend") was read page-by-page (9 role dashboards + `roles/page.tsx`) and cross-referenced against every real `ascend-backend` route, producing `docs/ASCEND_ADMIN_ENDPOINT_GAP_ANALYSIS.md` - Admin and Leadership are fully backed; PT/IM (injury taxonomy, ROM tracking, quarterly injury-rate reporting, IDMT handoff) and the food/macro-logging half of Nutrition are the largest real gaps; the `Plan` role has no backend anchor at all (`SUPPORTED_ROLES` doesn't include it). User picked "Provider notes" (MP/PT-IM/PC clinical documentation) as the next build target - a DOCX check first found this is not just unsourced but **directly contradicted** ("the app should not become a medical record, behavioral health record, chaplain record... or official clinical documentation"), so it was skipped in favor of the one real, DOCX-backed adjacent requirement: Section 15's "support document masking or redaction... before information is shared with non-clinical roles." Built field-level redaction on `MedicalRecord` - `CLINICAL_VIEW_ROLES` (PT/IM + Admin, unchanged) always get the raw record; any other role reachable only via an admin-widened `approved_access_level` (e.g. SCS) now gets `file_name`/`access_reason`/access-log notes masked by default, with a reason-required, audit-logged, one-time (not persisted) `POST .../reveal-field`, and a hard 403 on raw file download. No new model - purely extends the existing `MedicalRecordAccessEvent` audit trail and `MIN_ACCESS_REASON_LENGTH` validation already used for uploads. Live-verified with 4 fresh `e2e-redact-*` accounts (Superadmin, Airman, SCS, PT/IM): masking correctly applied only to the non-clinical SCS viewer, `is_redacted` flag correct in both directions, file download correctly 403'd for SCS, reveal-field correctly returned the real value once and re-masked on the next fetch, a too-short reveal reason correctly 422'd, a non-redactable field name correctly 400'd, and PT/IM/Admin's existing unredacted access was confirmed unchanged (no regression).

**2026-08-09: Roles & RBAC screen, planned and live-verified same pass.** Next screenshot in the same Control Plane series. Checked it against both the DOCX and the actual question-bank/role code before building - found a "Plan" role, cluster/scope pills, access-expiration/purpose-consent tracking, and a 46-row Contract Question Registry, none of which existed in either source. Reported all of it and got 4 explicit per-item decisions from the user (skip the fake "Plan" role; build cluster/scope as new real metadata; build access-expiration and purpose-consent as real tracking; build a simplified 40-question registry instead of the fabricated 46-row one). Live-verified: `GET /roles/catalog`'s `member_count` for SCS matched an independent direct DB count exactly; `GET /roles/matrix` correctly marked only Admin/Superadmin's "Deactivate user" cell `gated` (matching the real second-reviewer requirement from the prior pass) and Airman's `none`; a freshly-registered user got a real `access_expires_at` ~365 days out, and `renew-access` correctly extended it; toggling the Chaplain pathway off for a real test user incremented `withdrawn_count` by exactly 1 without inflating `active_count`, and re-enabling correctly reversed both without double-counting; the question registry correctly reported `total_questions: 40` (not 46) with real per-question component/routing data. Full regression: 36 checks, all passing.

**2026-08-13: PT/IM injury taxonomy - real DOCX fields + net-new severity/days-out.** Next gap-analysis item after the redaction pass. A DOCX check found this area is a mix: Section 8.4 (line 752) names a real `Injury/Recovery` record shape - most of it already existed under different names in `ReconditioningPlan` (`phase`≈status, `ptim_clearance_status`≈return-to-performance status, `next_review_date`≈ptim_follow_up_date), but `limitation_flag`/`rehab_strategy_summary`/`scs_coordination_status` were genuinely missing. The "Quarterly Injury/Recovery Report" the DOCX also names turned out to **already be built** (`ReportsService.get_injury_report()`/`admin/reports/injury`, live-verified earlier this session) - correcting an overstatement in the gap-analysis doc, which had called it "not built" while really only describing the frontend mock's unsourced "rate per 100 airman-months" framing, not the real DOCX per-individual-list version. Diagnosis/ICD codes, severity grading, and days-out tracking have no DOCX anchor at all (DOCX explicitly disclaims "OPS is not a diagnosis... or substitute for PT/IM judgment") - user explicitly chose to build severity (`L1`-`L4`, reusing the existing threshold-warning tier vocabulary) and days-out anyway as real net-new operational tracking, not fabricated diagnosis. `days_out` is derived (`today - injury_reported_on`, null once `phase == "completed"`), same real-derivation pattern as the pre-existing `days_in_phase`, never a stored/staleable counter. Extended `get_injury_report` with the 5 new row fields plus one honest real aggregate, `severity_breakdown` (real counts of what's in the window - not a fabricated per-100 rate). CSV/PDF export needed zero code changes - `report_export_service.py` already derives columns from row dict keys. Live-verified: all 5 new fields round-tripped correctly through create/read; `days_out` computed to the exact real backdated value (10) and correctly went `null` on completion; `admin/reports/injury`'s row and `severity_breakdown: {"L3": 1}` matched exactly; a direct in-process CSV render against real DB data confirmed all 5 new columns present with no export-service changes (the `injury` export's second-reviewer approval gate was already proven working earlier this session, so re-proving it here would have only required bootstrapping a throwaway 3rd admin, not tested anything new); a second, untouched operator correctly still returned an honest `available: false`.

**2026-08-13: Nutritionist meal-consistency/hydration trend - real, DOCX-grounded, no food/macro log.** Third gap-analysis item. The frontend mock shows per-meal food logging (kcal/carb/protein/fat, a macro donut) - a DOCX check found **zero anchor** for any of it: no macro/calorie/food-item concept, no "consult" booking concept, anywhere in the DOCX. What the DOCX *does* define for Nutritionist (role-scope text) is narrower: "review meal consistency, skipped meals, hydration, quick/processed meal patterns, and related energy/recovery summaries." User explicitly chose the DOCX-grounded path over the net-new food-log path. Found the real question inventory this maps to - 5 real check-in codes tagged `readiness_component: "Nutritional Readiness"` (`d0_04`, `w_03`, `w_04`, `m_03`, `m_04`) - and one honest, documented gap: no question anywhere covers "quick/processed meal patterns" specifically (grepped the full question-bank codebase, zero matches), so that one DOCX phrase was left out rather than approximated with a fake field. Built `ProviderDashboardService._build_nutrition_signals()`, reusing the exact real query-then-first-vs-last-delta pattern already in `DashboardService._build_influences` (its `d0_03` recovery-trend logic), wired into the existing `GET /dashboard/specialist` route (no new route, no new model) - role-gated so only the Nutritionist pathway gets `nutrition_signals` per operator row plus one real `low_consistency_operator_count` aggregate over their own already-visible caseload (no k-anonymity concern - same access level they already have to individual scores). `raw_score_1_to_4 <= 2` (an already-real field) is reused directly as the "flagged/skipped/low" signal instead of hardcoding per-question option strings. Live-verified: inserted 2 real, correctly-scored `CheckinAnswer` rows (via the real `score_ordered_answer` function, not hand-typed numbers) showing a genuine low-to-high `w_03` consistency swing - the Nutritionist's real dashboard correctly returned `meal_consistency_trend: "improving"`, `skipped_meals_or_low_hydration_flags_60d: 1`, `checkins_logged_60d: 2`, and `low_consistency_operator_count: 1` at the top level, all matching hand-verification exactly; a Mental Performance test account's dashboard correctly showed `low_consistency_operator_count: null` and no `nutrition_signals` leak (role-gating confirmed).

**2026-08-13: `Plan` dashboard - decision only, nothing built.** Fourth gap-analysis item, picked by the user specifically as "decide first." A targeted DOCX check for the actual workflow the frontend mock shows - a plan authored once and routed with read/write/notify access across all 5 provider roles (SCS/PT-IM/MP/Nutrition/Chaplain) at once, with cohort-level progress tracking - found no real requirement anywhere. Every "plan" concept the DOCX actually defines (Training plan, Reconditioning plan, Return-to-Performance, Kit/Fly-Away plan) is single-owner or, at most, the SCS↔PT/IM dyad already built as `ReconditioningPlan` - never a 5-role shared object. This is a second, independent confirmation of the same conclusion an earlier pass reached from a different angle (2026-08-09, Roles & RBAC: a "Plan" *role* was found and skipped for lack of DOCX backing). User decided: skip, no code written - marked resolved-as-not-building in `docs/ASCEND_ADMIN_ENDPOINT_GAP_ANALYSIS.md` rather than left as an open question.

**2026-08-13: Plan-link category tag + real IDMT Documentation Handoff.** Fifth gap-analysis item, split into two very differently-grounded halves by a DOCX check. "SCS coordination items" (frontend: a full "Open items" table with trigger/owner/status columns) turned out to be only a **category label** in the DOCX ("Every provider-facing plan link must state whether it is an SCS recommendation, PT/IM review item, joint coordination item, specialist routing item, or user-only recommendation") - no entity, no trigger/owner/status schema. Added a real, constrained `Recommendation.plan_link_category` field (5 DOCX values) instead of overloading the existing `provider_action_type`/`specialist_route` fields, which already carry different meanings. Set automatically for rule-triggered recommendations (`app/core/recommendation_rules.py`'s existing per-component table) and inferred from `assigned_provider_role` for manual assignments, plus a new explicit `AssignActionRequest.is_joint_coordination` flag for the one DOCX-named judgment call ("pain, limitation, failed OFT, recovery decline, or uploaded medical history affects training plan decisions") that can't be auto-inferred. "IDMT handoff," by contrast, turned out to be **real and specifically defined** in the DOCX's own data dictionary (`handoff_id, user_id, export_type, prepared_by, recipient_role, prepared_date, transmitted_date, acknowledgement_status`), with explicit prepared->transmitted->acknowledged lifecycle tracking and a "never auto-transmit raw medical records without an approved recipient role/purpose/audit log" requirement. Built a real `IdmtHandoff` model + service + 5 new `/admin/idmt-handoffs/*` routes, reusing the existing `PendingConfirmation`/`AdminConfirmationService` two-person-rule queue (a 4th `action_type`) rather than a parallel approval mechanism - the exact same real gate restricted `ReportExport`s already use. The handoff's content is always a real, structured summary (reconditioning phase/RTP-status/severity/days-out from the 2026-08-13 injury-taxonomy pass, `MedicalRecord` document-type counts, OFT status) - never raw file bytes or `storage_path`, reusing the existing `render_prose_pdf`/`render_csv` renderers with zero new rendering code. Live-verified: `plan_link_category` correctly resolved to `joint_coordination_item`/`scs_recommendation`/`specialist_routing_item` across 3 real manual-assignment scenarios; a real handoff went 202 pending -> approved (superadmin, a genuinely different account than the PT/IM requester) -> correctly invisible to IDMT pre-transmit -> transmitted -> visible to IDMT -> downloaded as a real `%PDF`-header PDF whose underlying content (directly inspected) exactly matched the real reconditioning/medical-record/OFT data on file -> acknowledged (real name attributed) -> a second acknowledge attempt correctly 400'd (not silently idempotent); the untouched `role_change`/`deactivation`/`export` confirmation types all still listed and serialized correctly alongside the new `idmt_handoff` type (no regression).

**2026-08-13: ROM measurement tracking + real reconditioning-event timeline (net-new) - closes out the gap-analysis list.** Last gap-analysis item. DOCX check found zero grounding for either piece: "ROM"/"range of motion" never appears (every raw hit is a substring inside "from"/"program"), and "timeline" in the DOCX always means compliance due-dates, never a chronological injury/recovery event view. User explicitly chose net-new, same category as the earlier severity/days-out decision. Design principle: a timeline only means something if it's real, so instead of a manually-authored entity, `ReconditioningEvent` is a real append-only log automatically written by `ReconditioningService.upsert_for_user` whenever a field *actually* changes value (`plan_created`/`phase_changed`/`clearance_changed`/`severity_changed`) and by the new `RomMeasurementService.add_measurement` (`rom_measurement_added`) - nothing backfilled or fabricated, same shape as the existing `MedicalRecordAccessEvent`. New `RomMeasurement` model tracks real per-movement degree measurements (`straight_leg_raise`/flexion vocabulary kept from the frontend mock's own filter values - a real vocabulary choice, not a fabricated data claim). 5 new routes under `/records/reconditioning-plan/...` (self + provider views for both timeline and measurements, plus the provider-only POST). **A real bug was found live-verifying this**: the new provider-view routes passed a route-supplied string `user_id` straight into a Beanie `== ` query against a `PydanticObjectId` field - the exact same class of silent-no-match bug already documented and fixed once this session in `CoverageService` (Beanie doesn't coerce a string to match an ObjectId field type). Fixed with the same `_as_object_id`-style coercion in both `ReconditioningService.get_timeline` and `RomMeasurementService.list_for_user`. Live verification also hit a second, real MongoDB Atlas SSL-handshake outage mid-pass (same documented pattern as earlier this session) - waited for real recovery rather than guessing, then completed the interrupted calls. Full live verification: a real `plan_created` event on first plan creation; a combined phase+severity change producing exactly 2 new events with real before/after detail strings, while a same-call unrelated-field-only change produced zero; a real ROM measurement matching exactly between provider and self views, appending exactly 1 `rom_measurement_added` event; a second same-movement measurement at a later date correctly grouped and ordered; the full 5-event real timeline sequence matched the exact real order of actions taken (verified via `created_at` timestamps, not insertion-order coincidence); existing `reconditioning-plan` response shape unchanged; a second, untouched test operator's timeline correctly stayed empty.

**2026-08-13 (later): `ascend-admin` re-analysis against `origin/new-feature-branch` - no code changes.** User pointed at a real GitHub branch with a large frontend rewrite (38 files, ~8,800 lines - every dashboard rebuilt) and asked for an updated gap analysis. Checked out the branch cleanly (working tree was clean, safe checkout) and re-read every dashboard page plus the new supporting files (`privacy/population-scope-badge.tsx`, `privacy/privacy-state-badge.tsx`, `lib/terminology.ts`, `store/users-store.ts`, etc.). Confirmed the frontend is still 100% local/mock (`NEXT_PUBLIC_API_BASE_URL` parsed but never referenced, no `fetch`/`axios` anywhere) - the rewrite's real content is a coherent privacy/governance UI layer (population-scope badges, a 7-state privacy vocabulary, cohort-k enforcement, field-level redaction/unredact) plus generic modal wiring so "New X" buttons mutate local state instead of just toasting. Cross-referenced the new concepts against the real backend and found most of it **already has real backend support built earlier the same day** - the admin Scope-matrix k-selector/driver-visibility toggles are exactly `RoleScopeConfig`/`GET/PATCH /roles/scope-config/{role}`; MP's redaction/unredact UI matches the real field-redaction pass; PT/IM's new ROM/Treatment-plan sub-tabs match today's ROM/timeline pass; SCS's Coverage-tab row shape matches `GET /roles/scope-matrix` field-for-field; Leadership's Briefing template sections match the real `BRIEFING_TEMPLATES`. A handful of genuinely new small gaps surfaced: a reason-*category* enum (Routine/Escalation/Follow-up/Audit/Other) the redaction reveal-field flow doesn't have yet, an admin-initiated password-reset endpoint that doesn't exist (only self-service forgot-password), a "restriction block gated by phase signoff" sub-concept on `ReconditioningPlan`, a distinct SCS→PT/IM sign-off audit record, and device-override/status tracking (no DOCX or existing backend anchor). Updated `docs/ASCEND_ADMIN_ENDPOINT_GAP_ANALYSIS.md` with a full delta table rather than rewriting the whole document - no backend code was touched this pass, analysis only.

**2026-08-13 (later still): reason-category enum for the reveal-field flow - first item off the delta list.** The `new-feature-branch` MP "Records" tab requires a reason *category* (Routine/Escalation/Follow-up/Audit/Other radio picker) alongside the existing free-text reason before a masked field can be unredacted - the real `POST /records/uploads/{id}/reveal-field` (built earlier today) only validated free text. Added `REASON_CATEGORIES` (in `app/schemas/medical_record.py`, the existing home for this file's other real enums like `DOCUMENT_TYPES`), a required, pattern-validated `reason_category` on `RevealFieldRequest`, and folded it into the existing `MedicalRecordAccessEvent.note` (`f"[{reason_category}] {field_name}: {reason}"`) rather than adding a new model field - real and auditable, not just a UI-only radio button with nothing recorded server-side. Live-verified: a valid category (`escalation`) correctly accepted and echoed back in the response; an invalid one (`urgent`, not in the real 5-value enum) correctly 422'd; the reveal remained one-time (re-masked on the very next fetch, same as before this change); the masked SCS viewer's own audit-log view still correctly shows the redacted placeholder for their own past reveal notes (not a regression - matches the existing masking-of-audit-notes behavior); PT/IM's unmasked view correctly showed the real `"[escalation] access_reason: ..."` note alongside the original ungated pre-existing note, confirming no regression to PT/IM's full access.

**2026-08-13 (later still): admin-initiated password reset - second delta item, with a deliberate security divergence from the mock.** The `new-feature-branch` frontend's `adminResetPassword` action shows a generated temp password directly to the admin in a modal - the real backend had no admin-triggered reset at all, only the self-service forgot-password flow. Before building, flagged a real security concern with the user directly: returning a plaintext password via an API response (or logging it) is a genuine OWASP-flagged exposure risk regardless of what the mock shows. User confirmed the secure path: the real temp password is generated (`secrets.token_urlsafe`), hashed onto `User.hashed_password` via the existing `get_password_hash` helper (`app/core/security.py`, the same one `AuthService.reset_password` already uses), and emailed via the real `EmailService.send()` (Resend, built earlier this session) - **never returned in the response, never written to the audit log**. If Resend isn't configured, the reset still genuinely happens but the response honestly reports `emailed: false` - the same "no fabricated success" pattern already established for `SmsService`/`PushService` this session, not invented new for this feature. New `AdminUserService.reset_password` matches the file's exact established per-method shape (`renew_access` was the closest template - fetch-404-mutate-save-audit-return); confirmed via the existing `AdminConfirmationService`/`GATED_DEACTIVATION_ROLES` reasoning that this action correctly stays **ungated** (no caseload-reassignment blast radius like deactivation, immediately self-correctable by the user). Live-verified: a real reset genuinely changed the password hash (the user's *old* password correctly 401'd on a subsequent login attempt); the response body and the real `AuditLog(event_type="admin_password_reset")` entry both contained no password value anywhere, only `emailed: false` (honestly reported, Resend unconfigured this session); `renew_access` (untouched in the same file) still worked identically, confirming no regression.

**2026-08-13 (later still): PT/IM duty restrictions with phase-gated signoff release - third delta item, net-new.** The `new-feature-branch` PT/IM injury deep-dive shows restriction cards like "Heavy lifting > 80% 1RM - Block remains in effect until Phase 3 signoff." A DOCX check found no real anchor: "restriction" only appears as a loose field/tag ("restriction flags"), "phase" as a progression concept doesn't appear in the DOCX at all, and no sign-off/approval workflow exists for either. User explicitly confirmed net-new. Kept the gate real, not decorative - "block remains until signoff" implies two genuinely checkable conditions, not an auto-release the instant a phase field changes: new `Restriction` model (`required_phase`, `status`) + `RestrictionService.release_restriction` requires **both** the operator's real current `ReconditioningPlan.phase` to have genuinely reached `required_phase` (checked via a new `PHASE_ORDER` derived from the existing `PHASES` tuple's own declared sequence, not an invented one) **and** an explicit provider signoff call - reaching the phase alone never auto-releases anything. Reused the exact real `ReconditioningEvent` timeline pattern from the earlier ROM/timeline pass (`restriction_added`/`restriction_released` added to the same real `EVENT_TYPES`). 4 new routes under `/records/reconditioning-plan/...` (self + provider list, provider-only add, provider-only release). Live-verified the full real gate end to end: a restriction added while the plan was still `phase: "active"` correctly showed `phase_requirement_met: false`; an early release attempt correctly 400'd with an honest message naming both the real current and required phase; advancing the real plan to `phase: "maintenance"` (the existing `PUT .../reconditioning-plan/{user_id}`) correctly flipped `phase_requirement_met` to `true` while `status` stayed `"active"` (no auto-release); the explicit release call then correctly succeeded with a real `released_at` and a `restriction_released` timeline event; existing ROM measurements from the earlier pass stayed intact, and a second, untouched test operator correctly had zero restrictions.

**2026-08-13 (later still): SCS<->PT/IM coordination sign-off state - closes out the delta list except device-overrides.** The `new-feature-branch` PT/IM "SCS" tab and SCS page both show a "Send to PT/IM" workflow with its own trackable state (`SentForSignOff[]`), distinct from a recommendation's ordinary status. `Recommendation.plan_link_category == "joint_coordination_item"` (built earlier today) already covers the real DOCX categorization, but nothing tracked the sign-off lifecycle itself. Kept it additive and small - 3 new fields on the existing `Recommendation` (`coordination_signoff_status`, `signed_off_by`, `signed_off_at`), 2 new real, audit-logged, guarded service methods (`send_for_signoff` - 400s on anything that isn't a `joint_coordination_item`; `sign_off` - 400s unless already `pending_signoff`, PT/IM-role-gated at the route matching the DOCX's real PT/IM-as-primary-reviewer framing used elsewhere this session). Converting `RecommendationService._serialize` from sync to async (to resolve `signed_off_by_name` for display) touched every existing call site in the file (9 sites) - mechanical, no behavior change to any of them. Live-verified: a real `joint_coordination_item` correctly started at `"not_required"`; `send_for_signoff` correctly succeeded on it and correctly 400'd on an ordinary `scs_recommendation`-category recommendation; `sign_off` correctly 400'd on an item never sent, then correctly succeeded on the real pending one with a real `signed_off_by_name`/`signed_off_at`; both new real `AuditLog` event types (`coordination_sent_for_signoff`, `coordination_signed_off`) confirmed present; an ordinary recommendation's existing `complete()` flow (untouched logic, only the `_serialize` signature changed under it) still worked identically, correctly showing `coordination_signoff_status: "not_required"` throughout - no regression from the sync-to-async conversion.

**2026-08-13 (later still): SCS provider-facing OFT read + real L0-L5 driver flag.** First item from the original (pre-`new-feature-branch`) gap-analysis pass, revisited after the delta list closed out. A DOCX check on "driver flag" found the frontend mock's per-component chips ("sleep flag", "OFT flag") have no anchor, but the real L0-L5 escalation framework (`app/core/routing_levels.py`, DOCX Table 20) does, and is already partially computed today via `Recommendation.route_level` (the documented moderate->L2/high->L4 approximation). User confirmed: build both gaps, using the real L0-L5 value rather than fabricating per-component tags. New `GET /oft/{user_id}` (`app/modules/oft/routes.py`) reuses the exact real `OFTService.get_status_for_user()` and the file's own existing `_get_target_user` helper - zero new service logic. `GET /dashboard/scs` gained one new field, `driver_flag`, which is simply `active_recommendation.route_level` - `get_scs_dashboard` already fetched `active_recommendation` per operator for the existing `active_risk_flag` field, so this is a one-line passthrough of already-tested data, not new computation. Live-verified: the provider-view OFT endpoint returned byte-identical output to the same real operator's own `GET /oft/me`; an unauthorized role (Airman) correctly 403'd on the provider route; a real operator with no active recommendation correctly showed `driver_flag: null` on the dashboard (matching `active_risk_flag: null` on the same row); a real pre-existing `Recommendation(route_level="L4")` was independently confirmed in the DB, verifying the field's correctness by direct data inspection (that specific operator wasn't currently SCS-assigned to appear on a live dashboard row, so the non-null case was verified via direct code/data correctness rather than an end-to-end HTTP round trip - documented honestly, not overclaimed).

**2026-08-13 (later still): correction, no code change - Leadership "feedback sessions" was already built.** User picked this as the next gap-analysis item; a DOCX check confirmed `Assessment.feedback_session_status` (offered/completed/declined/pending) is real and DOCX-sourced (line 109), but the codebase check that followed found the gap-analysis doc's verdict was simply wrong - `ReportsService.get_feedback_session_summary()` already exists and has been wired into `GET /dashboard/leadership/aggregate` as `feedback_sessions` since the original Leadership Aggregate pass (2026-08-10), already live-verified then (a real completed `Assessment` correctly counted 1/1 in-quarter). The original gap-analysis research pass simply missed this method when it searched the codebase. Corrected the doc rather than building anything - the Leadership section's gap line now reads "none," matching reality. No DOCX-defined completion-rate target exists for feedback sessions specifically (unlike the real 50%/90% assessment-completion targets), so the frontend's exact "38/125" framing has no DOCX-sourced denominator, but the real underlying tracking and aggregation are genuine and already shipped.

**2026-08-13 (later still): SCS real workload-by-flight (reconditioning caseload only) - honest partial build.** The frontend's SCS Coverage "Workload by flight" table has 5 columns (PT/OFT lanes, Rehab, Reconditioning, Capacity, Load%); assessed each honestly before building anything: "lanes" has no real taxonomy anywhere, "Capacity" has no real number tracked for any flight/provider, and the "SCS availability" weekly heatmap would require an entirely new provider-facing availability-submission mechanism that doesn't exist (a much bigger feature, not an aggregation). Only "Rehab"/"Reconditioning" is honestly buildable - a real per-flight count of operators with an active `ReconditioningPlan`, using the exact same k-gated flight-grouping shape already proven in `LeadershipAggregateService.get_flight_comparison()`. New `CoverageService.get_reconditioning_load_by_flight()` + `GET /admin/coverage/reconditioning-load-by-flight`. User explicitly confirmed building only this real slice, skipping the rest rather than fabricating lane/capacity/availability data. Live-verified: a real 5-member flight ("Alpha Flight Test") correctly included (real `cohort_k=5` met) while a real 3-member flight ("Bravo Flight Test") stayed correctly excluded; direct DB inspection confirmed the flight's real members honestly had zero active reconditioning plans before the test, matching the endpoint's `active_reconditioning_count: 0`; setting one real member's `ReconditioningPlan.phase` to `"active"` correctly moved the count to `1` and `load_pct` to the exactly-correct `20.0` (1/5).

**2026-08-13 (later still): shared "Specialist notes" (MP + Chaplain + Nutritionist) - resolves 3 previously-separate gaps at once, real DOCX Section 17 grounding.** MP's "SOAP note editor," PC's "pastoral note editor," and Nutritionist's missing note-taking had all been separately assessed and correctly skipped earlier this session as DOCX-contradicted ("the app should not become a medical record, behavioral health record, chaplain record... or official clinical documentation"). A closer re-read of DOCX Section 17 ("Specialist Notes and Boundaries") found a real, narrower, generic concept distinct from that rejected one: *"If specialist notes are added, keep them simple and role-specific: date, specialist type, user concern, action assigned, follow-up needed, and status... Specialist notes should support coaching continuity, not formal clinical or privileged documentation."* This is explicitly lightweight, explicitly generic across every specialist pathway, and explicitly not the clinical/pastoral documentation already correctly rejected - user confirmed building this shared, real concept once, resolving all 3 role gaps at the same time. New `SpecialistNote` model (`user_id`, `specialist_id`, `specialist_type`, `note_date`, `user_concern`, `action_assigned`, `follow_up_needed`, `status: "open"/"closed"`) fields matching the DOCX list verbatim, registered like every other model this session. `SpecialistNoteService.create/list_for_user/update_status` - `note_date`/`specialist_type`/`specialist_id` always set server-side from the real caller, never client-supplied (same pattern as `Recommendation.assigned_by`). **Visibility principle (not DOCX-specified, a real design choice matching this codebase's established minimum-necessary pattern)**: a note is visible only to the specialist type that wrote it plus Admin - not shared across MP/Chaplain/Nutritionist, matching the same pathway-siloed access already enforced on medical records (`VIEW_ALLOWED_ROLES`) and dashboards (`visible_components`); "coaching continuity" in the DOCX means continuity within one specialist's own relationship with the operator, not cross-specialist sharing. Proactively applied the now-3-times-found string-vs-`PydanticObjectId` fix in `list_for_user` before it could bite a 4th time. 3 new routes under `/specialist-notes/{user_id}` (create/list) and `/specialist-notes/{note_id}/status` (update), gated to `ADMIN_ROLES + SPECIALIST_ROLES`. Live-verified end to end with real test accounts on a shared real operator (`e2e-notes-airman-*`): a real Chaplain account created a note (201, `specialist_type: "Chaplain"`, `status: "open"`); a real Mental Performance account independently created a separate note on the same operator; the Chaplain's own `GET` correctly returned only its own note (pathway-siloed, `['Chaplain']`) while the Admin's same call correctly returned both (`['Mental Performance', 'Chaplain']`); the MP account successfully closed its own note (`PATCH .../status` -> `200`, `"closed"`) but correctly got a `403` (`"Only the authoring specialist or an Admin may update this note."`) attempting to close the Chaplain's note; real `AuditLog` query for `event_type=specialist_note_created` confirmed exactly 2 entries, one per specialist, both `outcome_status: "success"`.

**2026-08-19: full mobile (Airman) + full Admin & Provider Dashboard API sweep - built a combined Postman collection, wrote a mobile-app integration guide, then live-tested essentially every endpoint in both surfaces.** Built `docs/postman/Ascend_API.postman_collection.json` (200 requests) with two independent top-level folders - "App (Mobile - Airman)" and "Admin & Provider Dashboards" - each with its own `*_access_token`/`*_refresh_token` collection variables so two different integrators can work from the same file without clobbering each other's session; wrote `docs/MOBILE_APP_INTEGRATION.md` (base URL/auth/response-envelope/error-shape reference, a section per module, a "known gotchas" section, and a copy-paste AI prompt for whoever integrates the mobile app). Then ran two full real-HTTP sweeps against a live server:
- **Mobile (Airman) surface**: ~90 real calls across every module (auth, onboarding, check-ins, dashboards, AI insights, recommendations, support requests, messaging incl. group threads, notifications, OFT, records incl. real file upload/download with byte-for-byte match, workouts) plus the full real-code email-verify and password-reset flows and messaging between two roster-linked accounts. Zero real bugs - every apparent failure was traced to the test script itself (missing a required onboarding follow-up answer, wrong workout payload shape, messaging attempted before establishing a real `TeamAssignment`, JSON-parsing a raw file download).
- **Admin & Provider Dashboards surface**: ~110 real calls, one real account per role (2x Admin, SCS, PT/IM, MP, Chaplain, Nutritionist, Leadership, IDMT, Airman) across Roles/RBAC, Users & Access, Org Units, Audit Log, the 2nd-reviewer Confirmations queue (including the self-approve guard correctly 403ing and a different-admin approve correctly succeeding), Credentials/Equipment/Utilization/Coverage, Scoring/Threshold config, Reports & Exports (including the restricted-report 202-pending-approval flow), Scheduled Exports, System, IDMT Handoffs (full prepare-approve-transmit-acknowledge-download lifecycle), all 5 role dashboards, and every Provider action (OFT, Recommendations incl. the SCS<->PT/IM signoff flow, Records incl. ROM/restrictions with the real phase-gate correctly 400ing, Specialist Notes, Support Requests). Found and fixed a real, systemic bug: **9 routes in `app/modules/admin/routes.py` never set an explicit `status_code`**, so FastAPI's default 200 leaked through where every equivalent create/pending-approval route elsewhere in the codebase correctly returns 201 or 202 - fixed all 9 (`org-units`, `credentials`, `equipment-gaps`, `utilization-events`, `coverage-logs`, `scoring-config`, `recommendation-thresholds`, `scheduled-exports` -> 201; `users/{id}/deactivate` -> 202, matching the identical pending-approval pattern `idmt-handoffs`/restricted-report-export already use correctly), restarted the server, and live-reconfirmed all 9. Also live-verified the full Leadership Briefing lifecycle end to end (create is a genuinely slow endpoint - 30-60s+ of real sequential Claude calls, one per section, confirmed not a bug by checking the server log and re-fetching; then get/outline-update/send/PDF, real PDF magic bytes checked), and resolved one apparent bug as correct-by-design: `GET`/`complete`/`steps/{i}/complete` on `/recommendations/{id}` are scoped to `record.user_id == caller.id` - only the Airman the recommendation belongs to can call them, not the assigning provider (404 for a provider token is intentional, not a bug) - corrected the Postman collection's mislabeling of those 3 as "Provider" actions to spell this out.

**2026-08-22: fixed a real, codebase-wide bug - invalid path-param IDs crashed with a raw 500 instead of a clean 400.** User reported real Postman traffic showing 500s on `/admin/users/{id}/*` and `/admin/deactivation-requests/{id}/*` (caused by leaving the collection's `REPLACE_WITH_...` placeholders in the URL). Reproduced: `Beanie`'s `Model.get(id)` raises a bare `pydantic.ValidationError` (`"Id must be of type PydanticObjectId"`) when the id isn't a valid ObjectId, and nothing anywhere caught it - FastAPI's default handling only catches its own `RequestValidationError` (body/query parsing), not a raw `pydantic.ValidationError` raised later inside application code, so the client got a full unhandled stack trace as a 500. Every route in the codebase with a `{something_id}` path param passed straight to `.get()` had this same latent bug, not just the 8 that happened to get hit. Fixed with one global FastAPI exception handler (`app/main.py`, `@app.exception_handler(ValidationError)`) returning a clean `400 {"detail": "Invalid ID format in the request."}` - no per-route patching needed. Live-verified: all 8 originally-reported 500s now correctly 400; a normal malformed request body still correctly 422s (confirms no interference with FastAPI's own validation-error handling, since that's a distinct `RequestValidationError` subclass raised before user code runs); a real, valid ID on the same route (`renew-access`) still works identically (200), no regression.

**2026-08-23: Admin-provisioned account creation ("Add person") - a real, DOCX-grounded gap the whole session had missed.** User pointed at the `ascend-admin` "Roles & RBAC" screen's "Add person" modal (Name/Email/Role/Status/Unit/auto-generated Initial Password with a regenerate button and "Share this with the person directly - it isn't emailed") and asked whether the backend existed. It did not: `admin/routes.py` had `GET /admin/users` plus six actions on an *existing* user (`change_role`, `assign_unit`, `deactivate`, `renew_access`, `reset_password`, `assign_provider`), but no way to create a brand-new account at all - the only account-creation path in the entire backend was self-registration (`POST /auth/register`, where the user picks their own password). Checked the DOCX before building and found this is a genuine requirement, not a Figma-only invention - Section 2A Step 1 ("Account Provisioning/Activation"): *"DWS Admin, Government-approved roster import, or authorized account setup creates or provisions the account. If self-activation is used, it should be minimal and should not feel like a separate account-creation workflow."* Built `POST /admin/users` + `AdminUserService.create_user` + `AdminCreateUserRequest`. **Deliberate divergence from `reset_password`'s email-only rule, confirmed with the user first**: the initial password is returned once in the response (matching the frontend's visible/copyable/regeneratable field) rather than emailed - that earlier security decision was scoped to *resetting* an existing account's password, and the DOCX specifies no delivery mechanism for initial provisioning; it is still never written to the audit log. Reuses the existing `validate_password_strength` (`app/schemas/auth.py`) for an admin-supplied password and `secrets.token_urlsafe(9)` when omitted, and mirrors `change_role`'s Superadmin-only gate for admin-level target roles - but deliberately skips that method's second-reviewer confirmation-queue detour, since a brand-new account has no `snapshot_before` state to revert to and a mistaken creation is corrected by deactivating it (already real) rather than reverting it. Admin-provisioned accounts are created `is_verified: True` (DOCX's "authorized account setup" means already vetted, unlike self-registration which still requires the email-code flow). Live-verified all 7 planned scenarios: Superadmin created an Airman with a generated password and that exact password logged in immediately (200); an explicitly-supplied password round-tripped and logged in; a duplicate email correctly 400'd; a plain `DWS Admin` correctly 403'd trying to create a `DWS Superadmin` but correctly succeeded creating an `SCS`; an unsupported role (`Wizard`) correctly 400'd with the real allowed-roles list; a weak `initial_password` (`alllowercase`) correctly 422'd via the reused validator; and the real `AuditLog(event_type="user_provisioned")` query returned 3 entries with correct actor roles and **empty `metadata_payload`** - programmatically confirmed neither the generated nor the explicit plaintext password appears anywhere in the audit-log payload.

**2026-08-23 (later): self-service password change - closing a gap that two already-shipped features silently depended on.** User pointed at the `ascend-admin` `/dashboard/profile` "Change password" card (Current password / New password / Confirm new password) and asked whether it existed. It did not. The backend had exactly two password-mutation paths: the emailed-code recovery flow (`/auth/forgot-password` -> `/auth/verify-reset-code` -> `/auth/reset-password`, for a locked-out user) and `POST /admin/users/{id}/reset-password` (an Admin resetting someone else's). Nothing let a signed-in user change their own password. **Checked the DOCX first and found zero mentions of the word "password" anywhere in the entire document** - so unlike the "Add person" gap closed earlier today, this one has no DOCX anchor at all. Built it anyway, because it is a real functional gap rather than a Figma-only one, on two independent grounds found in this codebase's own already-shipped behavior: (1) the admin password-reset email written earlier this session literally instructs the recipient to *"Sign in and change it as soon as possible"* (`admin_user_service.py:265`) - an instruction that was impossible to follow; and (2) `POST /admin/users`, built hours earlier today, hands a brand-new account an admin-chosen initial password, which without this endpoint would leave the provisioning admin permanently in possession of that user's working credentials. New `ChangePasswordRequest` (`app/schemas/auth.py`, reusing the existing `validate_password_strength` validator that `RegisterRequest`/`ResetPasswordRequest` already use), `AuthService.change_password`, and `POST /users/change-password` (mounted in the users module alongside the other profile-screen actions, though the logic lives in `AuthService` to keep every password mutation in one service next to `reset_password`). Reuses the existing `verify_password` helper - the same one `_authenticate_user` uses at login - to prove the current password, which is the meaningful difference from `reset_password`: there the user is locked out and proves identity with an emailed code, here they are already authenticated and prove intent with the password they hold. Also rejects reusing the current password as the new one. Neither the old nor the new password is written to the audit log (`password_changed`, empty `metadata_payload`). Live-verified the full realistic chain end to end, not just the endpoint in isolation: Superadmin provisioned a real `SCS` account -> the new user logged in with the admin-generated initial password (200) -> a wrong `current_password` correctly 400'd, a mismatched `confirm_password` correctly 400'd, reusing the current password as the new one correctly 400'd, a weak new password correctly 422'd via the reused validator, and an unauthenticated call correctly 401'd -> the valid change returned 200 -> logging in with the NEW password succeeded (200) while the OLD admin-issued password correctly failed (401), proving the admin no longer holds working credentials -> the real `AuditLog(event_type="password_changed")` entry was present with the correct `actor_role` and programmatically confirmed to contain neither plaintext password.

**2026-08-23 (later still): "Last edit" column on the Roles & RBAC People table - derived from the audit log, deliberately not from `User.updated_at`.** User pointed at the People table's `LAST EDIT` column and asked whether it existed. Checked `GET /admin/users` live: every other column mapped to a real returned field (`full_name`/`email`/`role`/`unit_id`/`is_active`), but the response carried nothing for last-edit at all. **The obvious one-line fix would have been wrong.** `User.updated_at` exists and is genuinely maintained, but grepping every writer of it showed it is bumped by *any* save of the document - `AuthService.login_user` bumps it on an ordinary login, and so do `checkin_service` and `onboarding_service` on the user's own daily activity. A "Last edit" column on an admin role-management screen built on that field would move every time the person merely used the app, which is not what the column means. Derived it from the audit log instead, which records exactly the account mutations, is keyed by `target_entity_id`, already has an index on `(target_entity_type, target_entity_id)`, and matches the Roles & RBAC screen's own policy banner ("Every modification to standard role assignments must write to the control plane log"). New `ACCOUNT_EDIT_EVENT_TYPES` (8 real event types: `user_provisioned`, `role_changed`, `role_change_requested`, `unit_assigned`, `access_renewed`, `admin_password_reset`, `password_changed`, `admin_deactivation_requested`) explicitly **excludes** `login_success`/`login_failed`, which target a user but are activity rather than an edit. New `_get_last_edit_map()` does one grouped query and folds to `{user_id: latest}` in Python rather than one query per user (N+1), using the dict-style query form because this codebase's Beanie version can't combine `(A) & (B)` conditions. `_serialize` also gained a real `created_at` so the frontend has an honest fallback for an account that has genuinely never been edited - `last_edit_at` is `null` there rather than being backfilled with something invented. Live-verified with a proof that actually proves the design decision: first attempt was a **weak test** (picked an account whose `last_edit_at` was already `null`, so "unchanged after login" was trivially true and demonstrated nothing) - caught and redone against `dash-admin@ascend.mil`, which carries a real `2026-08-22T12:37:32` timestamp: 3 consecutive logins left it **completely unchanged** (the exact behaviour `User.updated_at` would have failed), then a single real edit (`renew-access`) correctly moved it forward to `2026-08-23T11:49:31`. Also confirmed across the full 113-account list that 33 accounts carry a real derived timestamp and 80 correctly carry `null` with a real `created_at` alongside.

**2026-08-23 (later still): "Accounts & onboarding" summary card on the Roles & RBAC screen.** User pointed at the card's 6 metrics (Account status, Onboarding, Access expiration, Assigned providers, Effective permissions, Purpose consent) and asked whether it existed - it did not; no route or service method anywhere matched its shape. DOCX has zero grounding for this card (checked "annual review", "awaiting", "in flight" - 0 hits), so every metric is real data serving a Figma-only card, same category as the Role catalog/RBAC matrix built earlier this session. New `RoleAdminService.get_accounts_and_onboarding_summary()` + `GET /roles/accounts-onboarding-summary`. Five of six metrics were straightforward real aggregates: `account_status` (`User.is_active`/`access_expires_at`), `assigned_providers` (the real `always_available` pathways from `app/core/support_pathways.py` - exactly `SCS` + `PT/IM`, matching the mock), `effective_permissions` (a reference note pointing at the already-real `GET /roles/matrix`, no new computation), and `purpose_consent` (reused the already-existing `_purpose_consent_summary()` verbatim). **The access-expiration panel surfaced a real contradiction, resolved with the user first**: the mock's text reads "All scopes renew automatically on annual review", but `app/models/user.py`'s own doc comment on `access_expires_at` already states it is "not auto-renewed by any background process" - renewal is the existing manual `POST /admin/users/{id}/renew-access` action. Asked via `AskUserQuestion`; user chose to keep the panel honest ("Real behavior bole dei - 'requires manual renewal'") rather than match the mock's false claim or drop the panel - the response carries the real counts plus an explicit `renewal_note`. **The onboarding metric caught a real bug during its own live verification, not before it.** The first implementation counted `onboarding_status != "completed"` across every account and returned `116` of `117` - because `ROLE_PERMISSIONS` grants `complete_onboarding` exclusively to `Airman`, and no other role's routes ever touch the onboarding flow, so a staff/admin account's `onboarding_status` sits at its unreachable model default ("incomplete") forever, not because it's stuck mid-flow. Rescoped `in_flight_count` to `role == "Airman"` only; a direct per-role/per-status DB breakdown confirmed the corrected number exactly (62 incomplete + 3 in_progress + 1 completed = 65), and the endpoint now returns `65`. `awaiting_role_confirmation_count` (a real `PendingConfirmation(action_type="role_change", status="pending")` count) was cross-checked against the existing `GET /admin/confirmations?status_filter=pending` endpoint and matched exactly (3 of 5 total pending). Live-verified RBAC gating too: a PT/IM (non-admin) call correctly 403'd.

**2026-08-23 (later still, again): fixed a real bug in the Contract Question Registry - a whole real question bank was missing from the count, and I had wrongly concluded the design's "D1-D6" section was fabricated.** User pointed at the live `ascend-admin` Roles page's Contract Question Registry table (O1-O20 / D1-D6 / W1-W10 / M1-M10 = 46 questions, with DRIVER/DIRECTION/ROUTING/VALIDATION columns) and asked whether it was real. Live-checked `GET /admin/question-registry` and found it returning `total_questions: 40`, with no `daily` section at all - `build_question_registry()` (`app/core/question_registry.py`) only ever summed onboarding + weekly + monthly. Its own docstring, written when the endpoint was first built, claimed the design's "D1-D6" section referred to a nonexistent 6th "Recovery"/"Compliance" driver. **That docstring was wrong.** `D1`-`D6` are the real `label` values of `DAILY_CHECKIN_QUESTION_BANK` (`app/core/checkin_question_bank.py`) - a fully live bank already wired into `CheckinService` for real daily check-ins - that the registry simply never included. With it included, the real total is 20 + 6 + 10 + 10 = **46**, exactly matching the design's own framing, which was real all along. Also re-checked the DOCX (Table 22, the data dictionary) and found two real, previously-missed entities: "Question Answer Option Set" (`reverse_score_status` among its fields - the real source for a per-question `direction`) and "Question Bank Version" (`version_id, effective_date, retired_date, approved_by, ...question_set_id, change_reason` - "controls updates to question wording, answer choices, scoring, and driver mapping"). Fixed `build_question_registry()` to include the daily bank and added a real, derived `direction` field ("Higher = better" / "Lower = better") to every entry - derived the exact same way `CheckinService._score_answer` derives it live (`question.get("reverse_scored", False)`), not copied from the design; every one of the 46 real questions is currently `False`, so "Higher = better" is genuinely true for all of them today. Live-verified: `total_questions` now returns `46`, the `daily` section has exactly 6 entries labeled `D1`-`D6` with correct components/routing/direction. **`validation_status` is still deliberately not included** - DOCX's only related entity (`Question Bank Version.approved_by`) is a whole-bank approval/version record, not a per-question valid/invalid flag, and has no model or endpoint anywhere in this codebase - inventing a per-row "Valid" badge would repeat the same mistake this fix just corrected. Flagged to the user as a real, DOCX-grounded, unbuilt feature if they want it.

**2026-08-23 (later still, once more): built Pathway Approval - the Scope page turned out to be the same hardcoded-frontend story, all the way down, but this time the one real gap was accepted as new scope anyway.** User kept walking the same `ascend-admin` Scope page: "Coverage · active selection"/"Scope inheritance" (checked `page.tsx` lines ~1596-1723 - `units`/`cohortSizes` are hardcoded arrays, driver visibility is local Zustand state, "Scope inheritance" is 3 completely static `<p>` tags with no props/state), then "Admin coverage map" (lines ~1753-1764 - a hardcoded `CoverageRow[]` array whose values contradict the real `GET /roles/scope-matrix` output: the mock shows SCS/PT-IM as `wing: "gated"` while the real `aggregate_wing` logic only grants that column to Admin/Superadmin/Leadership at all - SCS/PT-IM get `"none"`, not a restricted-but-existing "gated" state), then "Conditional pathway matrix" (lines 1814-1817 - `app`/`en` are the literal identical string `"Approved · 14 Jan 2026"` / `"Enabled · 01 Feb 2026"` copy-pasted across all 3 pathway rows, no variation at all). Live-verified all 4 real backend endpoints behind these screens work correctly (`GET/PATCH /roles/scope-config`, `GET /roles/scope-resolve`, `GET /roles/scope-matrix`, `GET /roles/pathway-matrix`) - confirming every mismatch is a frontend wiring gap, not a backend one. But APPROVAL/ENABLEMENT/ACCESS DATES specifically had **zero grounding anywhere** - re-checked DOCX ("pathway approval", "enablement date", "access window", "caseload window" - all 0 hits) - unlike Question Bank Version, which was DOCX-required. Asked directly whether to build a real version anyway with no DOCX anchor; user confirmed yes. New `PathwayApproval` model (one record per optional pathway - Nutritionist/Mental Performance/Chaplain only, same "current record per key" style as `RoleScopeConfig` rather than a versioned history, since a pathway is approved once then separately enabled once), `PathwayApprovalService` (`approve`/`enable`/`get_status`/`list_all`), and 2 routes: `POST /roles/pathway-approvals/{key}/approve`, `POST .../enable`. Deliberately no two-person-rule gate (`PendingConfirmation`'s pattern) - that's reserved for destructive actions elsewhere, and approving a pathway isn't in that category. `enable` 400s if the pathway hasn't been approved yet; both actions 400 for `SCS`/`PT/IM` (`always_available`, never gated on approval) and 404 for an unknown pathway key. Merged the real `approval` object into each row of `GET /roles/pathway-matrix` at the route layer (kept `RoleAdminService.get_pathway_matrix` itself unchanged, same "merge at the route, don't couple the services" pattern used for `QuestionBankVersion`/`current_version`). Both mutations audit-log (`pathway_approved`/`pathway_enabled`). Live-verified the full lifecycle: `pathway-matrix` showed `status: "pending"` for all 3 pathways with nothing recorded; enabling before approving correctly 400'd; approving `SCS` correctly 400'd; approving a bogus pathway correctly 404'd; approved then enabled `Nutritionist` with a real `access_policy` - `pathway-matrix` immediately reflected `status: "enabled"` for `Nutritionist` while `Mental Performance`/`Chaplain` correctly stayed `"pending"`; a non-admin (PT/IM) correctly 403'd; both audit event types confirmed present.

**2026-08-23 (later still, yet again): built Question Bank Version, closing the one real gap the registry investigation surfaced.** After the registry total-count fix, the user pushed further on a live screenshot's `DRIVERS · D1-D6` section (DIRECTION and VALIDATION columns circled) - direct inspection of `ascend-admin/src/app/dashboard/admin/page.tsx` (lines ~800-870) showed the entire Contract Question Registry table is a hardcoded static array in the React component, never calling `GET /admin/question-registry` at all - it invents categories with no backing anywhere in this codebase ("Recovery" driver with a combined "SCS + PT/IM" routing, "Compliance", "Workout adherence") and a false "W5 REVERSE-SCORED" claim (checked `weekly_checkin_question_bank.py` and `CheckinService` directly - W5 stores no `reverse_scored` field and is scored identically to every other weekly question via the same `False` default). That's a frontend wiring gap, out of this session's established scope ("frontend integration is the frontend dev's job"). But `VALIDATION` specifically had a real, DOCX-grounded gap underneath the frontend's fabrication: Table 22's "Question Bank Version" entity (`version_id, effective_date, retired_date, approved_by, onboarding_question_set_id, daily_question_set_id, weekly_question_set_id, monthly_question_set_id, change_reason` - "controls updates to question wording, answer choices, scoring, and driver mapping without breaking scoring history") had no model or endpoint anywhere. Asked the user directly whether to build it; confirmed yes. New `QuestionBankVersion` model (mirrors `ScoringConfig`'s versioned/effective-dated pattern, plus a real `retired_date` gate that entity doesn't have), `QuestionBankVersionService` (`create_version`/`retire_version`/`list_versions`/`get_active_version_summary`), and 3 routes: `POST /admin/question-bank-versions`, `GET /admin/question-bank-versions`, `POST /admin/question-bank-versions/{id}/retire`. `approved_by` is set server-side from the real caller, never client-supplied. Retiring is a deliberate, separate action rather than an implicit side effect of recording a newer version, matching the DOCX phrase "without breaking scoring history" - old versions are kept, not replaced. Both mutations audit-log (`question_bank_version_created`/`question_bank_version_retired`). Wired a real `current_version` field into `GET /admin/question-registry` (the active version, or `null` if none has ever been recorded - never fabricated). Live-verified the full lifecycle: registry showed `current_version: null` before any version existed; created a real version (`is_active: true`); registry immediately reflected it; retired it (`is_active: false`, `retired_date` set); retiring again correctly 400'd ("already retired"); registry correctly returned to `current_version: null`; both audit events present; a non-admin (PT/IM) correctly 403'd on both create and list; a bogus version id correctly 404'd.

**2026-08-23 (later still, one more time): fixed a real bug in `AuditLogService.get_stats` - its own docstring promised a "destructive-action snapshot" the code never actually returned.** User pointed at the Audit log screen's 4 quick-metric cards (24h count + % vs 7d avg, 7d count + "log only" %, Record accesses + "with reason", Destructive actions + "pending review"). Checked `ascend-admin/page.tsx` first (lines ~1955-1982) - all 4 cards are hardcoded static JSX (`"2,184"`, `"+12% vs 7d avg"`, `"7"`, `"3 pending review"`), same pattern as every other screen checked today. But `GET /admin/audit-log/stats` (`AuditLogService.get_stats`) already existed and already covered the first two cards for real (`count_24h`, `count_7d`, `percent_vs_7d_avg` - live-verified: real values `95`/`173`/`284.4`, correctly computed, just not wired to the frontend). The 4th card had no backing at all, and the method's own docstring was stale: *"Real 24h/7d totals + destructive-action/record-access snapshot for the Overview stat cards"* - but the actual return dict only ever had `record_access_count_24h`, never a destructive-action field, meaning this was documented as done and never finished. Fixed it: added `destructive_action_total_count`/`destructive_action_pending_review_count`, sourced from `PendingConfirmation` - not an invented definition, `PendingConfirmation`'s own module docstring literally calls itself *"Second-reviewer confirmation queue for destructive admin actions"*, so counting every action type it tracks (`role_change`/`deactivation`/`export`/`idmt_handoff`) rather than an arbitrarily narrowed subset is the definition the model's own name already settles. Live-verified: `GET /admin/audit-log/stats` now returns `destructive_action_total_count: 19`, `destructive_action_pending_review_count: 5` - cross-checked by hand against a direct `GET /admin/confirmations?status_filter=all` breakdown by status (19 total: 12 approved + 5 pending + 2 rejected), exact match. Non-admin (PT/IM) correctly `403`'d.

**2026-08-23 (later still, yet again once more): built real `recipient_role`/`sensitivity_level` on `ScheduledExport`, closing the one genuine gap under the Exports "Schedule export wizard".** User asked whether the wizard's `SCOPE`/`RECIPIENTS` fields had real backing. Checked `ascend-admin/page.tsx` first: `schedules` is a plain `useState([...4 hardcoded rows...])`, `handleSubmitSchedule` never calls any API (confirmed zero `fetch`/`axios` calls anywhere in the entire file) - the screenshot's Lorem-Ipsum-looking rows ("Odit cillum similiqu", "Aut quod enim et pro") aren't in the source anywhere either, meaning someone previously typed placeholder text into this purely-local form and it never left that browser tab. Checked the real `ScheduledExportCreate` schema next: `name`/`report_type`/`export_format`/`cadence` only - no `scope`, no `recipients`. But `RECIPIENTS` pointed at something real and DOCX-required that had simply never been wired to this newer model: DOCX Table 22's "Report" data-dictionary row already lists `recipient_role` as a required field, and `ReportExport` (the on-demand export log) already has it - just always defaulted to the generating admin's own role, never a real admin-chosen value. `SCOPE` was different: rather than add a second, driftable free-text copy of the same gating concept, reused the exact real mechanism already governing on-demand exports (`REPORT_SENSITIVITY` -> `sensitivity_level`, aggregate/controlled/restricted) - computed once at schedule-creation time from `report_type`, not client-supplied. Added `recipient_role`/`sensitivity_level` to `ScheduledExport`, made `recipient_role` required on `ScheduledExportCreate`, and threaded it through `run_due_schedules()` into both real execution paths - the plain `export_report()` call and the restricted-sensitivity `request_export()`/`PendingConfirmation` path - both now accept an optional `recipient_role` override (defaulting to the old admin-role behavior for the still-unchanged on-demand call sites, so nothing regressed there). Live-verified end to end, not just the create endpoint: missing `recipient_role` correctly `422`'d; an aggregate `report_type` (`wing_weekly_ops`) correctly derived `sensitivity_level: "aggregate"`; a restricted one (`injury`) correctly derived `"restricted"`; backdated each schedule's `next_run_at` and ran the real scheduler function directly - the resulting real `ReportExport` row carried the schedule's `recipient_role` through on **both** the plain-export path and the restricted/`PendingConfirmation` path, not just at creation time. Confirmed no regression on the still-unchanged on-demand export route (`GET /admin/reports/{report_type}/export`, still `200`) and correct RBAC (`403` for a non-admin).

**2026-08-23 (later still, yet once more again): built real `scope_summary` on `PendingConfirmation`, and confirmed a prior real-vs-fabricated decision was already correct.** User pointed at the Exports page's "Review Pending Request" modal (ACTION/TARGET/SCOPE/IMPACT/RISK CODE) and asked to check it. Checked `ascend-admin`'s `admin-store.ts:107-111` first: `pendingConfirmations` is a hardcoded seed array (`{ id: "conf-1", action: "Export", target: "PT/IM audit (Q3)", consequence: "14 records - restricted", scope: "PT/IM caseload", risk: "L4" }`), and "Approve & Write Log" only calls `adminStore.removeConfirmation(id)` - a local array filter, never the real `POST /admin/confirmations/{id}/approve` - so despite the button's name nothing is actually written to any log. `ACTION`/`TARGET`/`IMPACT` already had close real analogs (`action_type`/`target_summary`/`consequence_summary` - the real format, `"{row_count} records - {sensitivity_level}"`, nearly matches the mock's IMPACT text verbatim). `RISK CODE: "L4"` turned out to be a **prior, already-documented, deliberate divergence** - `report_export_service.py`'s own comment explains real export risk tiers are intentionally never `L`-prefixed (`compute_risk_tier` returns `low`/`elevated`/`high`) specifically because this system's real `L0-L5` already means something else entirely (clinical routing-escalation levels, `app/core/routing_levels.py`) - reusing the mock's `L4` label would have created a genuinely dangerous naming collision in a military health context, so this was correctly left unmatched, not missed. `SCOPE` ("PT/IM caseload") had no real field anywhere. Built it: new `PendingConfirmation.scope_summary`, computed once at request time (same pattern as `target_summary`/`consequence_summary`, not derived live) from data already tracked at each of the 4 real creation sites - `role_change`/`deactivation` use the affected user's real `role`+`unit_id` (`"SCS - no unit"`), `idmt_handoff` uses its real `content_category`+target `role` (`"reconditioning - Airman"`), `export` uses `sensitivity_level`+`recipient_role` (`"restricted - DWS Superadmin"`, correctly falling back to the requesting admin's own role when a `ScheduledExport`-supplied one isn't available, matching yesterday's `recipient_role` fallback pattern). Also fixed a stale inline comment on `PendingConfirmation.action_type` that listed only 3 of the real 4 action types (missing `idmt_handoff`). Live-verified all 4 action types with fresh real confirmations, not just code inspection: triggered a real restricted on-demand export (`injury`) -> `"restricted - DWS Superadmin"`; a real gated deactivation of an active SCS account -> `"SCS - no unit"`; a real IDMT handoff prepare -> `"reconditioning - Airman"`; a real admin-level role change (Nutritionist -> DWS Admin, Superadmin-only) -> `"Nutritionist - no unit"`.

**2026-08-23 (later still, yet once more, again): built real `file_size_bytes` on `ReportExport` - honestly `null` until a size is actually measured, never estimated.** User pointed at the Exports page's "Recent exports" panel ("Wing Q3 aggregate · PDF · 8.4 MB · scheduled · sent 28 Jul · 08:00") and asked whether the whole thing was backed. Checked `page.tsx` first: the "Pending confirmations" table (left box) reuses the same `adminStore.pendingConfirmations` array already covered by the `scope_summary` work; "Recent exports" (right box) is a separate hardcoded 4-item array. Checked the real `ReportExport` model against it - `title`/`export_format`/`created_at`/`lifecycle_status` were already real, but no file-size field existed anywhere. Confirmed with the user before building. `download_export`'s own docstring already documents a real, deliberate architectural constraint that shaped the fix: exports "re-run the same report builder live rather than storing sensitive bytes at rest" - so `file_size_bytes` can only honestly be set at the two real moments bytes are actually produced, never guessed at creation for a not-yet-rendered restricted export. Set it in `ReportExportService.export_report()` (`len(content_bytes)`, immediate for a plain/aggregate export) and in the `download_export` route (materializes it the first time a previously-`pending_approval` restricted export is actually downloaded after approval) - persisting only the byte *count*, never the content, so this doesn't reintroduce the "bytes at rest" problem the live-render design was built to avoid. Live-verified both paths for real, not just code inspection: an immediate `wing_weekly_ops` CSV export returned `file_size_bytes: 526`, matching the download response's real `Content-Length: 526` header exactly; a restricted `injury` export correctly showed `file_size_bytes: null` while `pending_approval`, then a different Admin approved it, then downloading it correctly set `file_size_bytes: 953` - again matching the real `Content-Length: 953` header exactly.

**2026-08-23 (later still, yet once more again, more): extended `PATCH /admin/scheduled-exports/{id}` from status-only to a real full edit - closing the ACTION gap under the "Schedules" table.** User pointed at the "Schedules · recurring exports" table's ACTION column (Edit buttons) and asked whether it was backed. It partially was: the real route only ever accepted `{"status": "active" | "paused"}` - there was no way to actually change a schedule's name, cadence, format, or recipient, even though the frontend's Edit button opens the exact same full wizard used for creation (name/cadence/scope/format/recipients), pre-filled with the existing values. Confirmed with the user before building (`report_type`, and therefore `sensitivity_level`, deliberately stays non-editable here - the design's own Edit wizard never re-asks for it either, matching the create-time-only-derivation decision from the `recipient_role`/`sensitivity_level` work). Replaced `ScheduledExportStatusUpdate` with `ScheduledExportUpdate` (all fields optional - `name`/`cadence`/`export_format`/`recipient_role`/`status`) and folded the old pause/resume-only `set_status` into a single `ScheduledExportService.update()` that only touches fields actually given, correctly no-ops (no save, no audit entry) when nothing changed, recomputes `next_run_at` from now when `cadence` changes (same math used at creation), validates `cadence`/`export_format` against the same real allow-lists `create()` uses, and audit-logs a per-field old->new diff (`scheduled_export_updated`, or the existing `_paused`/`_resumed` event names when `status` is the only thing changing, preserving the old audit-log vocabulary for that case). Live-verified the full surface: a 4-field edit (name+cadence+export_format+recipient_role) on a fresh real schedule correctly updated all 4 and correctly recomputed `next_run_at` from `weekly` to a real `monthly` date; the old status-only call shape (`{"status": "paused"}`) still works unchanged for backward compatibility; an invalid `cadence` and an invalid `export_format` each correctly `400`'d; an empty-body call correctly no-op'd (confirmed via a direct audit-log count - no spurious entry written); a bogus schedule id correctly `404`'d; a non-admin correctly `403`'d; and the real audit-log entry for the 4-field edit carried a correct structured `{field: {old, new}}` diff for all 4 changed fields.

**2026-08-23 (later still, yet once more again, more still): realigned the entire report catalog against DOCX's real 9 required contract reports (Table 26) - the biggest single finding of today's Exports-page investigation.** User pointed at the "9 required contract reports" panel and asked whether it was backed. Re-checking DOCX before assuming fabrication (the lesson from earlier today's D1-D6 mistake) turned up Table 26, "Report | Required Sections | Primary Users" - a real, DOCX-verbatim table naming **exactly 9** reports, matching the mock's own "9 required contract reports" title precisely: Monthly OFT Metrics, Quarterly Utilization, Quarterly Injury/Recovery, Assessment Completion, PRS/QCP Support, Leadership Aggregate Readiness, IDMT Documentation Handoff Summary, Medical Records Upload and Access Audit, and Medical History Performance Summary Export. But the real backend's `REPORT_BUILDERS` (also 9 keys, coincidentally) didn't line up: 5 matched cleanly (injury/assessment_completion/utilization/prs_qcp/oft_metrics); the other 4 slots were filled by `audit_log` (a generic log export, not DOCX's #8) and three separate "not DOCX-sourced" leadership variants (`wing_weekly_ops`/`monthly_cohort_review`/`annual_wing_readiness`, each narrower than DOCX's single required "Leadership Aggregate Readiness Report") - meaning 3 of DOCX's 9 required reports (#7 IDMT Handoff, #8 Medical Records Audit, #9 Performance Summary Export) had **no builder anywhere**, despite their real underlying data (`IdmtHandoff`, `MedicalRecord`/`MedicalRecordAccessEvent`, `PerformanceSummary`) already existing from earlier work this session. Confirmed with the user before undertaking a real restructure rather than a field-level fix. Built: new `app/core/contract_reports.py` (`REQUIRED_CONTRACT_REPORTS`, DOCX Table 26 transcribed verbatim - `docx_name`/`required_sections`/`primary_users` per entry); 3 new real report builders in `ReportsService` (`get_idmt_handoff_summary_report`, `get_medical_records_audit_report`, `get_performance_summary_export_report`), each built strictly from real fields already tracked, each explicitly noting what DOCX asks for that isn't tracked and is therefore omitted (e.g. medical-records audit's "anomalies or unauthorized-access flags" - no detection exists anywhere in this backend); a 4th new composite, `get_leadership_aggregate_readiness_report`, replacing the 3 narrower non-DOCX variants for this slot with a real assembly of OPS trend + reconditioning-phase breakdown + utilization summary + open equipment gaps (DOCX's "recommendations" section omitted - no real aggregate recommendation-summary data source exists anywhere). All 4 registered as `restricted`/`aggregate` sensitivity in `REPORT_SENSITIVITY` and added to **both** copies of `REPORT_BUILDERS` (`admin/routes.py` and `scheduled_export_service.py` - the existing module docstring already warns these must stay in sync). New `ReportsService.get_required_contract_reports_status()` + `GET /admin/reports/required-contract-reports`, combining the real DOCX catalog with each report's real generation history (`last_generated_at`/`last_export_status`, derived from the most recent actual `ReportExport` row for that `report_type` - `null`/never if it has genuinely never been generated). Deliberately did **not** reproduce the mock's DUE DATE, named-approver APPROVAL, or "CUI" SENSITIVITY label columns - none of those have any real backing anywhere (no compliance-due-date registry, no per-report named-approver system), and inventing them would repeat exactly the mistake this whole investigation exists to fix. Live-verified extensively: the new endpoint returns all 9 real DOCX reports with correct names; all 4 newly-registered report types actually generate without error (`leadership_aggregate_readiness` returns 200 immediately as `aggregate`; the other 3 correctly return `202 pending-approval` as `restricted`); re-fetching the catalog after generating them correctly moved `generated_at_least_once_count` from 2 to 6 with accurate per-report `last_export_status` (`completed`/`approved`/`pending_approval`); inspected the real rendered CSV content of `leadership_aggregate_readiness` directly and confirmed real dates/cohort-sizes/component-averages, not placeholder text; non-admin correctly `403`'d on the new endpoint.

**2026-08-23 (later still, once more): built real `active_sessions`/`pending_transmission_count`/`confidence_rule` on `GET /admin/system/overview`, closing 2 of the System screen's real gaps and correcting a fabricated threshold value - deliberately left 3 genuinely unbackable metrics unbuilt rather than invented.** User asked to check the entire System screen (banner, 8 quick-metric cards, "Services - status" table, "Threshold rates" table). Checked `admin-store.ts`/`page.tsx` first: every number is hardcoded, same pattern as every other screen this session. But unlike most of today's investigations, this screen already had 2 real, previously-built endpoints (`GET /admin/system/diagnostics`, `GET /admin/system/overview`) specifically constructed to replace exactly this kind of fake panel - `system/diagnostics`'s own docstring already says "every field here is a real, no-cost check, not a fabricated microservice list." Cross-checked every card/row against them: `Questions bank - active: 42` (mock) vs real `46` (matches today's earlier `Contract Question Registry` fix); `Cohort minimum (k): 5` and `Deactivation grace: 14 days` (mock) both exact real matches (`DEFAULT_COHORT_K`, `INACTIVITY_GRACE_DAYS`); `Export approval window: 12h` (mock) vs real `72h` (`EXPORT_APPROVAL_WINDOW_HOURS`) - a real, wrong number, not just missing; `Confidence limit: >80%` (mock) had no real analog at all - `calculate_confidence()` returns categorical `high`/`medium`/`low` labels, no percentage concept exists anywhere. Of the 5 completely unbacked quick-metric cards (Uptime 24h, Active sessions, Pending transmission, Compliance - 55d, Last backup), 2 turned out to be genuinely buildable from real data already tracked, and 3 are not: **built** `active_sessions` (explicitly NOT a real session store - this app is stateless-JWT with no server-side session table - but a real, clearly-labeled proxy: users whose real `User.last_login_at` falls within the last 30 minutes, our own reasonable inference since no design/DOCX source defines "active session," broken into staff/admin/imt by real role) and `pending_transmission_count` (genuinely real, not inferred - `IdmtHandoff` already tracks a real `approved -> transmitted` state transition per DOCX Section 8.5, so counting handoffs sitting in `approved` with no `transmitted_date` yet is a real, not-invented number); **left unbuilt** 24h uptime percentage, a backup-tracking system, and the "Compliance - 55d" day-count - none of these have any real data source anywhere in this backend, and inventing a plausible-looking rule for any of them would repeat the exact mistake this whole session's investigations exist to catch. Also added a 7th real `threshold_rules` entry, `confidence_rule`, writing out `calculate_confidence()`'s actual categorical logic in place of the mock's fabricated `>80%` numeric threshold. Live-verified both new fields for real: `active_sessions` correctly reflected `{total: 2, staff: 1, admin: 1, imt: 0}` matching genuinely-recent test logins; `pending_transmission_count` started at `0`, then a real pending IDMT handoff confirmation was approved by a different Admin through the existing two-person-rule flow, and the count correctly moved to `1` on the next fetch - proving it's reactive to real state, not a static number.

**2026-08-24: pulled a genuine remote update (real Resend email delivery wired into auth), then mapped all 5 `ascend-app` mobile screens against the real backend field-by-field and wrote 5 self-contained integration prompts, then built the one real gap a frontend dev flagged from a live Postman response.** Session resumed with the OPSEC-severity docs/commit sequence from the prior segment (`docs/E2E_TEST_STATUS.md` row confirmed landed, `UPDATE.MD` entry written, committed as `8ab7e0a`, pushed clean). User then asked for 5 field-accurate integration prompts (sign-in, onboarding+home, trends, support, profile) so a frontend engineer could wire `ascend-app` to this backend section by section. Rather than write these from memory, spawned 5 parallel Explore agents (one per section) to re-derive every real endpoint/field/mismatch directly from both codebases, then synthesized their findings into `ascend-app/INTEGRATION_PROMPTS.md` - a shared "Prompt 0" (fixing `baseApi.ts`'s placeholder `baseUrl` and its comment-only "Mock authorization headers" that never actually attaches a token) plus 5 section prompts, each with exact request/response shapes, explicit mismatch call-outs the agent must not silently resolve on its own (e.g. `document_type` dropdown sends `"Labs"` but the backend wants lowercase `"labs"`; support's `fitness`/`injury` topic ids don't map cleanly to backend `pathway_key`s `SCS`/`PT/IM`), and explicit "this doesn't exist, don't fabricate it" notes (no EDIPI field anywhere, no personal PDF export, no peer-cohort percentile). User then said `pull koro` with `ascend-backend/.env` open in the IDE - fetched first (2 new commits, `58a02e5`/`5ea08a7`, diff-stat checked before pulling per the standing malware-recurrence habit: only `config.py`/`main.py`/`auth_service.py` touched, real CORS + Resend-email wiring, not the user's own work this session but a real upstream push), pulled clean fast-forward, confirmed `RESEND_API_KEY` already sits in `.env`. User then pasted a Postman screenshot of a real `GET /users/profile` response and relayed two claims from a frontend dev: no profile-photo field, and no profile-update API. Checked both against real code rather than trusting the claim: the "no update API" claim was wrong (`PATCH /users/profile/settings` is real, just scoped to `rank_grade`/`theme_preference`/`notifications_enabled`); the "no photo" claim was right - grepped the entire backend for `photo|avatar|picture`, zero hits. Checked DOCX before building anything: no profile-photo/avatar requirement anywhere, and the Security/OPSEC section actively *restricts* photo uploads ("No photo/video upload unless specifically enabled and approved for official use") - the DOCX's own "User Profile" data-dictionary row lists only `user_id, name, role, unit, team, assigned_scs_id, assigned_ptim_id, status, activation_date, deactivation_date`. Asked the user directly rather than deciding unilaterally on either a DOCX-contradicting feature or an existing documented admin-only-fields decision (`profile_service.py`'s own docstring: "unlike a CAC-sourced identity model, `role`, `full_name`, and `unit_id` are admin/provisioning-set fields"); user said build the avatar anyway, and to make self-editable "whichever fields genuinely need editing." Built real avatar support reusing the existing `FileStorageService` pattern (new `avatars` namespace, same Fernet-at-rest encryption as medical records/message attachments) - `avatar_storage_path`/`avatar_content_type`/`avatar_uploaded_at` on `User`, JPG/PNG/HEIC only, 5 MB cap, `POST/GET/DELETE /users/profile/avatar` for the caller's own photo plus `GET /users/{user_id}/avatar` (deliberately no team-assignment gate - a photo isn't sensitive PII the way a medical record is) so a provider's avatar can be shown on the Support/chat screens too; `ProfileResponse` gained `avatar_available: bool`. For the edit-scope question, respected the existing documented decision rather than overriding it wholesale: `role`/`unit_id` stayed admin-only (self-edit of either is a real authorization/assignment risk, not a cosmetic preference) but added `full_name` to `PATCH /users/profile/settings` (a name-correction is a legitimate self-service need, and the docstring's own reasoning for the admin-only grouping was "no CAC/PKI to source it from," not "must never be user-controlled") and built a separate `POST /users/change-email` (JSON body, `new_email` + `current_password` - same "prove intent with the current password" pattern `change_password` already uses) since the login-identifier email genuinely had no self-service path before this - on success it resets `is_verified` to `False` and sends a fresh verification code to the *new* address via the just-pulled real `EmailService`, since the old code/expiry were only ever valid for the old email. Live-verified the entire surface end to end with a real Python script (not just curl one-offs): uploaded a real PNG, downloaded it back and confirmed the bytes matched exactly plus the correct `image/png` content-type; deleted it and confirmed a subsequent download correctly `404`'d; a disallowed extension (`.exe`) correctly `400`'d; `full_name` edit round-tripped correctly and an all-whitespace name correctly `400`'d rather than silently clearing to empty; `change-email` correctly rejected a wrong password, correctly rejected a colliding email, and on success the old email could no longer log in while the new one immediately could; queried the real `AuditLog` collection directly and confirmed `avatar_updated`/`avatar_removed`/`email_changed` entries all exist with correct metadata and, deliberately, no password anywhere in them.

**2026-08-24: built real OPSEC severity levels (1-5) for the message content scan, and a real audit trail for blocked send attempts - closing the second of two real gaps found comparing `ascend-app` (the mobile Expo app, pulled fresh from the remote after a force-push) against this backend.** User asked for a full gap comparison between the just-pulled mobile app and the real backend. Read all 34 real screen files in the app; found the app has zero real API integration anywhere (`baseApi.ts` is an empty RTK Query stub against a placeholder URL, `endpoints: () => ({})`) - same "frontend built, not wired" pattern as `ascend-admin` - but almost every screen's hardcoded placeholder text describes this backend's *real* fields/rules with striking accuracy (e.g. Checkin Gateway's literal `"first_use_state.weekly_cadence"` and "Cadence resets every Tuesday at 0600 local" text matches `app/core/cadence.py`'s real `WEEKLY_RESET_WEEKDAY`/`WEEKLY_RESET_HOUR` exactly; "My team"'s "Send a message (Open in v1.1)" disabled state for the 3 optional pathways matches the real, already-documented `messaging_v1_supported: True` restriction to SCS/PT-IM only). Found exactly 2 genuine gaps: (1) CAC/PIV/EDIPI smart-card authentication with backup codes and IP/device/location sign-in history (`profile/activation.tsx`) - explicitly excluded from this pass per the user's own instruction, a whole separate DoD authentication subsystem this backend doesn't have any part of; (2) graduated OPSEC keyword severity ("Level 5 (highest) never routes through messaging", `data-use.tsx`/`support/chat.tsx`) - the real `scan_for_opsec_terms()` (`app/core/notification_rules.py`) already existed and already blocked every match, but as an undifferentiated flat 10-term list with no severity concept at all. Re-checked DOCX first (0 hits for "OPSEC severity"/"keyword scan") - not a requirement, a real new-scope Figma-triggered addition, same category as several other features built this session. Built `OPSEC_TERM_SEVERITY` (1-5 per term, our own reasonable classification - 5 for unambiguous geolocation/movement terms like "grid coordinates"/"troop movement"/"convoy", down to 3 for higher-false-positive-risk planning words like "mission"/"deployment"), `opsec_severity()`/`highest_opsec_severity()` helpers, all added without touching the existing `scan_for_opsec_terms()` signature/behavior at all (6 call sites across `medical_record_service.py`/`notification_service.py`/`support_service.py`/`messaging_service.py` stayed untouched except the 3 real blocking sites in `messaging_service.py` that needed severity surfaced). **Deliberately did not weaken the existing block-on-any-match security behavior** - every severity still blocks the send; severity is real added metadata for audit/reporting granularity, not a loosening, since an unreviewed "let lower severities through" change would be a real security regression with no basis to make unilaterally. Also caught and fixed a real, adjacent gap while in this code: a blocked send previously left **no audit trail at all** - `send_message`/`send_group_message` now write a real `message_blocked_opsec` audit entry (with the real blocked terms + severity) before raising the 400, closing a real, silent audit-coverage hole that existed independent of the severity feature. Live-verified extensively: `POST /messaging/scan` correctly returns `severity: 5` for "grid coordinates", `severity: 3` for "mission", `severity: null` for clean text; a real blocked send attempt (PT/IM -> Superadmin, "unit location"/"convoy") correctly `400`'d with `severity: 5` and wrote a real, verified `AuditLog(event_type="message_blocked_opsec", outcome_status="blocked")` entry with the exact matched terms and severity; a second real attempt with a Level-3 term correctly returned `severity: 3`; a genuinely clean message correctly still sent successfully (`201`) - no regression to normal messaging.

**2026-08-25: diagnosed a real ngrok interstitial masquerading as a CORS bug, then ran a 6-agent parallel gap analysis across the entire `ascend-admin` staff dashboard (9 role pages, ~19K lines) against the real backend - report only, nothing built.** User reported a browser CORS error hitting `/users/profile` through an ngrok tunnel from a mobile-web client on `localhost:8081`. Verified the real `CORSMiddleware` config first (`allow_origins` from `CORS_ORIGINS`, defaulting to `["*"]`) - a real preflight `OPTIONS` call against the tunnel returned correct CORS headers. The actual `GET` call through the tunnel with a browser User-Agent returned `Ngrok-Error-Code: ERR_NGROK_6024` with zero CORS headers - ngrok's free-tier browser-warning interstitial intercepting the request before it ever reached FastAPI, not a backend bug. Confirmed the fix (`ngrok-skip-browser-warning: true` header) actually resolves it by re-running the same request with that header and getting a real `401` from the real backend (proving the request reached `uvicorn`) instead of ngrok's HTML page. User then asked to check whether `ascend-admin`'s dashboard has all its backend built - given the last full pass stopped 2026-08-13, and `ascend-admin`'s `git log` showed a huge rewrite landed 2026-08-14 (`21d47cc`, "Update dashboard to new-feature-branch snapshot," 8,842 insertions across all 8 role dashboards plus a new privacy/governance UI layer), this was genuinely new ground. Split into 6 parallel Explore agents (Admin; Leadership; SCS; PT-IM; MP+Nutritionist+Chaplain combined; Profile+Roles+Plan+Auth pages combined), each told explicitly not to re-flag "not wired to a real API yet" as a gap (established, separately-tracked) and to report only genuine zero-backing concepts or real frontend/backend contradictions. Findings, compiled: **the backend itself is essentially complete** - every real DOCX/design-grounded concept has real backing. Most of the Admin-dashboard findings turned out to be *already-known, already-fixed-on-2026-08-23* mismatches (Questions bank 42 vs real 46, export window 12h vs real 72h, `>80%` confidence vs the real categorical rule, 6-of-9 required reports shown) - the frontend snapshot (08-14) simply predates those backend fixes (08-23), not a new gap. But several genuinely new, real fabrications turned up: Leadership's entire "Recovery program" concept (explicitly named in `briefing_service.py`'s own docstring as an example of screenshot language the real templates were built to exclude) and a `L1`-`L5` risk-heatmap labeling scheme that collides with the real, differently-meaning routing-escalation `L0`-`L5`; SCS's "OFE" metric (a string that appears nowhere else in the entire frontend or backend - every other surface correctly says "OPS") and fabricated PT-session-scheduling/leave-tracking/plan-template-gallery concepts with zero backend model; **PT-IM's most serious finding** - a "Visibility level" selector offering "PT/IM + IDMT recipient" as a real record-visibility option, directly contradicting `idmt_handoff.py`'s explicit design statement that "IDMT is a summary recipient, never a raw-record viewer" - plus an entirely fabricated structured-EHR view (immunizations/allergies/medications) replacing the real 6-field `PerformanceSummary`, and a fake OFT "/100" score; Nutritionist's fully fabricated per-meal macro/calorie tracking *and*, in the opposite direction, raw clinical data shown with zero redaction where the real rule requires masking for a non-clinical role; MP's invented note-visibility ACL/lock-and-sign/escalation system contradicting the real `SpecialistNote`'s two real fields; Chaplain's entirely fabricated reflection/journal entity; and, in the smaller shared pages, real security-relevant frontend bugs independent of any backend gap - a forgot-password flow that leaks account existence (the opposite of the real `AuthService.forgot_password`'s explicit anti-enumeration design), a 6-digit reset-code UI against the real 4-digit format, a password-strength check weaker than the real 8-char/uppercase/digit policy, and fake "AES-256"/"compliance check" security-theater text on the login screen that performs no such operation. Presented the full findings to the user; explicitly asked whether to act on any of it. **User chose report-only - nothing built or changed**, since virtually everything found is a frontend-side problem (stale mock data or invented content), not a backend gap, matching this session's long-standing frontend/backend scope boundary.

**2026-08-25: converted all 3 raw-binary file-response endpoints (avatar, medical record file, message attachment) to base64-encoded JSON, and added the real self-service deactivation-request endpoint to the Postman collection where it was missing.** User pointed out two things after testing in Postman: `GET /users/profile/avatar` returns a raw image instead of JSON, and the real deactivation-request endpoint couldn't be found under the Activation folder in Postman. The raw-image behavior was intentional (same pattern already used for medical record files and message attachments, so a client can use the URL directly as an image source) - but the user explained their frontend dev's tooling can't handle raw binary responses well and asked for JSON everywhere, "shob jaygay" (everywhere). Rather than push back, implemented it as asked: added a shared `guess_content_type()` helper (`app/services/file_storage_service.py`, a real extension->MIME map for pdf/dcm/jpg/jpeg/png/heic) and converted `GET /users/profile/avatar`, `GET /users/{user_id}/avatar`, `GET /messaging/message/{id}/attachment`, and `GET /records/uploads/{id}/file` to all return the standard `{message, data, meta}` envelope with `data = {file_name, content_type, file_size_bytes, content_base64}` instead of a raw `Response`. Added a real `avatar_file_name` field to `User` (previously only inferrable, awkwardly, from the encrypted storage path) so the avatar response has a clean real filename rather than a hacky path-parsing hack. Separately, confirmed via a live test that the *real* self-service deactivation endpoint (`POST /users/deactivation-requests`, built earlier this session) works correctly end to end (`201` on first request, real `400` "already pending" on a second) - the user's confusion was that the mobile app's own `activation.tsx` screen doesn't call it yet (confirmed dead frontend button, already documented in `INTEGRATION_PROMPTS.md`), and that this real endpoint was simply missing from the Postman collection's Activation folder - added it there now. Live-verified all 3 converted endpoints end to end with a real Python script (not assumed): uploaded a real PNG avatar, fetched it back as JSON, base64-decoded it, and confirmed the bytes matched exactly plus correct `content_type`/`file_name`/`file_size_bytes`; uploaded a real medical record and confirmed its file-download JSON round-tripped identically; sent a real message with a real attachment (registering an admin-role recipient to bypass the real team-assignment gate rather than faking one) and confirmed the attachment-download JSON round-tripped identically too. Postman collection regenerated (94->97 KB, avatar/attachment/record-file items now documented as JSON not raw, deactivation-request added).

**2026-08-25, later: reverted the avatar endpoint specifically from base64 JSON back to a real, public, direct image URL - the JSON-everywhere change from a few hours earlier was right for medical records and message attachments, wrong for avatars.** User's immediate follow-up: "decode nah direct url dao, image er jonne" (don't make me decode, give me a direct URL, for images) - a plain request that base64+client-side-decode is the wrong shape specifically for something meant to render as an `<img>`. Reasoned through why avatar is different from the other two: medical records and message attachments are access-controlled and not always images (PDFs, DICOM) - JSON+base64+auth stays correct for those, unchanged. Avatars were already established (both times this feature was built and re-touched today) as *not* sensitive PII, with *no* team-assignment gate even when they required auth - meaning dropping the auth requirement entirely for the by-id GET doesn't change who could already see the photo, only how conveniently. Removed the now-redundant `GET /users/profile/avatar` route (own avatar) entirely and changed `GET /users/{user_id}/avatar` to (a) require no authentication at all and (b) return raw image bytes again - a real, working `<img src>`/`Image source={{uri}}` URL, since browsers and RN's Image component don't attach bearer tokens to plain URLs. Added a real `avatar_url: str | None` field to `ProfileResponse` (replacing the now-redundant `avatar_available: bool`) so the client never has to hand-build the path - null if no avatar, else the real `/users/{id}/avatar` path (host-relative on purpose, since this backend has no way to know its own externally-visible URL, especially behind an ngrok tunnel like the one just diagnosed hours earlier). Also added the same `avatar_url` to `AssignedProvider` (the SCS/PT-IM entries already surfaced on the Profile screen) so a provider's photo can be shown on Support/chat screens too, matching the reasoning already written into the endpoint's own docstring - built without an extra per-provider DB lookup to check upload status first, since a provider with no avatar simply 404s the same way a broken `<img>` would, handled client-side like any other. Live-verified end to end: `avatar_url` is `null` before any upload; after uploading a real PNG, the returned `avatar_url` matches the real user id; fetching that exact URL with **zero Authorization header** returned the real image bytes byte-for-byte with the correct `Content-Type: image/png`; a second user with no avatar uploaded correctly `404`'d at their own avatar URL. Postman updated to match (own-avatar item removed, by-id item switched to `NOAUTH`), regenerated cleanly.

**2026-08-25, later still: built real backing for the 2 Leadership dashboard fabrications from the 6-agent audit the user explicitly chose to act on - a real "Recovery program" caseload aggregate, and a real non-colliding risk-severity scale.** Of the audit's several findings, the user singled these two out to build (the rest stayed report-only). Verified both against the real code first rather than trusting the audit summary blind: `briefing_service.py`'s own docstring already named "recovery signal" as an example of screenshot language the real `SECTION_CATALOG` was deliberately built to exclude - confirmed there was genuinely no real cohort/before-after tracking anywhere; `get_risk_heatmap()` confirmed to return only band strings (Ready/Monitor/.../Unavailable), no L-anything. Asked the user directly on both before building, since each meant either fabricating a new tracked concept or adding a 3rd L-prefixed severity scale to a codebase that already has 2 real, differently-meaning ones (`routing_levels.py`'s clinical L0-L5, and reconditioning's own injury L1-L4, per `app/schemas/reconditioning.py`'s own comment about reusing that exact vocabulary deliberately rather than inventing a new one) - user said build both. For "Recovery program": rather than invent a fabricated adherence/before-after metric, reused the real `ReconditioningPlan` data the same way `CoverageService.get_reconditioning_load_by_flight` already does for SCS (confirmed that real, already-existing endpoint first, rather than duplicating unknowingly) - new `LeadershipAggregateService.get_recovery_program_summary()`, k-gated under Leadership's own configured cohort-k (not SCS's), returning real per-flight active-plan counts, a real phase distribution, and an honestly-defined `on_track` signal (no plan in that flight has an overdue `next_review_date` - not an invented percentage). Wired into `GET /dashboard/leadership/aggregate` as `recovery_program_summary`, and into a new real `recovery_snapshot` briefing section type (`SECTION_TITLES`/`_get_section_data`/`_build_section_stub`), replacing the fabricated "recovery signal" narrative in the `recovery_rollout` template with this real data. For the risk-heatmap severity: added `RISK_SEVERITY_BY_BAND` (Ready=1...High Priority=5) and a `risk_severity_label()` helper producing `"R1"`-`"R5"` (R for Risk, deliberately not L-prefixed, documented inline exactly why) - `get_risk_heatmap()` now returns a real `driver_severity` dict alongside the existing `driver_bands`, same cells, just the non-colliding numeric view. Live-verified end to end with real seeded data (not assumed): created a real flight with exactly 5 users (meeting the real cohort-k=5 minimum) each given the same real component-score profile averaging to 58 - confirmed the real heatmap cells all correctly banded `"Caution"` with `driver_severity: "R3"`, matching `DEFAULT_BAND_THRESHOLDS` exactly; created 2 real `ReconditioningPlan`s in that flight (1 with an overdue `next_review_date`, 1 not) and confirmed `recovery_program_summary` correctly reported `active_plan_count: 2`, `overdue_review_count: 1`, `on_track: false`, and the correct real phase-distribution labels; created a real `recovery_rollout` briefing and confirmed its outline included the new `recovery_snapshot` section with the real title, and that its real (non-stub, actual Claude-generated) narrative accurately described the seeded data by name ("Recovery Test Flight ... not on track and has 1 overdue review") rather than falling back to the generic "No data available" placeholder. Postman regenerated, TASKS.MD/CHANGELOG/E2E_TEST_STATUS/UPDATE.MD all updated.
